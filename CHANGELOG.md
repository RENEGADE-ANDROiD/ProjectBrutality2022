# Changelog

All notable changes for this working tree are documented here. Earlier history lives in `git log`; this file tracks the consolidated **2026-04-21 → 2026-04-30** session (and follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) style).

## [Unreleased]

### Added

- **Burning corpse FX throttle (`PB_BurnFXThrottle.zc`, `BURN.dec`, `CVARINFO`, `GoreDebrisMenu`):** Tracks active **`BurningZombie*`** / **`GenericBurningGuy`** actors and scales down **`BurnParticles`**, **`ExplosionParticleVerySlow`**, and **`TinyBurningPiece3`** spawns when many are alive at once. Menu: **Gore/Debris → Burning corpse FX**; **`pb_burnfx_throttle`** (default on). Honors **`LowGraphicsMode`** / **`pb_lowgraphicsmode`**. Tiers use wider bands + higher spawn odds than the first pass (full FX until **`soft_limit + 1`** burners; heavy cut not until **`soft_limit + 6`**).
- **Live incinerate FX throttle (`PB_BurnFXThrottle.zc`, `PBMonster.zc`, monster `Death.Incinerate`):** Same load counter includes monsters still playing **`Death.Incinerate`** (pre-**`BurningZombie*`** corpse). **`PB_Monster`** actions gate sparks, embers, explosion puffs, and tiny burn pieces; **`AccuDeath`** fire particles honor the same tiers.

### Changed

- **Kill streak decluster (`PB_KillstreakThrottle.zc`, `PB_Killstreak_Handler.zc`):** **`PB_KillstreakThrottleHandler`** always throttles **5-kill rewards**, **announcer** lines, and **gold-globe** rolls during kill bursts (large roster fights). Hardcoded: **4 s** minimum between rewards, **3 s** burst window, density tiers from **5** kills in-window. No extra menu/cvars. Streak counter still climbs when a grant is skipped.

- **Kill streak HUD copy (`PB_Killstreak_Items.zc`, `PB_Killstreak_Handler.zc`):** White reward subtitles no longer repeat **Kill streak:** (title line already shows the milestone). Shorter reward names (**Haste!**, **Backpack!**, pinata labels, etc.); announcer text fallback is a single caption; globe pickup **Streak active!**; default title fallback **Reward!**.

- **Meat Hook chain trail (`SPRITES/equipment/HookChain/TENDA0.png`, `HookTrail` in `PlayerPawn.zc`):** Replaced **`TENDA0`** with **`warcarlssonhook`** chain art. **`HookTrail`** drops **`+FORCEXYBILLBOARD`**, uses **`+ROLLSPRITE`** + **`OrientAlongRope()`** (angle/pitch along the grapple line, roll **90°** so upright lump reads flat along the rope).

- **`MENUDEF.txt` / `language.enu` — PB 2022 Enhanced menu full pass:** Rebuilt **`PB_2022AdditionsMenu`** as a short hub (Quick Presets + category submenus). New sections: **Gameplay & Movement**, **Combat & Finishers**, **Gore & Performance**, **HUD & Interface**, **Weapons & Spawns**, **Monsters & Spawns**, **Extras & Content**. **Quick bundle** `Command` rows (Simpler Monster-style) for movement, combat, gore, HUD, monsters, and extras. Deduped weapon wheel / tactical motion (canonical home **HUD & Interface**). **HP arcade** (`pb_hp_arcade`), **item mutators** (`pb_item_mutators`), and **Custom Red Armor** (`pb_redarmor_spawn`) live under **Extras & Content** → **Pickup Rules** (removed from **Weapons & Spawns**). Title-case labels, **gold/cyan/green/orange** section headers, multi-line tips (no wall-of-teal), **cvar names** kept on hint lines. Main/options entry renamed **PB 2022 Enhanced**.

- **`PB_DemonExterminator` — fire + weapon special (`PB_DemonExterminator_Functions.zsc`):** **`UNM_WeaponReady`** now **`return A_DoPBWeaponAction(...)`** so ready can branch to **`Fire`** / **`WeaponSpecial`** (calling it without returning left the gun stuck on idle). **`Fire`/`AltFire`** barrel helpers return their redirect state; ammo **`A_JumpIfInventory`** uses numeric thresholds (4/6/12/12/20/30).

- **`PB_DemonExterminator` — ZScript re-port (Weapons Pack `DemonicExt` pattern):** Weapon logic moved to **`zscript/Weapons/Slot9/DemonExterminator/PB_DemonExterminator.zs`** + **`PB_DemonExterminator_Functions.zsc`**; **`DemonExterminator.dec`** keeps ammo/mode tokens/projectiles only. **`ExterminatorMode`** drives sprite sets: laser/incineration **`UNMS`/`UNMD`/`UNMI` A–H** vs lightning **`UNMS`/`UNMD` F–J** + **`Ready.Lightning`** **`UNMI` I/J/K** loop. Fire mode is chosen with the **weapon special wheel** (**`Select_PB_DemonEx_*`**, not cyclic tap). All modes spend **`PB_DTech`** only.

- **Burning corpse FX throttle — lighter cut (`PB_BurnFXThrottle.zc`):** Full FX for one more corpse before tier 1; wider steps to heavy/off tiers; ~75% burn particles / 50% embers at tier 1; tier 3 keeps ~25% particles instead of zero.

- **Burn FX throttle — live incinerate (`PB_BurnFXThrottle.zc`, `PBMonster.zc`, monster `Death.Incinerate`):** Load counter now includes monsters in **`Death.Incinerate`** (melt animation before **`BurningZombie*`** spawns). **`PB_Monster`** throttled actions for sparks, embers, heavy/very-fast puffs, tiny burn pieces, and plasma smoke; **`AccuDeath`** fire particles use the same tier gates.

- **`BHGen` (Black Hole Generator) — reverted to DECORATE:** Removed **`zscript/Weapons/Slot8/BlackHole/BHGen.zs`**; weapon is **`Actor BHGen : PB_Weapon`** in **`actors/Weapons/Slot8/BLACKHOLE.dec`** again. Kept post-port fixes: **`Ready3`** uses **`Loop`** (not **`Goto Ready+8`**), fire/alt/special return **`Goto Ready3`**, select ends **`Goto Ready3`**, fatality/cant-fire/cell guards on **`Fire`**, **`Inventory.Icon` / `Inventory.AltHUDIcon`**. **`maint_scripts/Restore-BHGenDecorate.ps1`** for repeat restore.
- **ZScript `FlashPunching*` states — reverted bulk `Stop` pass:** Restored **`Goto Ready3`** (or weapon-specific ready labels, e.g. **`X12ReadyToFire`**) on ported guns; shared **`PB_WeaponBase`** **`QuickMelee`** PSP_FLASH clears make that safe for most weapons. **`ArgentSith`** / **`BeamKatana`** keep **`Stop`** on **`FlashPunching`** where a long flash strip or custom melee path needs it.

- **Core roster ZScript weapon ports (batch, `RiotShield` skipped):** Remaining high/medium-priority **`PB_Weapon`** guns moved to **`class … : PB_WeaponBase`** under **`zscript/Weapons/`** with DECORATE shards trimmed to tokens/projectiles and **`#include`** block in **`ZSCRIPT.zc`** (`// === Remaining DECORATE weapon ports (auto) ===`). **High:** **`Rifle`**, **`PB_Pistol`**, **`PB_BFG9000`**, **`PB_M2Plasma`**, **`PB_LMG`**, **`PB_Nailgun`**, **`PB_MP40`**, **`MarauderSSG`**, **`PB_Freezer`**, **`PB_Flamethrower`**, **`Hell_rifle`**. **Medium:** **`PB_Fusil`**, **`PB_SideFusil`**, **`PB_XM21`**, **`X12Shotgun`**, **`PB_DemonExterminator`**, **`PB_Unmaker`**, **`BioAcidLauncher`**, **`BHGen`**, **`ProSurv_Ballista`**, **`OldHMG`**. **Cat's Frozen:** **`PB_CryoASG`**, **`PB_CryoElectroRifle`**, **`PB_CryoCannon`**. Tooling: **`maint_scripts/Port-RemainingWeapons.ps1`**, **`Convert-DecorateWeaponToZScript.ps1`** (brace scan ignores **`//`** comments; inline **`States {`** closes on actor tail **`}}`**), **`Fix-ZScriptWeaponZs.ps1`**, **`maint_scripts/Smoke-GZDoom.ps1`** (**`-Norun`** log scan).

### Fixed

- **`PB_DemonExterminator` disabled — ZScript compile (`PBPDA.zc`, `PB2022_Hud_Weapons.zs`, `MastermindChaingun.dec`):** Stripped HUD/PDA mode branches and **`DEx_Cur*`** inventory checks after **`DECORATE`** / **`ZSCRIPT.zc`** includes were commented out (unknown class **`DEx_Cur2`** / **`PB_DemonExterminator`** at parse). Removed orphaned Mastermind chaingun **`ChooseUpgradePath`** take.

- **`PB_DemonExterminator` — wrong Soulfire mode + sprite strips (`PB_DemonExterminator.zs`, `PB_DemonExterminator_Functions.zsc`, `DemonExterminator.dec`, `ev_core_special.zsc`, `ZSCRIPT.zc`):** Re-aligned with Weapons Pack **`DemonicExt`**: weapon special wheel is **Laser / Incineration / Lightning** (not Soulfire). Mode **2** (pack `SelectSoul` / `Ready.Soul` labels) is **Lightning** — **`UNMS`/`UNMD` F–J** + **`UNMI` I/J/K** idle/fire. Removed **`DemonEx_Soul`** ammo; all modes spend **`PB_DTech`**. Restored **`WeaponSpecialLayer`**, **`UMFL`/`UMTB`/`D3T2`** overlays, laser **`AltHold.Laser`**, and **`LightningFlash`** **`UNHL`** lumps (authored under **`Flash/`** from **`LFX1`** when upstream **`UNHL`** was missing). **`UNMI` I/J/K** also copied into **`Idle/`** for reliable lump resolve. Fixed **`ZSCRIPT.zc`** include order (class before **`extend`**) and **`UNM_FireStorm`** (no missile return assign on 4.5.0).

- **`BioAcidLauncher` — could not switch weapons after alt-fire / flash punch (`BioAcidLauncher.dec`):** Fire/alt-fire used **`A_Overlay(-4, "CausticFlash")`** and quick melee used **`FlashPunching`** on **`PSP_FLASH`**, but neither state was defined on the gun (only on **`Hell_rifle`**). Stuck psprites on those layers kept **`A_DoPBWeaponAction`** from reaching a normal ready loop. Added local **`CausticFlash`** / **`FlashPunching`** / **`FlashPunchingStop`** states, **`Stop`** on the flash overlay (with **`A_ClearOverlays(PSP_FLASH, …, false)`**), and clear layer **`-4`** when primary/alt fire ends and on **`Deselect`**.

- **`Paingiver` — startup fatal `Unknown actor class Paingiver` (`Paingiver.dec`):** A partial ZScript port left **`Paingiver.dec`** as support actors only (**`SeekerRocket`**, **`PaingiverRespected`**) while **`zmapinfo.txt`** / spawners still referenced **`Paingiver`**. Restored full **`ACTOR Paingiver : PB_Weapon`** from **`fdc95b6b0`** via **`maint_scripts/Restore-DecorateWeapons.ps1`**.

- **Reverted flash-punch “no blink” experiment (`BaseWeapon.zc`, weapon `FlashPunching` states):** Rolled back QuickMelee on overlay **`-776`**, **`QuickMelee` → `Stop`**, and per-weapon **`FlashPunching` → `Stop`** — in-game behavior regressed. Restored **`PSP_WEAPON` QuickMelee** + **`Goto Ready3`** return (PSP_FLASH punch layer unchanged). Shield-saw throw/catch fixes in the same files were **kept**.


- **Shield Saw — no launcher click on unavailable quick melee (`BaseWeapon.zc`, `BaseWeapon.dec`):** Cooldown / in-flight saw no longer routes through **`ShieldSawNotReady`** (**`Equip_notready`**). **`ThrowShieldSaw`** falls back to **`QuickMeleePunchSetup`** / **`GoMeleePunchSetup`** instead; DECORATE **`QuickMelee`** uses an explicit **`return PB_ResolveQuickMeleeShieldThrow()`** block so the resolve helper is not mis-invoked.

- **Shield Saw throw / catch — weapon select SFX (`BaseWeapon.zc`, `BaseWeapon.dec`, `ShieldSaw.dec`):** Throw/catch ended with **`Goto Ready3`**, which hit the shared **`PB_WeaponBase`** stub (**`Ready3` → `SelectAnimation`**) on guns without their own **`Ready3`** and replayed equip/select audio on the still-held weapon. **`ReturnFromShieldSawAnim`** + **`PB_ReturnFromShieldSawToReady()`** resume **`Ready3`** when the weapon defines one, otherwise **`Ready`** with one-shot **`PB_SkipWeaponSelectAnim`** so **`PB_RespectIfNeeded`** does not branch into **`SelectAnimation`**.

- **Shield Saw throw / catch animation + projectile spawn (`BaseWeapon.zc`, `BaseWeapon.dec`):** Restored GK wind-up offsets (**`SHIE` J/K steps toward `(160,118)`**, not absolute **Y 200**) and dropped the extra **L/M/N/O** follow-through strip that shoved the sprite off-screen. **`PB_FireShieldSawThrown`** spawn height back to **`-2`** (was **`-32`**, which launched **`SHDP`** below the crosshair). Catch anim mirrors the wind-up in reverse back to **`(0,32)`**.

- **Four weapons reverted to DECORATE (`HellPistoler`, `Paingiver`, `BioAcidLauncher`, `OldHMG` / UAC-320 HMG):** Removed ZScript weapon classes and restored full **`Actor … : PB_Weapon`** blocks from pre-port **`fdc95b6b0`** in each **`.dec`** file. Deleted matching **`zscript/Weapons/**`** ports; **`ZSCRIPT.zc`** includes removed. Kept port-era HUD icons: **`BioAcidLauncher`** **`BPBLM0`**, **`OldHMG`** **`HMGNA0`**. **`PB_DemonExterminator`** was re-ported to ZScript separately (see **Changed**). **`maint_scripts/Restore-DecorateWeapons.ps1`** for repeat restore.

- **HASG corner weapon HUD icon (`PBPDA.zc`, `GRAPHICS/pywheel/weapons/HASG.png`):** **`0ESGA0`** is a wide pickup strip with heavy transparent padding — contain-fit shrank the visible gun in the portrait box. Portrait now prefers **`graphics/pywheel/weapons/HASG.png`** (Golide side art from **`GLDRA0`**) and draws **`HASG`** / extreme-aspect icons with **cover-fit** + clip so they fill **`pb_weaponhud_maxwidth` × `pb_weaponhud_maxheight`** like other slot weapons.

- **`VorpalBlade` — Shield Saw quick melee (`VorpalBlade.zc`):** Custom **`QuickMelee`** skipped **`PB_ResolveQuickMeleeShieldThrow()`** and always ran the sword **`GoMeleeInstead`** path. Restored GK parity checks (**`ShieldSawThrown`**, **`A_JumpIfCloser(99)`**, inherited **`ThrowShieldSaw`**) before the sword swing.

- **Burn FX throttle — startup `Invalid parameter 'A_PB_BurnFX_*'`:** Bulk patches applied throttle actions on actors missing the ZScript parent that defines them. **`maint_scripts/fix_burnfx_non_pbmonster.py`** restores vanilla melt FX on non-**`PB_Monster`** monsters (pinkies, cacos, barons, etc.). **`BURN.dec`:** burning corpse actors that call **`A_PB_BurnFX_*`** (including **`BurningImp`**, **`BurningDemon2`**, **`BurnedDemon2`**, etc.) now inherit **`PB_BurningFXActor`** (same as **`GenericBurningGuy`** / **`BurningZombie`**) so corpse **`A_PB_BurnFX_BurnParticles*`** calls compile. **`Trite.zc`** uses direct spark spawn (**`Trite : Actor`**).

- **`BHGen` (Black Hole Generator) — weapon state lockout / vanishing after flash punch (`BHGen.zs`):** Auto ZScript port used **`Goto Ready+7/+8`** after primary fire. **`SelectContinue` → `Goto Ready3`**, fire/alt/special bail to **`Ready3`**. **Quick melee:** inherited **`QuickMelee`** ended with **`Goto Ready3`** on the **`PSP_WEAPON` overlay**, stacking a second **`Ready3` `Loop`** (`A_DoPBWeaponAction`) on top of the real idle loop and killing the view weapon. BHGen now overrides **`QuickMelee`** / **`QuickMeleePunchSetup`** to **`Stop`** (+ **`A_GunFlash("Null")`**, offset reset); **`FlashPunching`** / **`FlashPunchingStop`** on **`PSP_FLASH`** also **`Stop`**. Ambient **`DSNUKBID`** (chan **6**) is stopped for the punch window (`docs/punch_flash_psp_flash.md`).

- **Batch ZScript weapon ports — compile (`maint_scripts/Fix-PortedWeaponCompile.ps1`, ported `*.zs`):** Post-port pass fixes DECORATE leftovers: **`+WEAPON.POWERED_UP`**, **`return ResolveState`**, quoted multi-frame sprite lumps, **`A_StartSound`/`A_StopSound`** use **`CHAN_AUTO`** (not **`"Auto"`** — including **`CHANF_OVERLAP`** stop-fire on **`PB_Flamethrower`**), **`A_RailAttack`** puff colors **`-1`** with puff name **`'none'`** (**`PB_XM21`**, **`PB_Unmaker`**; avoid blind regex that corrupts **`'none'`** into **`''none''`**), **`A_Quake`** sound arg **`0`** instead of **`'none'`**. **`MarauderSSG.zs`** semicolon after inline **`A_GunFlash`**. UZDoom **4.14.3** **`-norun`** smoke (**`maint_scripts/Smoke-GZDoom.ps1`**, **`doom.wad`**): **0** `Script error` / `Script warning`. Do **not** run **`Fix-ZScriptWeaponZs.ps1`** globally on hand-ported **`Deagle` / `Shotgun` / `NeoHMG`** — it corrupts **`else`** / state blocks and mis-replaces **`CHAN_AUTO`** on **`A_StartSound`**.

- **PBX corner weapon / fire-mode HUD — hard clip removed (`PBPDA.zc`, `CVARINFO`):** Portrait overlay no longer uses cover-fit + `DTA_Clip*` (that pair scaled icons past the max box then cut them at the edges). Both overlays use contain-fit without screen clipping; PDA blocky preview tiles still opt into clip. Default portrait/mode max box **128×96** / **64×64** (was **96×72** / **56×56**).

- **PBX corner weapon / fire-mode HUD icons clipped (`PBPDA.zc`):** Portrait and mode overlays treated **`pb_weaponhud_x/y`** (and mode offsets) as the **texture center** while **`DrawClipFittedTexture` cover-fit** clipped to the quadrant above-left of that point — icons looked tiny, cut off, or floating. Draw position is now the **center of the max-width × max-height box** anchored from the screen bottom-right; cover-fit and contain-fit share a symmetric clip rect around that center (PDA blocky tiles already passed center coords).

- **Battle Rifle ADS semi-auto (`BDPBattleRifle.zs`):** Scoped fire is one round per trigger pull again. **`ReadyADS`** no longer enters **`FireADS`** every tic while **`+attack`** is held; **`FireADS`** drops the hip-fire triple tap and **`A_Refire("FireADS")`**, using **`JustPressed(BT_ATTACK)`** on the recovery frame (same pattern as **`PB_MetalSniper`**). Hip-fire **3-round** burst + **`A_Refire("Fire")`** unchanged.

- **PBX corner weapon HUD after ZScript ports (`PBPDA.zc`, ported `*.zs`, `XM21.dec`):** **`PB2022GetModeIcon`** now initializes to **`PB2022_NoTex()`** so weapons without a fire-mode glyph (e.g. **`PB_Pistol`**) no longer draw random textures when **`pb_weaponhud_enable`** is on. Portrait resolve prefers **`Inventory.Icon`** then **`AltHUDIcon`** from the live weapon and class default before **`Spawn`** walks; extreme-aspect lumps are no longer replaced by **`Spawn`** frames except **`HASG`**. Spawn fallback skips **`PLAY*` / marine / fist** sprites. Ported guns restored explicit **`Inventory.Icon`** (**`PB_Carbine`**, **`PB_QuadSG`**, **`PB_Autoshotgun`**, **`PB_SSG`**, **`PB_SMG`**, **`PB_Minigun`**, **`PB_MG42`**, **`PB_RocketLauncher`**, **`Railgun`**, **`PB_XM21`**).

- **Tier A/B ZScript smoke compile (`Carbine.zs`, `LeverAction.zs`, `PB_Minigun.zs`, `PB_SMG.zs`, `PB_SSG.zs`, `PB_Autoshotgun.zs`, `PB_RocketLauncher.zs`, `HASG.zs`, `PB_MG42.zs`, `HellPistoler.zs`, `PB_DarkMatterRifle.zs`, `maint_scripts/Fix-ZScriptWeaponZs.ps1`):** GZDoom **`-norun`** against **`DOOM2.WAD`** + loose mod folder now reports **0** `Script error` lines. Port fixes: quote digit/alpha sprite lumps (case-sensitive matcher — do not quote **`Goto`**); overlay blocks use **`return ResolveState(...)`** / **`return ResolveState(null)`** (not **`return state("")`**); **`A_DoPBWeaponAction(WRF_*|WRF_*)`** flag OR (not extra string args); **`PB_CheckReload(null,…)`**; **`A_RailAttack`** puff colors **`-1`** + **`'none'`**; **`A_PlaySoundEx`** channel **`"SoundSlot5"`** / **`"Auto"`**; **`A_DestroyLaserPuff()`** / **`A_Refire()`**; HASG drum path **`A_JumpIfInventory("Unloaded_Golide", 1, "LoadMag")`** (DECORATE offset **`0`** is invalid in ZScript).

- **HASG corner weapon HUD (`PBPDA.zc`, `HASG.dec`):** Portrait slot now resolves **`graphics/pywheel/weapons/HASG.png`** first, then the weapon **`Spawn`** frame ( **`0ESG`** pickup art) when **`0ESGA0`** is an extreme aspect portrait lump. Drawing uses **cover-fit** into **`pb_weaponhud_maxwidth` × `pb_weaponhud_maxheight`** (clipped) so tall icons fill the configured box instead of letterboxing to a thin strip.

- **Hell Pistoler shrink beam — fire SFX (`HellPistoler.dec`, `SNDINFO.txt`):** Shrink mode now plays **`Shrinker`** (`SHRINKR.ogg`) via **`A_PlaySoundEx(..., "Weapon")`** on the **`AMGF D`** fire frame (was **`HRFire`** on a **`TNT1 A 0`** tic, which often produced no audible shot during hold-fire). Hell-round **`Hold`** uses the same **`AMGF D`** + **`PlaySoundEx`** pattern for **`HRFire`**.

- **Lady Golide / HASG corner HUD (`HASG.dec`, `PBPDA.zc`):** Weapon portrait uses **`Inventory.Icon` / `Inventory.AltHUDIcon` `0ESGA0`** (was only on the chamber ammo actor; **`Weapon.AltHUDIcon`** is invalid in DECORATE). Fire-mode glyph reads live shell tokens **`HASG_Danmaku`**, **`HASG_WPhos`**, etc. (not transient **`HASG_Select*`** wheel picks). Commander SSG (**`PB_CSSG`**) mode icon now uses **`shellsmode`** on the ZScript weapon, matching in-game shell state.

- **PBX fire-mode corner glyphs (`PBPDA.zc`):** **`PB2022GetModeIcon`** now covers weapon-special-wheel guns that were missing (CSSG, HASG, Quad SG, HDMR **`Rifle`**, Metal Sniper, Lever Action, Hell Pistoler, Super GL, Excavator, Rocket Launcher, M1 Plasma, Dark Matter Rifle, Ballista, pistol/axe shield loadouts). Carbine wheel PNG paths corrected (**`Carbine_Dual` / `Carbine_Auto` / `Carbine_Burst` / `Carbine_Semi`**). Texture lookup uses **`IsValid()`** so empty lumps no longer block the overlay; M41A / cryo–electric / SMG fallbacks aligned with the equipment wheel. Invalid texture sentinel uses **`PB2022_NoTex()`** (default-local) — **`TextureID()`** with no args fails ZScript compile on 4.5.0.

- **DECORATE startup — corner HUD icons (`BLACKHOLE.dec`, `BioAcidLauncher.dec`, `Unmaker.dec`):** `Weapon.AltHUDIcon` is not a valid actor property on the 4.5.0 pin; use **`Inventory.AltHUDIcon`** (same as other slot weapons).

- **ZScript 4.5.0 — PS fodder spawn (`PB_ProjectSurvivalFodderPassive.zc`):** `Actor.Spawn` moved from static util into instance **`SpawnFodderHere()`** on spawner actors (4.5.0: can't call play **`Spawn`** from data/static context). **`BioAcidLauncher`** corner icon lumps **`BPBLM0`** (matches **`Spawn:`** `BPBL M`; **`BPBLA0`** was missing).

- **ZScript 4.5.0 — `FodderExtraPassiveSpawn` compile (`PB_ProjectSurvivalFodderPassive.zc`):** Class-body `const int` members are invalid on the pinned grammar; seeker limits moved to file-scope `enum PB_FodderSeek`. Player scan uses `for (int i = …)` like other spawner controllers.

- **Beam Katana / Argent Sith quick-melee soft-lock (`BeamKatana_Weapon.zs`, `ArgentSith_Weapon.zs`):** `FlashPunching` on `PSP_FLASH` no longer `Goto Ready3` (that spun `KatanaReadyToCut` + `A_DoPBWeaponAction` on the flash layer — duplicate sprite blink and no weapon switch). Flash layer now `Stop`s after `safety=false` clear; melee exits on `PSP_WEAPON` go to `Ready3`. Quick melee matches `PB_WeaponBase` hygiene (`GoFatality` / `FinisherToken`, `PB_SetUsingMelee`, `QuickMeleeAbort` → `Steady`, single `PB_Execute` try).

- **PBX weapon / mode HUD — six slot guns (`PBPDA.zc`, `PB2022_Hud_Weapons.zs`, `PB2022_Hud.zs`, `language.enu`, weapon DECORATE):** **`PB2022GetModeIcon`** now covers **`PB_Unmaker`**, **`PB_Flamethrower`**, **`BioAcidLauncher`**, **`BHGen`**, **`PB_CryoElectroRifle`**, and **`PB_SMG`** (wheel PNGs with **`AMMOIC*` / pickup-sprite** fallbacks). Fire-mode corner glyphs draw when **`pb_weaponhud_enable`** is on (legacy **`pb_weaponmodehud_enable`** still works alone). Integrated ammo HUD adds **`PB_SMG`** dual-mag rows and **`PB2022_DrawWeaponModeLabel`** mode tags (**INFERNO**, **ACID**, **CRYO**, **AKIMBO**, etc.) above the weapon name. **`Inventory.Icon` / `Weapon.AltHUDIcon`** added for Unmaker, Bio Acid Launcher, and Black Hole Generator.

### Changed

- **Rocket trails / fly lights (PB Staging parity):** `OldschoolRocketSmokeTrail1` / `2` / `Darker` no longer use `+CLIENTSIDEONLY` (smoke spawns replicate in multiplayer like Staging). All `M1SL` / homing rocket `Light("ROCKET")` states in `EXPLOSIVES.dec` use `Light("PB_ROCKET")`; `GLDEFS.txt` adds `pointlight PB_ROCKET` and `PB_ROCKET_X1`–`X3` (BigFireBall `MISL*` frames). `PB_Rocket` spawn starts on `M1SL` with `NoDelay` + loop SFX (drops the dummy `MISL` warmup frames).

- **Project Survival fodder as main roster (`PB_ProjectSurvivalFodderPassive.zc`, `FodderPassiveSpawn.dec`):** Shared **`PB_ProjectSurvivalFodderUtil`** drives both direct spawns and **`FodderExtraPassiveSpawn`** seekers (Shambler / Puker / Blighter / Screamer / Spiker / Burster). DECORATE wander seeker removed — ZScript sight polling (**48** tic cap, every **4** tics) keeps the same player-visible spawn rule with far fewer thinkers. Menu slider **`pb_fodder_passive_threshold`** under reinforcement settings (higher = more fodder rolls). **`pb_NoProjectSurvivalPassive`** still gates only optional PS passives (pustule/mimic/pods), not main fodder. Default threshold **`160`** (~**63%** spawn roll per successful seeker — main-roster mix, not near-guaranteed extras at **248**).

- **`PB_Monster` idle tick cost (`PBMonster.zc`):** Hitbox/execution handlers run every **8** tics when no headshot token, active hitboxes, or execution token; removed duplicate **`HitboxerHandler`** calls from ported grunt/imp **`Tick`** overrides.

- **Nash gore crowd cap safety (`NashGoreHandler.zc`):** Living-monster recount for dynamic gore limits runs only during **`GS_LEVEL`** (skipped on title / non-play maps) and caps the actor scan at **2048** thinkers per pass.

- **Crowd performance — gore defaults:** Nash gore defaults: **`nashgore_maxgore` `512`**, blood/gib amount **`200`**, casings **`40`**. **`NashGoreHandler`** scales the live gore/casing cap down when **>20** living monsters are on the map (re-count every **35** tics). Duplicate **`FodderExtraPassiveSpawn`** lines were removed from grunt-tier map spawners (~**18** fewer seeker spawns per wave vs the pre-cleanup tree).

- **ZScript compile fixes (Tier A ports + HUD FX):** Weapon **`default`** blocks now use trailing **`;`** (DECORATE paste had none). **`SpawnID`** commented out; sprite lumps starting with a digit quoted (**`"4A11"`** …). **`Weapon.BobStyle "InverseSmooth"`**; Quad **`Weapon.SisterWeapon "PB_SSG"`**. **`PB_HUD_INTERFERENCE_MAX`** moved to file scope in **`PB2022_Hud_FX.zs`** (const inside **`extend class`** failed on 4.5.0). Helper: **`maint_scripts/Fix-ZScriptWeaponZs.ps1`**.

- **Tier A/B ZScript weapon ports:** Weapon states moved from DECORATE **`PB_Weapon`** to **`class … : PB_WeaponBase`** and registered in **`ZSCRIPT.zc`**. **Tier A:** **`PB_Carbine`** (`Carbine.zs`), **`PB_QuadSG`** (`QuadSG.zs`), **`PB_Railgun`** (`Railgun.zs` + **`RailGunFire.zc`**). **Tier B:** **`HellPistoler`**, **`PB_SMG`**, **`PB_Autoshotgun`**, **`PB_SSG`**, **`HASG`**, **`LeverAction`**, **`PB_Minigun`**, **`PB_MG42`**, **`Paingiver`**, **`PB_RocketLauncher`**, **`PB_DarkMatterRifle`**. Trimmed DECORATE shards keep tokens, ammo, projectiles, and wheel **`Select_*`** actors; **`maint_scripts/Convert-DecorateWeaponToZScript.ps1`** + **`Fix-ZScriptWeaponZs.ps1`** for bulk extraction. Select paths retain **`Goto SelectFirstPersonLegs`** → **`SelectContinue`** → **`Ready3`** where the source gun already used them.

- **Tactical weapon motion — wall lowering removed (`PB_WeaponTacticalFeel.zc`, `CVARINFO`, `MENUDEF.txt`):** Dropped **`pb_tac_wall_*`** and the 7-direction **`LineTrace`** fan (~14 traces/tic while tactical motion is on). Strafe tilt, movement lowering, and lean stay. Wall dip rarely read in-game because weapon ready states keep resetting **`psp.Y`** via **`A_WeaponOffset`**.

- **`PB_WeaponBase` staging helpers (safe re-merge):** Restored **`PB_IncrementHeat`** (staging scale for barrel-heat smoke), **`PB_GunSmoke` → `PB_GunSmoke_Basic`**, **`PB_ReadyFire`** / **`PB_SetZoom`** / **`PB_GetZoom`**, and **`BaseWeapon_Barrels.zsc`** (carry / throw / place + **`PB_CheckBarrel*`** for ZScript guns). **`A_DoPBWeaponAction`** routes **`HasBarrel` / `HasBurningBarrel` / `HasIceBarrel`** to **`ReadyBarrel*`** pickup states. Does **not** reintroduce the reverted **`PB_GotoWeaponReadyView` + `Stop`** ready router (that path caused invisible first-person weapons).
- **`PB_ReadyFire` adoption:** Extended helper (`ammoInv`, `adsFireNeedsAlt`, `unloaderToken`, `adsHoldWrFlags`, `adsTapUsesHipFire`) and wired ADS/hip ready on **`PB_Pistol`**, **`PB_Deagle`**, **`PB_Revolver`**, **`PB_MetalSniper`**, ZScript pump shotgun, **`PB_MP40`**, **`UACSMG`**, DECORATE **`SHOTGUN`**, **`LeverAction`**, **`PB_LMG`**, **`PB_Fusil`**. **`PB_Deagle`** / **`PB_Revolver`** ready loops call **`PB_CheckBarrelThrow1`**. Carbine / scoped RL / MG42 / rifle+rail left on bespoke logic. Maintainer notes: **`docs/pb_staging_baseweapon_merge.md`**, **`docs/pb_staging_pistol_ads_port_plan.md`**.

- **PBX 2022 weapon HUD menu (`MENUDEF.txt`, `CVARINFO`, `PBPDA.zc`):** Restored **Fire-mode glyph** sliders under **PB 2022 Enhanced → PBX 2022 Weapon HUD Options** (**`pb_weaponmodehud_*`**). Mode glyph layout always reads those cvars (portrait uses **`pb_weaponhud_*`** separately). Defaults tuned to **x -195 / y -247**, scale **1.0**, box **56×56**, alpha **1.0**; **`pb_weaponmodehud_enable`** stays **false** (glyph with portrait on; enable **On** for mode-only without portrait).

- **Dragon Slayer (`DragonSlayer.zc`):** Primary swing is a single BDP-style lunge (**`A_BDPMeleeStart`** / **`A_BDPMelee`** + **`DragonSlayerLungeImpact`**, **`A_CustomPunch`**, silent rails — no flame/cryo custom missiles). Hold attack uses **`PB_ReFire("Hold")`** wind-up → lunge without **`DragonSlayerEnergy`** / **`DSAttack*`** FX. Removed energy pool, **`Fire2`** / **`SLBY`** second swing, and **`Weapon.AmmoType`** energy binding. **`PB_ObsidianDragonSlayer`** wear on **`Fire`** / **`AltFire`** / **`Hold`** only.

- **Stormcast — D3 Maledict boss drop only (`Stormcast.zc`, `D3Maledict.zc`, `BFG9000WeaponSpawners.dec`):** **`+WEAPON.CHEATNOTWEAPON`** blocks **`give all`**; removed from BFG map spawner dice. **`A_PB_DropStormcastBossLoot`** on every **`D3Maledict`** death path (respects **`pb_NoStormcastWeapon`** / menu **Drop Stormcast (D3 Maledict)**).

- **Lever Action (`LeverAction.dec`):** Hip and ADS **`A_FireBullets`** damage +10 (.357 **32→42**, Marlin **36→46**).

### Fixed

- **Fullscreen HUD crash on player death (`PB2022_Hud.zs`):** **`Tick`** called **`GetAmount("BasicArmor")`** while **`CPlayer.mo`** was null (suicide / corpse cleanup), aborting the VM in **`FindInventory`**. Refresh **`plr`** from **`mo`** every tic; skip armor / pawn inventory when **`plr`** is null; return before helmet / weapon / ammo polling.

- **Carbine HUD weapon name (`language.enu`):** **`PB_Carbine`** used **`Tag "$PB_CARBINE_TAG"`** but the string was missing — fullscreen HUD showed the raw key **`PB_CARBINE_TAG`**. Added **`PB_CARBINE_TAG`** / **`PB_CARBINE_PICKUP`** (matches PDA **UAC-41 Carbine** naming).

- **Dragon Slayer hold attack (`DragonSlayer.zc`):** Primary **`Fire`** ended with bare **`PB_ReFire`** (defaults to **`Fire`**), so holding attack repeated the tap swing instead of **`Hold`** wind-up → lunge. Now **`PB_ReFire("Hold")`** after the first swing (matches **`Hold`**’s own refire token).

- **Full-auto muzzle FX hitches (`PB_GunSmoke` / `PB_MuzzleFlashEffects`):** Default **`PB_GunSmoke`** on **`PB_WeaponBase`** again spawns the legacy **`GunFireSmoke`** missile (not **`PB_GunFireSmoke`** ZScript thinkers). Added **`A_PB_ThrottledMuzzleFX`**, **`A_PB_ThrottledMuzzleFXDual`**, **`A_PB_ThrottledMuzzleSparks`**, and per-weapon **`A_PB_ThrottledGunSmoke`** phase tokens in **`PB_MinigunFX.zc`** (every-other-shot cosmetics + **`LowGraphicsMode`** skip). Wired on high-rate weapons: **Carbine**, **Quad SG**, **SSG**, **HASG**, **CSSG**, **Demon Exterminator**, **Plasma M1**, **Revolver**, **Pump Shotgun** ZScript, **Battle Rifle** / **Metal Sniper** / **M41A**, plus **Minigun**, **PB Pistol**, **UAC SMG**, **XM21**, **Mastermind chaingun**, and **PBRIFLE** akimbo/alt smoke paths still on raw **`GunFireSmoke`**.

- **Hitches when taking damage (fullscreen HUD plasma glitch):** **`PB_HUDFXHandler`** no longer sets visor **`interference`** to raw damage (could run 80+ tics of per-frame HUD string garble). Capped at **36** tics; **`PB2022_Hud_ZS`** visor interference SFX throttled (**`gametic % 8`**, **`random(0,255) < 48`**) — old **`crandom() < 100`** was always true and played **`visor/interference`** every tic while interference was active.

- **Invisible first-person weapons after `give all` (`BaseWeapon.zc`, weapon select):** **`PB_Carbine`** no longer calls **`A_SelectWeapon("PB_LMG")`** on **`Ready`/`SelectContinue`** (with **`give all`**, that hijacked slot 4 to the LMG and left the carbine view empty). **`PB_RespectIfNeeded`** honors **`pb_NoHelmetAnimation`** and no longer gates helmet intro on missing ACS **`ToggleHelmetAnimation`** (returns 0 when **`cvars`** ACS is not loaded); intro token grant still runs when helmet is disabled, then respect/select routing continues (fixes **`Missing return statement`** compile error from **`else if`** chaining). Shared **`PB_WeaponBase`** **`WeaponRespect`/`Ready3`** stubs **`Goto SelectAnimation`** instead of an empty **`TNT1` loop**. DECORATE **`SelectContinue`** tails that ended on **`Wait`** now **`Goto Ready3`** (railgun, deagle, lever action, shotguns, launchers, etc.).

- **Weapon heat meters vs ammo HUD (`PB2022_Hud.zs`, `PB2022_Hud_Weapons.zs`):** Heat bars no longer use legacy **`BLBAR1` / `RESBAR4`** (flat reserve strip overlapping the primary **`BARBACY*`** frame). **`PB_MG42`** and ZScript overheat weapons (**`PB_NeoHMG`**, etc.) now draw on the standard secondary row (**`BARBACY2` + slanted `ABAR*`**); when a magazine row is already shown, heat moves to the dual-wield row slot so it does not stack on ammo counts.

- **Stutter after `give all` / large inventories (`PB2022_Hud.zs`, `PBPDA.zc`):** Fullscreen HUD no longer walks the entire player **`Inv`** chain every frame for the ammo-type list (**`pb_hideunusedtypes`** default on was **11×** inventory scans per draw), keys, and powerups — one cached pass every **4** tics (invalidates when **`itemcount`** changes). PDA equipment-codex polling uses **`FindInventory`** on codex classes every **14** tics (was full-chain every **7**). Weapon-codex sync skips **`Foundweapon`** events for rows already marked found.

- **Duplicate DECORATE / ZScript class warnings (startup):** Removed redundant DECORATE stubs for actors already defined in ZScript — **`PB_StunGrenadeExplosion`** (`ThrownGrenades.zs`), **`MarineMuzzle1`/`MarineMuzzle2`** (`Smoke.zs`), **`PB_762x51mmAP`** (`BulletDef.HighCal.zsc`), **`PB_762x51mm`** (`M41A.dec` → ZScript), **`PB_792x57mm`** standard round (`NeoHMG.dec` → ZScript; heated/charged stay DECORATE), **`PB_12GAPellet_ASG`** (`HASG_Projectiles.dec` → `BulletDef.Shell.zsc`). XM-21 still fires **`XM21_762x51mmAP`**; Metal Sniper / M41A / HASG use the ZScript **`PB_*`** projectiles via **`PB_FireBullets`**.

- **Railgun laser mode (`RailGunFire.zc`, `RailgunAOEPuff.zs`, `RAILGUN.dec`):** ZScript 4.5.0 compile pass — weapon `action` helpers called via `PB_WeaponBase(invoker)` (not bare `self`, which is the player in weapon states); `SecPlane.normal` instead of `.Normal()`; explicit `dr` init (no Vector3 ternary); `RailgunAOEPuff` in `zscript/Effects/RailgunAOEPuff.zs` + `ZSCRIPT.zc` include; removed duplicate DECORATE `RailgunLaserBlast1` (canonical class in `RailGunPj.zs`).

- **Railgun laser mode (`RailGunFire.zc`, `RAILGUN.dec`, assets):** Matched PB Staging charge/release — **`PB_VisualLaser`** / **`PB_FireLaser`** (multi-bounce **`PB_Laser`** trace, **`RedR1–4`** fire particles, **`RailgunAOEPuff`** along the beam, full-charge **`RailgunLaserBlast1`** + red shockwaves). Replaced **`LaserCannonPointer`** / **`MicrowaveTrail`** **`A_RailAttack`** paths; added **`HoldLaser`** / **`HoldLaser_Scope`** at max charge and **`CancelLaserHold`** (reload refunds ammo). Copied **`GRAPHICS/Weapons/RedR1–4`** from staging.

- **Railgun primary trail (`RailGunFire.zc`, `RAILGUN.dec`, assets):** Restored PB Staging spiral beam FX (`PB_DrawRailFxBlue`, `PBSWV` shock rings, `RailGunTrailSpark` along the trace) instead of `ElectroBlastTrail2` on `A_RailAttack`. Added `GRAPHICS/Weapons/BlueR1–4` + `PBSWV1–2` and `SPRITES/SMOKE/SMO1A0–D0` from staging; longer particle lifetime / roll for lingering spiral wisps.

- **Cryo Auto Shotgun movement (`PB_CatsFrozenWeapons.zc`):** **`PBCF_CryoPellet`** **`SpecialMissileHit`** was applying **`PBCF_CryocubusSlowdown`** ( **`PowerSpeed` 0.3** ) to the shooter; pellets spawn at the muzzle and could tag the player. Now skips **`target`** like **`PBCF_IceOrbCryo`** / **`PBCF_CryoWind`**.

- **`FamiliarSummon` ZScript compile (`FamiliarBase.zc`):** UZDoom 4.14.3 rejected `extend class FamiliarSummon` (*cannot be found in the current translation unit*) because the summon sphere was DECORATE-only. **`FamiliarSummon`** is now a full ZScript **`CustomInventory`** (spawn / use states + cheat **`SetGiveAmount`/`TryPickup`** filter); the duplicate DECORATE actor block in **`PB_UnlessYouHavePowah.dec`** was removed.

- **Riot shield startup (`Axe.dec`, `PBPISTOL.dec`):** **`RS_Axe*`** / **`RS_Pistol*`** states now live inside **`ACTOR PB_Axe`** / **`ACTOR PB_Pistol`** (not a second merge lump or **`ThrownAxe`**). Fixes duplicate **`PB_Axe`** warning, **`Invalid parameter 'A_GunFlash'`**, and **`invalid state RS_AxeReady`** (truncated insert). Shard files are comment-only stubs; **`DECORATE`** includes **`RiotShieldModes.dec`** only.

- **Minigun DECORATE (`MINIGUN.dec`):** Removed invalid 8th argument on **`PB_SpawnCasing`** (7-parameter ZScript action only) — fixes *Expected ')', got ','* at startup.

- **Minigun fire / alt-fire (`MINIGUN.dec`):** Restored PB 3.0 **`Holding1`** / **`FiringFast`** / **`Holding1_Chaingun`** / chaingun first-shot logic from legacy **`Project_Brutality-JMartinezBranchOfficial`** — **`CHAF`** frames with per-tic **`A_SetWeaponSprite("UCHF")`** + **`AmmoMeterOverlayFire`** when upgraded (not **`"####"`** / bare **`UCHF`** states). Kept PB2022 audio fixes: **`Holding1_Loop`** / **`FiringFast_Loop`** / **`Holding1CG_Loop`** **`A_ReFire`** targets (no gunflash every cycle), **`CHANF_NOSTOP`** on loop SFX, **`A_StopSound(CHAN_WEAPON)`** on release. **`AltFire`** / **`Ready2`** spin unchanged vs PB3.

- **Duplicate `PB_DragonsBreathTracer` (`ShotgunPellets.dec`):** Removed empty DECORATE subclass that collided with ZScript **`PB_DragonsBreathTracer`** in **`DragonsBreath.zs`** (HASG / ZScript shotgun / CSSG use the ZScript projectile via **`PB_FireBullets`**).

- **Minigun — PB Staging refold (`MINIGUN.dec`, `PB_MinigunFX.zc`, `language.enu`):** Replaced the broken alt-spin / **`Fire2`** / **`Ready2*_SpinHold`** port with PB Staging’s **`Fire`/`Hold`/`Altfire`/`AltHold`** loop (**`weapons/minigunfire`**, **`CHAINSPI`**, **`CHAINSTA`/`CHAINSTO`**, chaingun-mode SFX). Removed **`A_PB_MinigunAltSpin*`** and per-shot **`A_PB_Minigun*`** DECORATE hooks; kept **`PB_MinigunFX.zc`** throttles for other slot-4 autos. PB 2022 wheel (**`GoWeaponSpecialAbility`**), **`Replaces Chaingun`**, **`RespectMiniGun`**, and **`PB_HighCalMag`** ammo unchanged.

- **Kill-streak Drone Familiar movement lag (`FamiliarBase.zc`, `PB_UnlessYouHavePowah.dec`):** Chase no longer spawns **`GreenFlareSmall` / `GreenTrailSparks`** every few tics or recreates **`DroneFlashlightBeam`** + **`DroneFlashLightCone`** (size **192**) on a **12**-tic loop. Trail FX are throttled in **`PB_DroneTrailFxTick`** (~**14** tics); optional flashlight uses a single **`A_AttachLight`** pair on the drone (**`cl_drone_flashlight`**).

- **Projectile whiz-by VM abort (`BulletProjectile.zsc`):** **`PB_HandleWhizby`** called **`CVar.GetCVar("pb_altwhiz", plr.player)`** after the player pawn could outlive a valid **`PlayerInfo`** (e.g. fatal hit), causing **`tried to read from address zero`** on **`GetBool`**. Guards **`playeringame` / `pi.mo` / `plr.player`**, use **`CVar.FindCVar`** for **`pb_altwhiz`**, and fix **`viewz`** + **`whizSound`** assignment. **`PB_UserCvarBool`** helper also hardens tracer **`pb_tracerlight`** spawn; **`PBBloodImpacts`** / nail **`pb_naillifetime`** reads use the same null-safe pattern.

### Removed

- **`PB_DemonExterminator` (active roster):** Weapon disabled for now (commented **`DECORATE`** / **`ZSCRIPT.zc`** includes, stripped slot 9 / BFG spawner / weapon-special wheel / spawn menu / PDA catalogue). Source under **`actors/Weapons/Slot9/DemonExterminator.dec`** and **`zscript/Weapons/Slot9/DemonExterminator/`** kept for re-enable.

- **Enemy target HP bar (HP Bar.wad fold):** Reverted the full **HP Bar** integration (ACS, actors, handler, assets, menu/bind, merged **`TEXTCOLO`/`language.enu`/`CVARINFO`** blocks) after HUD conflicts and performance cost. Load **`HP Bar.wad`** separately again if you want aimed-enemy HP / boss tracker outside PB’s HUD.

- **Ammo pickup total floaters (`PB_AmmoTotalFloaterHandler`):** Removed the per-tic inventory scan that showed blue DamNum totals when picking up ammo (**`dam_enabled`** / **`pb_xp_rewards`**). Dropped **`zscript/Items/PB_XP/PB_AmmoTotalFloater.zc`**, **`ZSCRIPT.zc`** include, and **`zmapinfo.txt`** handler registration. Hazard-damage HUD still gates on **`PB_XPRewardsHandler.PB_FloatingHudExtrasEnabled`** (renamed from **`AmmoTotalFloatersEnabled`**).

- **ZScript port housekeeping:** Deleted unreferenced **`*_staging_src.zs`** reference copies (Deagle, Revolver, Shotgun, Super GL, M1 Plasma), superseded **`CSSG_PBProjectile.zs`** / **`SSG_PB10GA_Pellets.zs`** (live types in **`BulletProjectile.zsc`** / **`BulletDef.Shell.zsc`**), and **`BulletProjectile.old.bak`**. Retired full weapon **`*.dec`** bodies (**`SHOTGUN`**, **`REVOLVER`**, **`SUPERGL`**, **`PLASMA`**, **`Deagle`**) stay on disk with commented **`DECORATE`** includes.

### Added

- **Middle-finger taunt (PB Staging):** Copied **`FUCK*`** flip-off sprites from PB Staging into **`SPRITES/WEAPONS/Slot1/MELEE/HandToHand/Offend/`** and Doomguy voice lines into **`sounds/Doomguy and Marines/DOOMGUY/Voice/`**. New ZScript **`PB_TauntWeapon`** / **`PB_TauntAlertDummy`** (`zscript/Weapons/PB_Taunting.zc`) plays a random left/right/both animation, optional **`PB_FLIPOFF`** voice (`pb_voicetaunts`), and alerts nearby monsters. Bind: **Options → Project Brutality → Taunt** (`netevent RequestTaunt`, default **`X`**). **40-tic** cooldown per player.

### Changed

- **Ballistic shield module (replaces slot-2 `RiotShield` weapon):** Riot-shield gameplay is now **`PB_RiotShieldModule`** + loadout tokens on **`PB_Pistol`** / **`PB_Axe`** (`RiotShieldModes.dec`, `RiotShield_PistolStates.dec`, `RiotShield_AxeStates.dec`). **`RiotShieldPickup`** (replaces **`RiotShieldDrop`**) grants the module; **Weapon Special** wheels add shield loadout slices when the module is owned (`ev_core_special.zsc`). Removed **`RiotShield`** from slot 2 / **`23134`** editor pickup → **`RiotShieldPickup`**; PDA codex entry → **`PB_RiotShieldModule`**.

- **Dragon Slayer — single player weapon (`DragonSlayer.zc`, `PLAYER.dec`, `PDAWEAPT`):** Yes, the tree had two classes: **`DragonSlayer`** (unlimited implementation base) and **`PB_ObsidianDragonSlayer`** (Ravager drop with wear). Slot 1 / PDA codex now register only **`PB_ObsidianDragonSlayer`**; base **`DragonSlayer`** has **`+WEAPON.CHEATNOTWEAPON`** so **`give all`** cannot grant the infinite variant. Test with **`give PB_ObsidianDragonSlayer`** or kill an Obsidian Ravager.

- **`give all` exclusions:** **`FamiliarSummon`** (kill-streak Drone Summon Sphere) no longer appears from **`give all`** / **`idfa`** artifact grants — **`extend class FamiliarSummon`** in **`FamiliarBase.zc`** blocks cheat **`SetGiveAmount`/`TryPickup`**. Monster-drop weapons **`MastermindChaingun`**, **`MarauderSSG`**, **`MancubusFlameCannon`**, and **`PB_CyberdemonRL`** use **`+WEAPON.CHEATNOTWEAPON`** (same as **`PB_ObsidianDragonSlayer`**); use **`give <ClassName>`** or in-game drops to test. **`give everything`** still grants cheat-flagged weapons if you need a full audit loadout.

- **Fullscreen HUD — duplicate ammo refill bars (`PB2022_Hud.zs`):** Removed legacy **`BAMBAR*`** frame draws from **`DrawAmmoBar`** (Staging never used **`barBorder`**). Reserve row no longer forces flat **`slanted: false`** bars under slanted magazine fills. When **`pb_hud_slanted_bars`** is on, ammo **`DrawBar`** off-graphic is **`TNT1A0`** so flat **`BGBARL`** tracks do not stack under **`CURBAR*`** / **`BARBAC*`** art.

- **Project Survival fodder — Burster spawn rate (`PB_ProjectSurvivalFodderPassive.zc`):** **`PickClass`** no longer gives Burster the same 1-in-5 roll as Puker / Blighter / Screamer / Spiker. Burster is now a separate **1-in-10** roll on the non-Shambler pool (~**8%** of successful spawns vs ~**16%** before); the other four stay evenly split (~**18%** each). Shambler stays **20%**.

- **PBX 2022 weapon HUD icon sizing (`PBPDA.zc`, `CVARINFO`, `MENUDEF.txt`):** Pickup and fire-mode corner overlays now **box-fit** each **`Icon`/`AltHUDIcon`** (or mode wheel PNG) inside configurable max width/height (**`pb_weaponhud_maxwidth`** / **`maxheight`**, default **96×72**; mode overlay **56×56**) before the existing scale/offset sliders — replaces longest-edge normalization with a tight **0.70–1.35** clamp that left wide/tall weapon art visibly inconsistent.

- **Minigun alt-fire — hold to spin (`MINIGUN.dec`, `PB_MinigunFX.zc`):** Hold alt = barrel warm-up only (no alt-fire shooting). **`Spin`** / **`Fire`** → **`Fire2`** unchanged. **Audio:** wind-up/down on **`CHAN_5`**; sustained motor on **`CHAN_6`** via **`A_PB_MinigunAltSpinEnsureSound`** after spin-up. **Spin sprite:** **`Ready2*_SpinHold`** sets ammo overlays once, clears **`PSP_FLASH`** (alt glow), then **`Ready2*_SpinHoldLoop`** chains **`CHAG`/`UCHG`/`8HAF` A–H** without per-tic **`A_Overlay`** or combined-frame loop restarts (fixes barrel blink).

- **Glory Kill shoulder cannon — ice grenade SFX (GK staging fold):** Copied **`icebombfire.ogg`**, **`icebombready.ogg`**, **`flamebelchready.ogg`**, and **`cannonlaunch.ogg`** from Glory Kills PB Staging into **`sounds/IceBomb/`** and **`sounds/FlameBelch/`**. **`SNDINFO.txt`**: **`IceBomb/icebombfire`**, **`Equip_Activate`** (shared launch wind-up), and distinct cooldown-ready cues **`Equip_Iceready`** / **`Equip_flameready`** (replacing generic **`Beep`**). **`FireIceBomb`** in **`BaseWeapon.dec`** plays **`IceBomb/icebombfire`** on launch instead of **`weapons/firegrenade`**; flame and ice charge anim use **`Equip_Activate`** instead of the undefined **`Flamethrower/Charge`**.

- **Targeted combat SFX precache (`zmapinfo.txt` `GameInfo`):** Added **`PrecacheSounds`** (~45 logical names) for starter-kit weapons (pistol, rifle/DMR fire + carbine **`Weapons/Rifle/SFire`**, shotgun, plasma, minigun), melee whoosh/smash, player pain tiers, armor/bonus pickups, common Nash gore splats, and **`PB_DynamicTail`** atmosphere pools (**`weapons/atmo/int|ext/{pistol,pistol_mag,shotgun,ar,smg}`**). Runs on every map load (including **TITLEMAP**) so first-fire decode hitches move off the first combat tic; expand the list after profiling — not a full SNDINFO preload.

- **Full-auto FX throttling (`PB_MinigunFX.zc`, priority slot-4 weapons):** Extended minigun-style helpers to **LMG** (`A_PB_LMGFireFX`), **PBRIFLE** auto/sniper (`A_PB_RifleAutoFireFX` / `A_PB_RifleHeavyFireFX` — alt-tracer rail sparsity **6.0**), **Nailgun** (`A_PB_NailgunMuzzleSmoke`), **MG42** (`A_PB_MG42FireFX`), **PB_M41A** / **NeoHMG** (throttled smoke, brass, tracers), and **AUTOSHOTGUN** refire (`A_PB_ThrottledGunSmoke`). Shared throttle tokens in **`MINIGUN.dec`**. Still skips all cosmetics when **`LowGraphicsMode`** is held.

- **Helmet visor scale default (`CVARINFO`, `MENUDEF.txt`, `PB2022_Hud.zs`):** **`pb_visorscale`** default **0.5 → 1.0** (2× prior size at default). Menu slider range **0.4–1.0** (was **0.2–0.5**); help text now says **1.0** = full authored frame. Existing configs keep their saved value until you move the slider or reset the cvar.

- **M1 Plasma / Revolver ZScript ports (`PlasmaM1.zs`, `Revolver.zs`):** Corrected M1 Plasma view sprite prefixes **`P1SG`/`P1SF` → `PLSG`/`PLSF`** (matching shipped art + retired `PLASMA.dec`; wrong prefixes made the gun vanish on fire). Revolver reload resets **`A_WeaponOffset(0,32)`**, skips the unload-only **`R7V2`** strip that hid the mesh, and uses the same **`PB_CheckReload`** routing as other staging guns.

- **Staging weapon shim + minigun FX (`BaseWeapon_StagingShim.zsc`, `PB_MinigunFX.zc`, `MINIGUN.dec`):** Added **`PB_GunSmoke_Basic`** (respects **`LowGraphicsMode`**), **`PB_RevolverCasingSpawn`**, and **`PB_DynamicTail`** ( **`S_FindSound`** guard; plays when **`SNDINFO.Atmosphere_*`** + **`sounds/combat/Atmosphere/`** are present). Minigun brass/belt now use **`PB_SpawnCasing("EmptyBrassMinigun")`** / **`LMGBeltLink`** with throttling; alternating smoke skips belt frames via **`A_PB_MinigunMuzzleSmoke(0)`**.

- **Revolver → ZScript (`Revolver.zs`, `RevolverTokens.dec`):** Ported PB_Staging **`PB_Revolver`** on **`PB_WeaponBase`** (dual-wield, ADS, unload, respect animation). Reserve ammo **`PistolBullets`**; projectile **`PB_500SW`**; DECORATE weapon retired in favor of tokens-only **`RevolverTokens.dec`**.

- **Shotgun / Super GL / M1 Plasma → ZScript (PB_Staging):** **`PB_Shotgun`** (`Shotgun.zs` + **`ShotgunPellets.dec`**), **`PB_SuperGL`** (`SuperGL.zs` + **`SuperGLTokens.dec`** / **`SuperGLGrenades.dec`**), **`PB_M1Plasma`** (`PlasmaM1.zs` + **`PlasmaM1Support.dec`** / **`PlasmaM1Projectiles.dec`**). DECORATE weapon bodies retired; **`SelectFirstPersonLegs`** select flow; ammo mapped to **`PB_Shell`** / **`RocketAmmo`** / **`Cell`**. Language keys for pickup/mode HUD prints.

- **Carbine (scoped reticle preserved):** No wholesale **`Carbine.dec`** swap — kept **`PB_HandleCrosshair(59)`**, overlay **20/21** scope art, and zoom factors. Cherry-picked Staging **`PB_DynamicTail("ar","ar")`** on hip fire and **`PB_WeaponBase.TailPitch 1.5`** for when atmo sounds are folded.

- **`pb_gunsmoketype`:** New user cvar (**`0`** = **`GunFireSmoke`** projectile via **`PB_GunSmoke_Basic`**, **`1`** = legacy **`PB_GunSmoke`** missile).

- **Weapon atmosphere tails (PB_Staging fold):** Added root lumps **`SNDINFO.Atmosphere_Exterior`** + **`SNDINFO.Atmosphere_Interior`** (logical names under **`weapons/atmo/*`**) and **`sounds/combat/Atmosphere/**`** (**374** `.ogg`/`.wav` files from Staging — not extensionless lumps; only folder nodes were extensionless in git). Paths in the SNDINFO shards use lowercase **`sounds/combat/`** for Linux loads. Shard paths aligned to on-disk Staging names where upstream SNDINFO was stale (**`weap_ar3_fire_*`** exterior AR tails, **`sniper_atmo_ext_ext_*`** for sniper “far” MP layers, **`.ogg`** interior LMG far tails). **`PB_DynamicTail`** on Carbine (**`"ar"`**) and other tail calls resolve when those shards load (engine auto-loads all **`SNDINFO*`** lumps). Maintainer note: **`sounds/combat/Atmosphere/ASSET_NOTES.txt`**.

- **Fullscreen HUD — Phases A–C (ZScript `BaseStatusBar`, PB Staging layout):** Added **`zscript/PB2022Hud/`** with **`PB2022_Hud_ZS`** (ported from PB Staging **`PB_Hud.zs`**), optional helmet blood/glass FX (**`PB_HUDFXHandler`**), **`PB2022_Hud_Weapons.zs`** (dual-wield left-ammo resolve, DMR grenade launcher row, M41A/PB_MG42/Cryo Electro/Marauder SSG/Unmaker/Flamer/**PB_Freezer**/**BioAcidLauncher**/**Stormcast**/**Hell_rifle** specials, Cat's Frozen equipment wheel icons), and message/help shards. **`zmapinfo.txt`**: **`StatusBarClass = "PB2022_Hud_ZS"`**; **`SBARINFO.txt`** reduced to stubs (**`SBARINFO_LEGACY_FULLSCREEN.txt`** kept for reference). Visor in-HUD stays **off** — **`PB_VisorHandler`** (`RenderUnderlay`) remains authoritative. **`CVARINFO`** + **`PB2022_FullscreenHudOptions`** menu (presets **`pb_full_hud_style`** / **`pb_minimal_hud_style`**, ammo list, dynamics, blood/glass FX, **`pb_hud_slanted_bars`**). **Staging parity pass:** copied slanted bar art (**`ABAR*`** / **`HPBAR*`** / armor slices under **`GRAPHICS/HUD/FULLSCRN/{Health,Armor,LOWHEALTH,OVERHEALTH}`**), **`fonts/PBFONT`** + **`fonts/PBBOLD`**, and slanted bar PNGs under **`GRAPHICS/HUD/FULLSCRN/`** (engine-built bar fonts like PB Staging — **do not** add single-glyph **`{ 0 ABAR11 }`** blocks to **`Fontdefs.txt`**; **`maint_scripts/Generate-PB2022HudBarFontdefs.ps1`** only lists lump names for audits). **`pb_hud_slanted_bars`** toggles Staging-style layered slanted mag/health bars vs flat **`CURBAR*`** fallback. Generic magazine HUD still driven by ammo type + **`DrawAmmoBar`**; DECORATE dual-wield tokens feed the third ammo row without per-gun SBARINFO blocks.

- **PDA Deagle codex preview (`Deagle.zs`, `PBPDA.zc`):** Added **`PDA_Preview_Deagle_*`** animation strips (ready, hip fire, ADS fire, ADS raise, reload). Preview builds from those labels only (no long **`Ready`** loop walk); ready segment tic length is **75% shorter** than other weapons (**`readyFixedTic / 4`**). ZScript state lines use terminating semicolons (DECORATE-style bare lines fail compile on 4.14.3).

- **Map-load / cold-start hitch (non-PDA handlers):** **Weapon wheel** defers **`initializeSpecialWheel()`** (PYWHEEL lump parse) until **`level.maptime >= 35`** (was 12). **AccuDeath** blood-color dummies spawn on first monster/missile spawn that needs them, not synchronously in **`WorldLoaded`**. **NashGore** builds **`BLUDTYPE`** on first **`CheckReplacement`** (lazy) instead of **`WorldLoaded`**. **VUAS** skips title-map toast drawing; **`vuas_time`** relay waits until **`gametic >= 35`** (once per second) so unlock timestamps stay valid without title-screen work. **Wheel BSS blur** caches **`gb_EventHandler`** once per map instead of **`EventHandler.Find`** every **`UiTick`**. **`PB_EventHandler`** skips weapon/mode/dash/GK HUD draws and equipment-codex inventory sweeps for the first **35** tics. **Visor** **`RenderUnderlay`** uses the same early-tic gate. **Hit feedback** no longer runs **`PlayerEntered`** work (including a spurious shield-break sound) on the title screen.

- **PDA startup load (`PBPDA.zc`):** **`InitializeUiObjects()`** no longer runs on the first **`RenderOverlay`** frame after launch (that one-shot build of every codex tile was blocking the UI thread and showed as a Windows busy cursor / mouse jitter). UI objects are created only when the PDA is opened, a codex unlock needs them, or after **`PB_PDA_UI_INIT_MIN_GAMETIC`** (~1 s) if another path requests them. PDA mouse capture stays off the title screen and the first **35** tics.

- **PDA achievement browser layout (`PBPDA.zc`):** Browse list uses nearly the **full screen width** (small side margins). Each entry draws **title** (`[UNLOCKED]` / `[LOCKED]`, name, progress/date) on the first line and **description** on a second line under the icon column so long text is not clipped on one row. Text scale uses **`PB_PDA_ACH_BROWSE_TEXT_SCALE_MUL` (0.66)** (was **0.52**). Header, category, achievement icon+text blocks, empty/filter messages, and scroll hint are **horizontally centered** in the pane.

- **PDA achievement browser scroll + sort (`PBPDA.zc`):** List scroll is now a **pixel offset** inside the clip rect (rows draw at `listTop - scrollY`) so page-down reveals off-screen entries instead of dropping the top row only. Rebuild order is **all unlocked** achievements (by category) first, then **locked**. Page Up/Down advances **`PB_PDA_ACH_BROWSE_SCROLL_LINES` (4)** font lines of content per click (row-snapped), replacing the old **~72% viewport** jump that skipped entries. Page arrows align to the list center.

- **Bare hands icon (`MELEE.dec`, `GRAPHICS/pywheel/weapons/Melee_Attacks.png`, `SPRITES/WEAPONS/Slot1/MELEE/MLDFA0.png`):** Weapon-pick wheel and **`Inventory.Icon`** / **`AltHUDIcon`** now use the PB Weapons Pack **`melee-default`** fist graphic (32×32) instead of **`TH3AA0`**.

### Added

- **PB Staging projectile / FX fold (Risdar `Project_Brutality` parity for ZScript guns):** Wired **`zscript/Weapons/Projectiles/`** (**`BulletProjectile.zsc`**, **`BulletDef.*`**, **`AltBulletProjectile`**, **`DragonsBreath`**, **`Leech`**, **`Shrapnel`**, **`RailGunPj`**) plus **`zscript/Effects/{FxBase,BulletImpacts,Smoke,MuzzleSparks}`**, **`ThrownGrenades.zs`**, and stubs **`FastMaterialCheck_PB2022.zsc`**, **`PB_GunshotBloodStub.zc`**, **`PB_BulletPuffStub.zs`**. Removed duplicate **`CSSG_PBProjectile.zs`** / **`SSG_PB10GA_Pellets.zs`** / inline **`PB_500SW`** (**`Revolver.zs`**) / **`PB_50AE`** (**`Deagle.zs`**) / DECORATE **`PB_12GAPellet`** (**`M41A.dec`**). **`PB_Math`** + **`PB_Monster`** hitbox/damage multipliers extended for critical hits; **`STAT_PB_SMOKE`** + **`NashGoreStatics.QueueSmoke`**. Equipment grenades **`HandGrenadeAnother`** / speed variants inherit **`PB_ThrownGrenade`**; stun uses **`PB_ThrownStunGrenade`**. **`BaseWeapon_StagingShim`**: **`PB_GunSmokeSpawn`**, **`_SpawnMuzzleSparks`**. Full **`PBBloodImpacts.zs`** / **`BulletPuffs.txt`** deferred (missing staging-only actors / JSON matsys).

- **Simple Teleporter Effects v1.4** (tunbridgep): always-on gate-flat particle fountains on map load (`zscript/Effects/SimpleTeleporter/`, `TeleporterEffects` in `zmapinfo.txt`). PB compat: **`BrutalTeleportDest`** / `BrutalTeleportDest2` / `BrutalTeleportDest3` count as teleport destinations. No menu toggle; optional console cvars `teleporter_*` remain for flat lists and dest/exit/monster behavior.

- **AccuDeath V3.2.2 fold (Option C):** Corpse translation / particle death FX from **Airehnr66**'s AccuDeath, gated by **`pb_accudeath`** (default **on**). Core ZScript under **`zscript/Gore/AccuDeath/`**; **`PB_AccuDeathCompat.zc`** tags PB projectiles (`PB_Rocket`, `Plasma_Ball`, `PB_DoomImpBall`, `GreenPlasmaBall`, `CacodemonBall_`, `BigFireBall`, `SuperBFGBall`, `PB_DMR_PulseBall`, `LightningMissile2`, …) and extends class arrays. PB fork **removes `Explosive` from the fire damage-type list** (rockets no longer read as fire corpses) and defaults **`accudeath_stateoverride` to off** so PB `Death.*` / fatality states are not overridden unless you enable it. Translations merged into **`trnslate.txt`**; submenu under **Gore/Debris Settings**.

- **PDA Blocky achievements tile (`PBPDA.zc`, `AchievementPDAView.zs`):** Blocky home footer adds **ACHIEVEMENTS** between **Reward Spin** and **Exit**; click opens an in-PDA achievement browser (same cyan grid / gradient shell as the codex via **`PB_PDAUiBackdrop`**) without closing **`PDAMode`**. **BACK**, page arrows, or the **PDA** key (**`openpda`**) return to the hub; **EXIT** still closes the PDA. Options-menu browse (**`ach_open_menu`** / **`VUASAchievementBrowse`**) is unchanged for settings. Removed the separate **Achievement Menu** keybind from **`KEYCONF.txt`**.

- **Vortex Universal Achievement System v0.1.0 (folded, PB 2022–specific):** Achievement toasts, browse menu, and save persistence under **`zscript/Achievements/VUAS/`** with **`PB_AchievementSetup.zs`** (**60** PB goals at the VUAS cap; includes **multi-kill** technique goals for explosives / rail / BFG / sniper / plasma / equipment and **rocket jump** / **plasma climb** when **`pb_rocketjump`** / **`pb_plasma_wallclimb`** grant **`PBBlastMomentum`** — tracked by **`PB_AchievementCombatHandler.zc`**): combat tiers and family hunts, bosses, Glory Kills / shoulder launcher / Shield Saw / frozen statues / experimental executions, PDA codex, dash / ledge grab / weapon wheel, secrets / items, damage dealt/taken, killstreaks, XP rank, UV/NM challenges, two hidden entries). **`pb_achievements`** (default on, single-player only) under **PB 2022 Enhanced**; settings submenu **`PBAchievementsMenu`** and browse **`VUASAchievementBrowse`** titled **PB 2022 Achievements**. Hooks in **`BaseWeapon.zc`**, **`BaseWeapon.dec`**, **`PBPDA.zc`**, **`PlayerPawn.zc`**, **`ev_core_special.zsc`**, **`PB_FrozenSolidHandler.zc`**, killstreak / XP handlers. **`SNDINFO`** unlock sounds, **`GRAPHICS/ACHVMT*`** icons, **`LOADACS.txt`** **`ACHIEVEMENTS_BRIDGE_ACS`**. **`README.md`** player blurb. Do **not** also load external **`Vortex_Universal_Achievement_System_v010.wad`**.

### Changed

- **Achievement system performance (`AchievementHandler.zs`, `AchievementRenderer.zs`, `PB_AchievementCombatHandler.zc`, `PBPDA.zc`):** Damage tracking no longer scans all 60 achievements on every **`WorldThingDamaged`** hit (pooled per tic); kill/secret/custom hooks use prebuilt index lists trimmed on unlock; **`vuas_time`** relay throttled to once per second; PDA achievement tile counts refresh ~1 Hz instead of every frame; combat multikill handler skips work when achievements are off or in netgame. Unlock persistence (**`vuas_record*` / `vuas_maps*`** encode + **`SetString`**) is deferred to **`WorldTick`** (one CVar slot per tic) so the unlock frame no longer rebuilds/encodes the full slot synchronously; map exit flushes pending dirty slots only (not a full **`SerializeAll`** rewrite every unload).

- **AccuDeath default:** **`pb_accudeath`** is now **on** for new games and configs that have not saved the cvar yet (disable under **Gore/Debris Settings** if you prefer vanilla corpse colors only).

- **M41A / Lever Action (Garand) rifle damage:** **`PB_762x51mm`** (M41A primary + Battle Rifle) **44 → 30** per projectile — closer to the slot-4 DMR band instead of near-sniper output, especially with M41A’s two-round tap and headshot scaling. **Lever Action** Marlin (`.444`) hitscan **99 → 36** and `.357` **50 → 32** — Marlin was deleting imps/pinkies in one body or head shot; values sit just above the upgraded DMR without outclassing the XM-21 AP line.

### Fixed

- **Glory Kill shoulder flame / ice on ZScript weapons (`BaseWeapon_ShoulderCannon.zsc`, `BaseWeapon.zc`):** `FireShoulderCannon` / `FireFlameBelch*` / `FireIceBomb*` states now live on **`PB_WeaponBase`** so **`DoShoulderCannon`** resolves while holding ZScript guns (DECORATE-only **`PB_Weapon`** states were invisible to **`PB_Deagle`** and similar). **`A_DoPBWeaponAction`** checks shoulder (and glory melee) before the **`usingMelee`** early-out.

- **Glory Kill shoulder flame / ice (`BaseWeapon.dec`, `BaseWeapon.zc`, `PB_WeaponTacticalFeel.zc`):** **`FireFlameBelch`** / **`FireIceBomb`** gate states branch to **`FireFlameBelch_Fire`** / **`FireIceBomb_Fire`** (DECORATE action blocks must **`return ResolveState(...)`** — no **`""`**, **`null`**, or implicit fall-through). **`pb_shouldercannon`** mirrors **`pb_glorycryo`**. **`A_DoPBWeaponAction`** clears stale **`DoShoulderCannon`** when **`FireShoulderCannon`** cannot resolve.

- **PBX weapon pickup icon overlay (`PBPDA.zc`):** **`PB2022WeaponHudSkipWeapon`** no longer returns **`true`** for every weapon (regression from the **`Name.Null`** → unconditional-skip compile workaround). With **`pb_weaponhud_enable`** on, corner **`Icon`/`AltHUDIcon`** portraits draw again; only **`Melee_Attacks`** is skipped. Integrated **`AMMOIC*`** ammo glyphs are unchanged.

- **`PB_ObsidianDragonSlayer` and `give all` (`DragonSlayer.zc`):** Ravager-only boss drop (limited-wear Dragon Slayer variant) no longer appears from **`give all`** / **`idfa`** — added **`+WEAPON.CHEATNOTWEAPON`**. Obtain it by killing an **Obsidian Ravager** (unless **Drop Dragon Slayer (Obsidian Ravager)** is off in weapon spawns).

- **Plasma / Unmaker / Demon Exterminator ammo HUD double bars (`PB2022_Hud_Weapons.zs`):** **`PB_M1Plasma`**, **`PB_M2Plasma`**, **`PB_Unmaker`**, and **`PB_DemonExterminator`** were still drawing legacy SBARINFO **`BAMBAR*`** frames plus flat **`CURBAR*`/`RESBAR*`** fills at old coordinates on top of the slanted **`ABAR*`** path used by every other weapon. They now route through shared **`DrawAmmoBar`** (dual-wield plasma keeps the third row; Unmaker/Demon Exterminator keep dual ammo icons and soul **`%`** text).

- **PBX integrated weapon ammo HUD (`PB2022_Hud.zs`, `PB2022_Hud_Weapons.zs`, `PBPDA.zc`):** Bottom-right ammo now follows **`SBARINFO_LEGACY_FULLSCREEN`** layout for all guns: **`AANUM3`** digit font, legacy **`BAMBAR*`** frames, reserve/mag/dual rows at **`-73,-17` / `-72,-49` / `-111,-52` / `-122,-32`**, **`AMMOIC*`** at **`-66,-17`**. **`DrawAmmoBar`** no longer requires **`PB_WeaponBase`** (DECORATE guns work). Optional PBX pickup overlay skips every weapon so it cannot stack on the integrated bars. **`PB_MG42`** heat row uses **`BLBAR1`** without duplicate **`BAMBAR`** fills.

- **Fullscreen HUD mugshot centering (`PB2022_Hud.zs`):** Nudged **`GetMugShot`** draw position **+2 px** on X (now **28.5** / **27.5** depending on HUD scale path) so the face sits more centered in the **`EQUPBO`** equipment frame without shifting the frame art.

- **DarkShadow bonus / armor pickups (brightmap flicker):** **`HealthPlus`** / **`ArmorShard`** (**`B0N1`** / **`B0N2`**) and tier **`PB_*`** bonuses now use **`+BRIGHT`** so **`BMAP/CSPBonuses.txt`** brightmaps stay visible instead of pulsing dark→bright with sector light. **`PB_RedArmor`** and PBHP **`4RM1`–`4RM3`** pickups likewise use **`+BRIGHT`** and **`bright`** on every spawn frame (was **`AA` bright + `BB`/`ABC` unlit** in the bob loop).

- **PB_M1Plasma fire / select sprites (`SPRITES/WEAPONS/Slot6/PlasmaGun/`):** ZScript uses **`PLSF`** / **`PLSG`** strips, but **`Fire/`** only had **`PLSF` A–C** (missing **D–I** muzzle-flash frames for **`MuzzleFlashCenter`**). Copied staging **`P1SF*`** → **`PLSF*`** into **`Fire/`** (9 frames). Also added **`PLSD` E–H** for holster deselect and **`4LSG` J** empty-idle from PB Staging. **Asset audit follow-up:** copied **`PLSM` A–D** (alt-fire charge), **`PL4SA0`** (world pickup / PBVP spawn), dual-wield gaps **`DPR1` D/G**, **`DPR3I`**, **`DPR4I`**; fixed reload-complete sprite name **`P1SS` → `PLSS`** in **`PlasmaM1.zs`** (on-disk art was already **`PLSS*`**). Dual-wield slide-kick **`P3R0` G–M** uses **`lumps/includes/textures/Plasma.inc`** composites (patches **`DPRKA0`** / **`DPRKB0`** in **`Kick/`**) — removed temporary **`P3R0G0`–`P3R0M0.png`** copies of **`P3R0F`** that overrode those composites. **`PNUM`** / **`VLAS`** are texture/PBVP lumps (`lumps/includes/textures/Plasma.inc`, **`PBVP.inc`**) — not loose **`SPRITES/`** PNGs.

- **PB_Revolver ADS / fan-fire / dual-mag flash (`SPRITES/WEAPONS/Slot2/REVOLVER/`):** Copied staging **`R4V2*`**, **`R4V3*`**, **`R4V4` A/C**, **`R4VM*`**, **`R5VM*`**, **`R3V1J`**, and **`41VM` A–D** from PB Staging **`Ads/`**, **`Fire/Flash/`**, **`FanFire/Flash/`**, **`SwitchDW/`**, and **`DualMag/FireIndividual/Flash/`**. **`VVIC`** remains a PBVP optional texture (**`PBVP.inc`**). **`BRAS`** brass eject ( **`LooseRounds.zc`** ) has no matching loose lump in PB2022 or Staging — likely IWAD **`BRAS`** or a casing pack still to fold under **`SPRITES/WEAPONS/Misc/Casings/`**.

- **PB_Revolver reload vanishing weapon (`SPRITES/WEAPONS/Slot2/REVOLVER/Reload/`):** The ZScript port from PB Staging drives reload with **`R6V1`/`R6V2`** strips, but PB2022 only shipped the legacy **`R6RE`/`R9Z0`** reload lumps. Copied **39** staging **`R6V*.png`** frames into **`Reload/`** (brightmaps for **`R6V*`** were already in **`BMAP/Revolver.txt`**).

- **Middle-finger taunt ZScript compile (`PB_Taunting.zc`, `PBPDA.zc`):** UZDoom 4.14.3 rejects a file-scope **`bool`** helper — **`PB_WeaponReadyForTaunt`** now lives on **`PB_EventHandler`** and calls **`pbw.InStateSequence`**. Taunt weapon setup/teardown uses named **`action`** functions (**`PB_Taunt_Begin`** / **`PB_Taunt_End`**) with **`player.pendingweapon`** (staging idiom) instead of anonymous state blocks that tripped self-pointer errors.

- **Rifle alt-tracer FX (`PB_MinigunFX.zc`):** Cosmetic **`A_RailAttack`** / **`A_FireBullets`** calls use **`""`** puff names instead of **`"none"`** (parser warned **Bad hex number: no**).

- **Minigun performance (`PB_MinigunFX.zc`, `MINIGUN.dec`):** Full-auto fire was spawning muzzle smoke, brass (`FiftyCaseSpawn`), belt links, and dense **`Tracer_Trail`** rail puffs every shot (triple-barrel upgrade ×3 rails was worst). Added throttled **`A_PB_Minigun*`** weapon actions (brass ~1/2 shots, single brass on triple burst, rail sparsity **6.0**, skip cosmetics when **`LowGraphicsMode`** / **`pb_lowgraphicsmode`**). **`GunFireSmoke`** / casing spawners also respect **`LowGraphicsMode`**.

- **Minigun alt-fire barrel spin audio (`MINIGUN.dec`, `PB_MinigunFX.zc`):** **`A_PB_MinigunAltSpinEnsureSound`** was restarting **`CHAINSPI`** / chaingun **`spin`** every **`Ready2*`** re-entry (~8 tics), which read as rapid clicks instead of a smooth motor loop. Loop SFX now starts once on **`CHAN_5`** when **`MGSpinSndActive`** is unset; wind-up/spindown use the same channel after **`A_StopSound(5)`**.

- **Fullscreen HUD cell-ammo bar art (`GRAPHICS/HUD/FULLSCRN/AmmoGeneric/`):** Copied Staging **`AmmoGeneric`** (slanted **`ABAR81`–`ABAR87`**, **`BARBACC*`**, **`AMMOIC8`**, **`RESBAR8`**, etc.) — required when **`DrawAmmoBar`** uses **`ABAR8`** with slanted bars; was missing after the Ammo1–7 copy pass. **`BAMBAR8`** is not in Staging loose files or **`PBTEST.wad`**; **`DrawAmmoBar`** never draws the **`barBorder`** argument (Staging **`PB_Hud.zs`** same) — cell framing comes from **`BARBACC1`–`3`** backgrounds only.

- **UZDoom load freeze/crash after HUD bar FONTDEFS (`Fontdefs.txt`):** Removed the auto-generated **110** single-glyph bar font blocks (`{ 0 ABAR11 }` style). PB Staging does not define **`ABAR*`** / **`CURBAR*`** in FONTDEFS — the engine builds bar fonts from **`GRAPHICS/HUD/FULLSCRN/`** lumps; malformed one-glyph defs broke **`DrawBar`** during startup/HUD init. **`maint_scripts/Generate-PB2022HudBarFontdefs.ps1`** now writes a reference name list only (no splice into **`Fontdefs.txt`**). Stripped UTF-8 BOM from **`Fontdefs.txt`**.

- **PDA weapon preview strips — third-person / fatality sprite leak guard (`PBPDA.zc`):** Extended **`PDA_ShouldSuppressWeaponPlayerSideArt`** with Eld finisher prefixes (**`NZ0D`/`NATT`/`ESCR`/`SOBO`**, imp **`YUTI`/`49AN`**, HK **`CPUI`/`BOTU`**, baron **`PPLJ`/`TOPI`**, arch **`SATZ`**, hellduke **`HDF*`/`UH0D`/`H0HE`/`HS0D`/`H0PF`**, demon **`LSLS`**, draugr **`SAPO`**, nazi **`NZFT`/`NFTY`**, marine **`POSM`/`POSA`/`FOS*`**, ride **`MHML`/`ARF2`**) plus kick-overlay **`P1NS`–`P4NS`** so generic **`Fire`/`Ready`** walks and blocky hub **`Fire`** backdrops cannot flash player/marine/grunt choreography when a weapon lacks dedicated **`PDA_Preview_*`** labels. Boss-tech view prefixes **`RMNG`**, **`CYBF`**, and **`FUMS`/`FUMG`/`FUMY`** stay allowed.

- **PDA achievement browser compile (`AchievementPDAView.zs`, `PBPDA.zc`):** Moved list rebuild/draw/scroll off static **`VUAS_PDAAchievementView`** (readonly **`PB_EventHandler`** parameter in UI scope) onto **`PB_EventHandler`** **`ui`** instance methods; browse row arrays are **`transient ui`** with play-scope invalidate/scroll-reset generation counters synced in **`PDA_SyncAchievementBrowseState()`** (play **`NetworkProcess`** cannot **`Clear()`** UI arrays). **`AchievementPDAView.zs`** keeps only **`PB_PDAUiBackdrop`**.

- **PDA weapon codex (`PBPDA.zc`, `PDAWEAPT`):** `InitializePDATexts` only parsed the **last** `#` block in **`PDAWEAPT`**, so most catalogue rows (including **`PB_Deagle` / Martian Raptor**) never registered. Each block now loads as `Header$text$class$stats` with multiline stats joined after the third `$`. Opening the PDA rescans the player's inventory for unlocks (`SyncWeaponCodexFromOwner`). **`give all` still does not grant `PB_Deagle`** (`+WEAPON.CHEATNOTWEAPON` — upgrade weapon); use **`give PB_Deagle`** or the in-game revolver upgrade spawn to test that row.

- **Achievement unlock sounds (`SNDINFO.txt`):** Removed invalid **`$play vuas/unlock*`** tail entries that aborted startup with `Missing string (unexpected end of file)` at EOF; VUAS unlock SFX now use standard logical-name → path mappings like upstream Vortex.

- **Shoulder launcher achievements (`BaseWeapon.dec`, `BaseWeapon.zc`):** Replaced invalid DECORATE `{ PB_Achievements.OnShoulder*(); }` blocks (parser: `Expected ';', got '.'`) with **`A_PB_AchShoulderFlame`** / **`A_PB_AchShoulderIce`** on **`extend class PB_WeaponBase`** (not `Weapon` — ZScript 4.5.0 compile order).

- **Achievement ZScript compile (`PB_Achievements.zc`, `PB_AchievementSetup.zs`, `PB_AchievementCombatHandler.zc`):** **`VUAS_AchievementHandler.Unlock`** is play-scope only — hidden **Enhanced Brootality** unlock now runs from **`PB_AchievementSetup.WorldLoaded`**; dropped **`PB_Achievements.Unlock`** wrapper. Removed **`is "PBCF_CryoRocket"`** inflictor check (class not in tree; cryo RL not shipped yet).

- **Shield Saw throw / catch (`BaseWeapon.zc`, `BaseWeapon.dec`):** view strip is **`SHIE K → J → I → L → M → N → O`** on **`PSP_WEAPON`** (projectile still spawns on **`I`**); recall is the reverse **`O → N → M → L → I → J → K`**. Removed the split layer-**1006** overlay path that only played **L/M** on throw and **N/O** before catch.

- **Shield Saw catch (`PlayerPawn.zc`):** recall now starts **`ShieldSawCatchAnim`** with **`player.SetPSprite(PSP_WEAPON, …)`** (same pattern as ledge climb) instead of **`ReadyWeapon.SetState`**, which never advanced the view weapon layer. **`ShieldSawCatchPending`** is only consumed when **`FindState("ShieldSawCatchAnim")`** succeeds.

- **Title screen (`titlemap.wad` + `language.enu`):** `SubtitleMessage` ACS draws localized `TITLEMAP0` and random `TITLEMAP1`–`TITLEMAP23` under the logo; without language entries the engine showed raw keys like `TITLEMAP12`. Added empty strings so only the logo art remains visible.

- **Shield Saw throw / catch view (`BaseWeapon.zc`, `BaseWeapon.dec`):** dropped per-frame **`A_SetPitch` / `A_SetAngle` / `A_SetRoll`** and **`PB_WeaponRecoil(+1.15, …)`** on the throw (they were pitching the camera down and not restoring cleanly). Raised all **`A_WeaponOffset`** Y values by **+56** so the **`SHIE`** strip sits on the bottom edge instead of floating with visible gap under the lump.

- **Shield Saw throw / catch timing:** reverted yesterday’s **`SHIEL`/`M`/`N`/`O`** experiment (**7** tics, **Y 198–200**) back to the prior **5**-tic / **Y 186–192** release and catch offsets (keeps no-view-tilt throw + bottom-biased **`K`/`I`** strip).

- **Shield Saw `SHIEL`/`M`/`N`/`O` hold:** **+2** tics per state on throw/catch release flashes (**5→7**, throw **`M`** tail **4→6**); offsets unchanged from the reverted baseline.

- **Shield Saw throw spawn (`PB_FireShieldSawThrown`):** **`A_FireProjectile`** spawn Z **`-10` → `-26`** so the **`SHDP`** projectile (spawned on **`SHIE I 0`**, not the **`L`/`M`** psprite tail) starts lower and its bottom edge stays off-screen on launch.

- **Shield Saw catch + throw wind-up (`PlayerPawn.zc`, `BaseWeapon.zc`, `BaseWeapon.dec`):** **`ShieldSawCatchPending`** is kept until **`Fire`/`AltFire`** is not active (no longer dropped when attack buttons are held). Catch skip checks **`PSP_WEAPON`** state only, not **`BT_ATTACK`**. Throw wind-up **`SHIE K`/`J`/`I`** uses bottom **`A_WeaponOffset` Y 200** (same band as flash overlays).

- **Shield Saw throw/catch flash overlay position:** **`PSPF_ADDWEAPON` / `PSPF_ADDBOB` off** on layer **1006** so **`SHIE` L/M/N/O** use absolute **`A_OverlayOffset` Y 200** (bottom-anchored HUD space, not stacked on weapon Y). Face-on pop is **not** **`SHDP`**; projectile spawn Z **`-32`** nudged again for launch.

- **Shield Saw throw/catch flash overlays + fire block:** **`SHIE` L/M/N/O** play on overlay layer **1006** with a **single fixed `A_OverlayOffset`** (no per-lump **`A_WeaponOffset`** strobing). **`ShieldSawThrowAnim`** + **`CantFire`** + **`PressingFire`/`PressingAltfire`** gating stop shots for the full throw/catch strip (not only **`A_DoPBWeaponAction`** weapons).

- **Shield Saw throw/catch snappy flashes (`SHIE` L/M/N/O):** throw tail and catch intro use **one hold per flash lump** (**`L`/`M`**, **`N`/`O`**) at **4** tics (throw **`M`** snap **2** tics) instead of doubled **7**-tic holds; bridge **`I`** frames **2** tics with monotonic Y into/out of the face-on pops. Catch hand strip tightened to **2** tics per step (**272→144**).

- **Shield Saw throw / catch vs weapon fire (`BaseWeapon.zc`, `BaseWeapon.dec`, `PlayerPawn.zc`):** **`PB_BlockWeaponFireInput()`** + **`usingMelee`** gate in **`A_DoPBWeaponAction`** and the melee handler overlay suppress primary/alt fire during the throw strip so guns do not keep shooting invisibly mid-animation. **`PlayerPawnBase.PB_ShouldPlayShieldSawCatch`** skips **`ShieldSawCatchAnim`** when attack is held or **`PSP_WEAPON`** is still in **`Fire`** / **`AltFire`** (token return unchanged).
 **`SHIE` L/M/N/O** hold extended (**3→5** tics per frame; throw **M** tail **2→4**) so **`SHIEL`/`SHIEM`/`SHIEN`/`SHIEO`** read longer on throw and catch.

- **Weapon fire VM abort (`PB_WeaponBase.PressingFire` / `PressingAltfire`):** `A_DoPBWeaponAction` on DECORATE ready loops (minigun, shotgun, rifle, …) no longer dereferences a null implicit `player` during `Inventory.Tick` — input helpers use **`invoker.PB_WeaponActionPlayer()`** (non-`action` method on the weapon; fixes ZScript *Self pointer used in ambiguous context* at compile time on UZDoom 4.14.x).

- **Shield Saw throw / catch:** extra **`A_WeaponOffset`** Y (+20) on throw follow-through and catch wind-up so **`SHDP`** does not peek under **`SHIE`**; projectile spawn height **`-10`** (was **`-2`**). Prior pass used +24 on the full strip; projectile spawn restored to GK **`(7, -2)`** offsets. Catch runs via **`ReadyWeapon.SetState`** (not **`SetPSprite`**) and exits with **`HasCutingWeapon` / `PB_SetUsingMelee` / `A_ClearOverlays(PSP_FLASH, …, false)`** cleanup. **Punch flash after throw/catch:** removed exit-time **`A_ClearOverlays(-999, -999, false)`** (it destroyed **`Melee_Equipment_Handler_Overlay`** on layer **-777**, so Quick Melee could no longer reach **`FlashPunching`**); **`PB_RestoreWeaponMiniOverlays()`** re-spawns the mini handler overlays like **`SelectFirstPersonLegs`**.
- **Startup:** **`ShieldSawProjectile`** DECORATE user vars need terminating semicolons (**`var int user_timer;`**, **`user_ripping;`**) — fixes *Expected ';' but got 'var'* parse abort. Restored missing **`SPRITES/WEAPONS/Slot3/SSG_Staging/SGN2A0.png`** for **`PB_CSSG`** HUD icon (**`Inventory.Icon` / `AltHUDIcon`**).

- **Glory Kill Eternal HUD (`PB_GloryFuelHudOverlay.zc`):** Shield Saw icon (**`GKSHA*`)** now advances with the same **`step`** spacing as crucible / blood punch / shoulder equipment icons (removed fixed **`cx + step * 4.5`** slot that left a visible gap, especially with empty crucible pips).

- **Shield Saw Quick Melee (GK parity):** flash punch always runs when within **99** mu (**`A_JumpIfCloser`** before throw). **`PB_ResolveQuickMeleeShieldThrow()`** only returns **`ThrowShieldSaw`** when the player has **`ShieldSaw_token` + `ShieldSawReady`**, the saw is not in flight, and the target is **far** — it no longer hijacks punch or returns **`ShieldSawNotReady`** on the punch path (GK cooldown without ready = fist, not **`Equip_notready`**). **`ThrowShieldSaw`** also **`A_JumpIfCloser(99, …)`** back to punch like upstream **`11 GK_ShieldSaw`**.
- **Shield Saw throw / return (GK parity):** restored **DECORATE** **`ShieldSawProjectile`** + checker actors in **`actors/Weapons/Slot1/ShieldSaw.dec`** (removed ZScript class that replaced the actor and hid **`SHDP`** flight). **`ShieldBack`** gives **`ShieldSaw_token`** + **`ShieldSawNotifyReturned`** (clears **`ShieldSawThrown`**, catch anim). **`SRC/Shield_saw.acs`** **`CallBack`** polls **`CallBack`** on the projectile for **`FastCall`** recall; dropped false early **`WorldTick`** refund that could restore the token while the saw was still airborne (blocked re-throw). Rebuilt **`ACS/Shield_saw.o`** (SLADE ACC 1.60); **`LOADACS.txt`** loads **`Shield_saw`** again.

- **Custom Shards & Potions bonus brightmaps (`BMAP/CSPBonuses.txt`):** bindings now use existing **`brightmaps/ITEMS/*.png`** paths (PB **`BON1A`–`D`** + new **`BON1E`–`J`** from CSP health sprites, **`B0N2A0`–`H0`** from armor sprites) instead of missing **`Brightmaps/ITEMS/BON1A0.png`** / **`B0N2*.png`** lumps — clears startup *Brightmap … not found* warnings for **`B0N1`** / **`B0N2`** pickups.

- **`PB_HitmarkersUi.zsc` compile (4.5.0):** animation glyph tables use file-scope **`const String[]`** (not **`static const`** at file scope); hit/kill sound **`switch`** arms use multi-line **`case`** bodies (one-line **`case N: x = y; break;`** fails the 4.5.0 parser). **Ui/play split:** static hits use a read-only draw gate (**`hmStaticDrawSlot`** / **`hmStaticDrawExpire`**); shared helpers live on **`hitmarkershandler`** as **`clearscope static`**; avoid reserved locals **`dist`**, **`fnt`**, **`color`**, **`sound`**.

- **`ROCKETLAUNCHER.dec` startup parse:** lock-on indicator height / dead-target checks cannot use **`target.health`** / **`target.height`** inside DECORATE **`{ }`** blocks (**`Expected ')', got '.'`**). Restored **`CallACS("lockOnTargetHeight")`** and **`ACS_NamedExecuteWithResult("checkLockOnTargetState")`**; **`RLACS`** stays in **`LOADACS.txt`** for those helpers only (homing itself is **`PB_RLACSHandler`**).

- **`PB_RLACSHandler.zc` compile (4.5.0):** manual 3D range (**`range3d`**, not **`Distance3D`** / reserved **`dist`**); lock-on lookup via **`Level.CreateActorIterator(tid)`** (no **`Actor.FindByTid`**).

- **`PB_DMRReticleOverlay.zc` compile (4.5.0):** drift in **`WorldTick`**, draw in **`ui`** **`RenderOverlay`** with **`Font.GetFont`** (not bare **`Font()`**).

### Changed

- **Tier 2 ACS → ZScript (guided rockets, DMR reticle, Glory netevents):** **`PB_RLACSHandler`** replaces **`SRC/RLACS.acs`** / **`RLACS`** in **`LOADACS.txt`** — guided / homing / weaving rockets homing in **`Tick()`** on **`PB_GuidedRocket`** / **`PB_HomingRocket_*`**; lock-on indicators (**TID 30001–30003**) and **`GuidedLaser`** aim via **`WorldTick`** / actor **`Tick()`** (no per-tic **`rocketFollow`** / **`getHomingCoords_*`** ACS). **`PB_DMRReticleHandler`** replaces **`SRC/Reticle.acs`** **`DMR_Reticle`** ENTER loop for **`Rifle` + `Zoomed` + `ReticleStyle==1`**. **`GloryMelee`** lump dropped — **`pb_glorysaw_*`**, **`pb_shouldercannon`**, **`pb_glorycryo`** already handled in **`PB_TacticalFeelHandler`**. **`ExecutionsACS`** removed from load (unused; **`pb_execution_handler`** reads **`spawnHealth`/`health`** directly).

- **Tier 1 ACS → ZScript (Glory HUD, hitmarkers, Blood Punch, Marauder):** **`PB_GloryFuelHudOverlay`** draws all **`sv_fuelhud_background`** styles (**0–3**); **`GloryHUD`** removed from **`LOADACS.txt`**. **`PB_HitmarkersUi.zsc`** replaces **`hitmarkers.acs`** (no **`hm_bridge_*`** cvars). **`PB_BloodPunchReadyHandler`** / **`PB_MarauderSupportHandler`** replace **`BloodPunch`** / **`Marauder`** ACS; **`Shield_saw`** lump dropped ( **`ShieldSawPlayerHandler`** only).

- **Glory Kill equipment cooldowns (ZScript):** **`PB_GKEquipmentCooldownHandler`** replaces **`SRC/EquipmentCooldown.acs`** / **`EquipCooldown`** in **`LOADACS.txt`** — same **`CoolFlameBelch` / `CoolIceBomb` / `CoolShieldSaw`** → **`FlameBelchReady` / `IceBombReady` / `ShieldSawReady`** cadence (**35** tics per step, **`sv_*_Cool`** sliders unchanged).

- **Low health / low armor alerts (ZScript):** **`PB_LowHealthAlertHandler`** replaces **`SRC/lowhealth.acs`** / **`lowhealth`** in **`LOADACS.txt`** — **`be_LowHealthAlert`**, **`sv_LowHealthValue`**, **`be_LowArmorAlert`**, **`sv_LowArmorValue`** behavior preserved.

- **Monster curb / execution / GK fatality SFX:** every shared **SSG execution** warp (**`Death.ExeCution1`**, **30**-tic **`A_Warp(AAPTR_TARGET,-50,…)`**) is split into **three 10**-tic warps with **per-family** interstitial cues on **chan 6** plus a second layer on **0-6** only (legacy **`CHAN_7`** slots were remapped so these cues never fight **`PB_Killstreak_Handler`** announcer lines on **`CHAN_7`**, and numeric channels never use invalid **`CHAN_8`**-and-up indices). Each actor’s pain / bite / melee / boss analogue follows **`SNDINFO.txt`** (e.g. **`demon/pain`** + **`demon/melee`** on pinkies, **`caco/pain`** + **`caco/melee`**, **`spider/pain`** on mastermind / demolisher / arach variants, **`cyber/impact`** + **`cyber/melee`** on Baalgar, **`Paladin/Pain`** + **`Paladin/Melee`** on cyber paladin — not **`grunt/pain`**, which stays on **humanoid victims** such as **`FatalityMarine`** / baron-on-zombie chains). **~39** monster DECORATE actors. **Realm667 GK** wrappers: **`misc/xdeath2`** after **`GoFatality`**. **CyberBaron** **`CurbstompMarine`**: **`misc/xdeath3`** + **`grunt/pain`** after opening stomp frames. **Hellion** **`Death.Fatality`**: **`monster/hlnpai`**. **Pinkies** **`PB_Demon`** / **`PB_MechDemon`** stomp chains: opening **`grunt/pain`** on zombieman/sergeant/imp/commando victims.

- **Monster fatality / curb-feed audio (follow-up):** **`PB_Mancubus`** **`Fatality1`/`Fatality2`** cue **`Mancubus/pain`** on Glory-Kill token handoff; **`actors/Monsters/T3-Fats/GloryKills.dec`** (**`PB_DaedabusGK`**, **`PB_MancubusGK`**, **`PB_VolcabusGK`**) use **`Daedabus/Pain`** / **`Mancubus/pain`** / **`Volcabus/pain`** on **`Death.Fatality`** instead of generic **`fatso/pain`**. **`FatalityMarine`**: **`Overlord.dec`** opens with **`monster/ovlpai`**; **`Watcher.dec`** layers **`monster/watpai`** with the marine grunt; **`REVENANT.dec`** adds **`skeleton/pain`** on marine curb open, **`skeleton/swing`** before the **`REFP BCDE`** beat, **`skeleton/active`** before the long **`REFP J`** chew, and **`skeleton/swing`** on **`Death.ExeCution1`** between warps; **`BeamRev.dec`** / **`Draugr.dec`** **`Death.ExeCution1`** match that middle swing + gore layer. **`Draugr.dec`** **`FatalityMarine`** adds **`skeleton/pain`** before the opening **`FDR4`** hold. **Imp / caco / baron / pinky curb chains:** stock **`IMPS.dec`**, **`ImpVariant1.dec`**, **`SavageImp.dec`** **`FatalityMarine`** add **`imp/melee`** after the first footfall; **`CACODEMON.dec`** **`FatalityMarine`** adds **`caco/pain`** beside the marine cry; **`BARON.dec`** **`FatalityZombieman`** / **`FatalitySergeant`** split long **`3HF1 I`** / **`3HF1 A`** holds with **`superbaron/act`**; **`DEMONS.dec`** / **`MechDemon.dec`** **`FatalityZombieman`** add **`demon/melee`** after the opening grunt. **Dark Imp family** (**`DarkImpST`**, **`DarkImpNami`**, **`DarkImpNether`**, **`DarkImpVoid`**) **`FatalityMarine`** gnaw beats replace misplaced **`monster/ovlbit`** with **`dimp/active`**.

- **SSG execution warp audio (pass 2):** **`Death.ExeCution1`** on **`DEMONS.dec`**, **`MechDemon.dec`**, **`MeanDemon.dec`**, **`VOIDSPECTRE.dec`** adds **`misc/xdeath3`** between **`demon/pain`** and **`demon/melee`**. Nobles **`BARON.dec`** / **`KNIGHT.dec`** / **`BELPHEGOR.dec`** replace legacy **`PSXDPN`** with **`baron/pain`**, **`knight/pain`**, and **`superbaron/pain`**, and insert **`misc/xdeath3`** before the strike (**`CyberKnight.dec`**, **`CyberPaladin.dec`**, **`Infernus.dec`** aligned the same way). **`Afrit.dec`**, **`CYBERDEMON_BOSS.dec`** gain the middle gore layer. **Cat's Frozen** **`PB_CatsFrozen_Monsters.dec`** (**`PB_FrostBaron`**, **`PB_Cryocubus`**, **`PB_FrostBrain`**) replace the silent **30**-tic single warp with **three 10**-tic warps plus **`PBCF/*`**, **`fatso/pain`**, **`caco/melee`**, **`superbaron/pound`**, and **`misc/xdeath3`** as appropriate.

- **Options / PDA UI typography:** PDA codex UI (**`zscript/PBPDA.zc`**) uses **green** translation colors (**`Font.CR_GREEN`** / **`CR_OLIVE`**) for labels, description pane, blocky tiles, and XP readout while leaving **`pdaTextScale`** / **`DTA_ScaleX`/`Y`** sizing unchanged. (**`Font "BigFont", "Red"`** in **`MENUDEF.txt`** was removed — **UZDoom 4.14.3** **`MENUDEF`** does not support that keyword; use engine menu theming or a newer engine line if BigFont/red menus return.)

- **`BioAcidLauncher`:** **Alt-Fire** is always a **hold-to-spray Daedabus-style slime stream** (**`BioAcidLauncherSlimeStream`**). Removed from **`PlasmaRifleWeaponSpawners.dec`**; rare **`DropItem`** on **`PB_Daedabus`** and **`PB_Belphegor`** only. Pickup strings in **`language.enu`**; PDA preview uses **`PDA_Preview_AcidAlt`** for the stream strip. **`pb_NoBioAcidLauncherWeapon`** retained for legacy configs but no longer gates map spawns (menu row removed).

- **Custom Shards & Potions (sprites refresh):** Re-folded from **`Custom Shards&Potions old Sprites.wad`** (ZIP): **`B0N1`** / **`B0N2`** bonus sprites under **`SPRITES/ITEMS/Health/HP Bonus/`** and **`SPRITES/ITEMS/Armor/`**, refreshed **`4RM1`–`4RM3`** armor pickup art, hi-res **`GRAPHICS/HiResPickups/Bonus/ABON1`–`ABON8`**, pickup OGGs in **`sounds/items/csp/`**, and **`BMAP/CSPBonuses.txt`** brightmaps. **`HealthPlus`** / **`ArmorShard`** / tier **`PB_*`** bonuses use **`B0N*`** in DECORATE + GLDEFS (replacing vanilla **`BON1`** / **`BON2`** on those actors).

- **Kill-streak drone (`Familiar`):** **20 second** lifespan (**`35 * 20`** world tics via **`familiarLiveTics`** in **`FamiliarBase`**, **`zscript/Items/PBKillstreak/FamiliarBase.zc`**), then self-terminates. On any death, spawns **`FamiliarDroneBurst`** (monster-only **ExplosiveImpact** falloff out to **192** mu, skips players / friendlies / other **`Familiar`**), plays **`FAREXPL`**, and ejects five **`FamiliarRock*`** shards with momentum (**`Speed 18`** on **`FamiliarRock1`** base). **`actors/Items/PBKillstreak/PB_UnlessYouHavePowah.dec`**.

- **Health/armor bonus pickups (Custom Shards & Potions — DarkShadow):** folded **Custom Shards & Potions** randomizers for **HealthPlus**, **ArmorShard**, **PB_Stimpack**, and **PB_Medikit** (bridges under **`actors/Items/Health/CustomShardsPotions/`**, **`lumps/includes/gldefs/CustomShardsPotions.inc`**), with hi-res bonus art in **`GRAPHICS/HiResPickups/Bonus/`** and pickup sounds in **`sounds/items/csp/`**. Removed legacy Wolvus BON3–BON8 tier bonus actors; **BonusSpawners** tier branches now spawn **HealthPlus** / **ArmorShard** so the same colored pool applies everywhere. Soulsphere / megasphere / blue-armor elite paths still use **`ToggleItemMutators`** (**`pb_item_mutators`**). Kill-streak medikit pool and stimpack fallback grant **PB_Medikit** / **PB_Stimpack**. **Custom Red Armor** (**PB_RedArmor**, DarkShadow): **`actors/Items/Health/CustomRedArmor.dec`**, sprites **`SPRITES/ITEMS/Armor/4RM3*`**, optional hi-res **`GRAPHICS/HiResPickups/Armor/4RM3B0`**; **`BlueBodyArmorSpawner`** can roll **PB_RedArmor** instead of **HeavyArmor** when **`pb_redarmor_spawn`** is on (~25%).

- **Item mutators naming:** cvar **`pb_wolvus_item_mutators`** → **`pb_item_mutators`**; ACS script **`ToggleWolvusItemMutators`** → **`ToggleItemMutators`** (`SRC/cvars.txt` — recompile into the **`cvars`** library listed in **`LOADACS.txt`** so **`ACS_NamedExecuteAlways`** matches). Menu keys **`OPTMNU_PB_ITEM_MUTATORS`** (`language.enu` / **`MENUDEF.txt`**).

- **Weapon pickup hint banner:** removed **`DrawPB2022WeaponPickupHint`** from **`PB_EventHandler.RenderOverlay`** (`zscript/PBPDA.zc`), deleted related **`transient ui`** state, dropped **`pb_weapon_pickup_hints`** from **`CVARINFO`**, and removed the **Gameplay Settings** menu row (**`MENUDEF.txt`** / **`PB_PICKUP_HINTS*`** in **`language.enu`**). Center-screen gold **`PB_WPNHINT_*`** text no longer runs in-engine (strings may remain in **`language.enu`** for reference).

- **Kill streak / “Unless You Have Powah” powerup screen overlays:** `Powerup.Color` / colormap tint alphas brought down to **~0.002** on RGB-style pickups, **GoldMap** / **GreenMap** pairs to **0.008** where supported, plus minimal **`PB_KSHasteSphere` / `PB_KSDoubleSphere` / `PB_KSInvulnerabilitySphere`** overrides. Realm667 fold (**`Realm667Powerups.dec`** / **`.zs`**), **`PB_UnlessYouHavePowah*`**, **`PB_KillstreakItems.dec`**, **`ElectricAuraSphere` / `PowerDeflect`**, and vanilla-adjacent **`HasteSphere` / `DoubleSphere` / `TimeSphere` / `InvulnerabilitySphere_`** in **`SpecialPowerups.dec`** / **`GenericPowerups.dec`** match. **PB HP Arcade** pickup flashes (**`PBHP_PowerupArcade.dec`**) use **`A_SetBlend(..., 0.06, …)`** instead of **0.75** so Life / Energy / Berserk rewards do not white-out the view when **`pb_hp_arcade`** is on.

- **`PB_Railgun` hologram deploy:** **Alt-Fire** while **holding Reload** spawns **`PBCF_CF_HoloDecoyPlacer`** (BDP-style decoy: line trace, tracer anchor, retargeting). **Alt-Fire** alone still toggles the scope zoom when unscoped. Hint string updated in **`language.enu`** (`actors/Weapons/Slot7/RAILGUN.dec`).

- **Marauder (`PB_Marauder`) Arch-vile spawner odds:** the Peacekeeper only rolled from **`ArchVileSpawner`** packs; **`DiceMain`** **`EarlyLevelMob` / `LowLevelMob` / `MidLevelMob`** had **no** **`MarauderPack`** branch (so long cluster-1 stretches could see **zero** Marauders), and **`HighLevelMob` / `DiceTier*`** weights were low. Added rare **`MarauderPack`** **`A_Jump`** lines on early/mid **`DiceMain`** tiers, small rolls on **`DiceTier1`** / **`DiceTier2`**, and raised numerators on **`HighLevelMob`** / **`DiceTier3`** / **`DiceTier4`** / **`DiceDeathWish`** (**`actors/SPAWNERS/MonsterSpawners/ArchvileSpawner.dec`**). **`SRC/spawnscript.acs`** documents that **`SpawnerScript`**’s `LevelNumber` is **`LEVELINFO_CLUSTERNUM`**, not map index.

- **Dragon Slayer (`DragonSlayer` / `PB_ObsidianDragonSlayer`):** removed from **`ChainsawWeaponSpawner`** map dice (same treatment as **`MastermindChaingun`** / **`MancubusFlameCannon`** on launcher/plasma spawners). Obtain **`PB_ObsidianDragonSlayer`** only from **`Obsidian Ravager`** death; **`pb_NoDragonSlayerWeapon`** gates the drop, not map spawns. **`README.md`** slot-1 / boss-tech notes and **`PDAWEAPT`** dossier copy document Ravager-only acquisition (parity with acid launcher / mastermind chaingun entries).

- **Monster-sourced heavy weapons (wear + drops):** **`PB_CyberdemonRL`**, **`MastermindChaingun`**, **`MancubusFlameCannon`**, and **`MarauderSSG`** use parallel **`Ammo`** wear pools (**`actors/Weapons/MonsterWeaponDurability.dec`**) with **`WeaponBreak`** when depleted; duplicate pickup refills wear via **`AttachToOwner`** and same-class **`HandlePickup`** (**`PB_WeaponBase`** in **`zscript/Weapons/BaseWeapon.zc`**, **`zscript/Weapons/Slot6/CyberdemonRL/PB_CyberdemonRL.zs`**). **Drops:** **`CyberdemonBoss`** + **`XDeathCyberdemonGun`** (~50% after **`pb_NoPB_CyberdemonRLWeapon`**), **`PB_Mastermind`** chaingun chance **`72`** (was always-on), **`PB_Marauder`** table **`80`** + fatality **`A_Jump(128, …)`** on bonus floor spawns, mancubi / GK fatso family **`MancubusFlameCannon`** **`56`**. **`MastermindChaingun`** / **`MancubusFlameCannon`** removed from **`LauncherWeaponSpawner`** / **`PlasmaWeaponSpawner`** map dice (**`RocketLauncherWeaponSpawners.dec`**, **`PlasmaRifleWeaponSpawners.dec`**); removed orphan **`pb_NoMastermindChaingunWeapon`** / **`pb_NoMancubusFlameCannonWeapon`** cvars and their **Weapon Spawns** menu rows (**`CVARINFO`**, **`MENUDEF.txt`**).

- **Pro-Surv Ballista (`ProSurv_Ballista`):** Weapon Special uses the radial wheel (**`specialWheel_ProSurv_Ballista`** in **`zscript/PbWheel/ev_core_special.zsc`**) with **Standard** / **Demonic** / **Module not installed** slices; **`WeaponSpecial`** dispatches **`Select_Ballista_*`** tokens and keeps **pickup `SwapToDemonicMode`** and legacy “no wheel token” paths via **`BallistaSpec_ToDemonic`**. Upgrade parity with PBX **`Crossbow_Upgraded`** (mirrored on **`DemonicBallista`** pickup alongside **`BallistaUpgraded`**). Demonic reload / empty-bolt branch now gates on **`PB_DTech` ≥ 3** (was 5). **`DemonicBolt`** / **`ProSurvRazorBlade`** death FX use **`PBExplosionparticlesSmall`** instead of **`BDExplosionparticlesSmall`**.

- **`SelectFirstPersonLegs` on `PB_WeaponBase`:** the shared select bring-up (channel stops, inventory token clears, **`Melee_Equipment_Handler_Overlay` / `KickHandler_Overlay` / `Equipment_Toggle_Handler_Overlay`**, **`FirstPersonLegsStand`**, jump to **`SelectContinue`**) now lives in **`zscript/Weapons/BaseWeapon.zc`** instead of **`actors/Weapons/BaseWeapon.dec`**; DECORATE **`PB_Weapon`** subclasses keep **`goto SelectFirstPersonLegs`**, and ZScript-only weapons use **`Goto` / `goto SelectFirstPersonLegs`** after per-weapon setup. Staging ports **`PB_Deagle`** and **`PB_MetalSniper`** use the same entry, then **`PB_WeaponRaise`** at the start of **`SelectContinue`** (alongside **`VorpalBlade`**’s raise-after-legs pattern).

- **Asset filenames:** extensionless binaries under **`SPRITES/`**, **`GRAPHICS/`**, **`brightmaps/`**, and most of **`SOUNDS/`** were renamed to typed extensions (PNG/JPEG/GIF/OGG/WAV/FLAC/MP3, else **`.lmp`** for Doom patches and other non-audio binaries). Text references under **`BMAP/`**, **`lumps/`**, **`actors/`**, **`zscript/`**, etc., were updated for renamed **`brightmaps/`**, **`SOUNDS/`** / **`sounds/`**, and **`GRAPHICS/`** / **`graphics/`** paths. **Intentionally left extensionless:** Doom **DMX** audio (**`0x03 0x00`**), ASCII text shards (Realm667 **`DECORATE`** / **`SNDINFO`** / … under **`SOUNDS/`**), and reserved root lump basenames. Maintainer script: **`maint_scripts/rename_extensionless_assets.ps1`**. Fifteen basename pairs where an extensionless file differed from an already-present **`.png`** / **`.ogg`** were resolved by removing the redundant extensionless copy (see **`maint_scripts/extensionless_rename_conflicts.log`**). The **`tools/`** directory is **gitignored** in full for local-only helpers (nothing under it is committed).
- **Crucible first-person view (`CRCB` A–J):** updated **`SPRITES/WEAPONS/GloryKillLauncher/Crucible/CRCB*0.png`** from **`thedoctorofdoom/glorykills`** commit **`3b28b4c`** ([Sprites: update Crucible weapon view with Doomguy black fingerless gloves](https://github.com/thedoctorofdoom/glorykills/commit/3b28b4c47ff9e80ca0ae4faf1b687a3eb097c513)); lump names unchanged (`actors/Weapons/BaseWeapon.dec` Crucible states unchanged).
- **Fusil (`PB_Fusil`):** `actors/Weapons/Slot4/PB_Fusil.dec` reset to **`origin/PB2022_Staging`** so the weapon matches the staging branch (removes **`dev-ra`** main-fire `RPTF G` / `H` / `I` frames before `A_Refire` and any uncommitted local Fusil edits).

### Added

- **Shield Saw ↔ Glory Kill launcher:** Quick-melee throw now requires **`ShieldSawReady`** (same **`GKEquipmentLauncher`** / **`CoolShieldSaw`** cadence as flame belch and ice bomb; **`sv_ShieldSaw_Cool`** slider in **Glorykill Options**). Throw consumes **`ShieldSawReady`**; recall / return does not bypass cooldown. Eternal Glory HUD (**`PB_GloryFuelHudOverlay`**) draws **`GKSHA00`/`GKSHA01`** at the far-right strip slot.
- **Shield Saw (ZScript projectile):** **`ShieldSawProjectile`** is a full **`FastProjectile`** ZScript class (**`zscript/Weapons/ShieldSawProjectile.zc`**) — idle flight, rip stick, homing return, reflection, and path damage without per-tic checker actors or **`CallBack`** ACS polling. **`ShieldSawPlayerHandler`** replaces map-enter ACS; **`actors/Weapons/Slot1/ShieldSaw.dec`** keeps tokens, **`FastCall`**, and trail/VFX only.

- **Hitmarkers ([Tekish HitMarkers](https://github.com/Tekkish/HitMarkers), fold by TomiikiPro):** full upstream options submenu **`HM_Options`** under **PB 2022 Enhanced** (replaces on/off-only toggle). **`ACS/hitmarkers.o`** ships beside **`SRC/hitmarkers.acs`** (**`LOADACS.txt`**). Marker art for all **`Fontdefs.txt`** styles: **`GRAPHICS/default/`**, **`circle/`**, **`impact/`**, **`small/`**, **`vertical/`**. **`hitmarkershandler`** credits player-owned projectiles (inflictor chain), **`Species "Hitbox"`** headshots, and skips **`NukageBarrel`** family props; **`SNDINFO.txt`** lists every logical name used by the ACS bridge.

- **Shield Saw reflective flight:** thrown **`ShieldSawProjectile`** (**`zscript/Weapons/ShieldSawProjectile.zc`**) carries **`+REFLECTIVE`** / **`+AIMREFLECT`** ( **`+SHOOTABLE`**, **`+NODAMAGE`**, player **`Species`** ) so hostile shots in its path bounce back at the shooter; each tic also spawns a brief **`ShieldSawReflectField`** shell. ZScript **`DeflectHostileMissilesInPath`** manually aims **all** hostile missiles in range back at their shooter (clears **`+DONTREFLECT`** / **`+NOSHIELDREFLECT`**, no **`Destroy()`**), once per shot via **`ShieldSawDeflectedMark`**. Player-owned shots are skipped via **`+THRUSPECIES`**.

- **Helmet visor HUD SFX (PB Staging parity, minimal):** **`SOUNDS/HUD Sounds/`** (blood-drop drips, helmet shatter pool, interference, help ping, screen wiper, dying/garbled visor) plus **`SNDINFO.txt`** logical names matching Staging’s **`SNDINFO.PBDoomguy and Marines`** / root visor death lines. **`PB_VisorHandler`** in **`zscript/PB_VisorOverlay.zc`** now plays **`visor/glasscrack`** on **`DamageType` `Head`** hits, **`visor/interference`** while accumulated plasma interference decays (staging-style random per-tic), **`visor/blooddrop`** on nearby monster-sourced **`Blood`** spawns (excluding mist / **`NashGoreBloodParticle1`**, throttled), and a one-shot **`visor/dyingvisor`** on first dead tick when the fullscreen helmet is enabled and **`CameraRunner`** is off. **`visor/screenwipe`**, **`visor/helpnotification`**, and **`visor/visorgarbled`** remain defined for hooks / mods; no PB2022 dirty-screen or help-notification UI consumes them yet.

- **Maintainer script `maint_scripts/Diff-PlayerDecorateSounds.ps1`:** cross-checks **`A_PlaySound` / `A_StartSound` / *SoundEx** first-string arguments in **`actors/Player/PLAYER.dec`**, **`NEWPLAYE.dec`**, and **`actors/Weapons/Slot1/MELEE.dec`** against logical names parsed from **`SNDINFO.txt`** (`$alias`, `$random` blocks, simple defs). **`-FailOnUnknown`** exits **1** for CI; **`-AllowMissingSound`** extends the allowlist (default **`floorhard`** for vanilla IWAD footstep). **PowerShell:** `return ,$names` so a single-element **`HashSet`** is not unrolled into a scalar at the return site.

- **Lever Action Hell Mod:** T3/T4 upgrade pickups (**`LeverActionHellUpgradeT3`/`T4`**, **`LeverActionHellTier`**), **`UpgradeSpawner`** + tiered **`BackpackSpawner`** rolls, **`pb_NoLeverHellUpgrades`**, and **Weapon Special** slice **`Select_LeverAction_HellMode`** toggling **`LeverActionHellMode`** (Demon-Tech **`Hellbullet`** / **`Hellbullet2`** on hip-fire and ADS).

- **Obsidian Ravager — `PB_ObsidianDragonSlayer` boss drop:** **`class PB_ObsidianDragonSlayer : DragonSlayer`** (**`zscript/Weapons/MeleeWeaponPack/DragonSlayer.zc`**) shares the full Dragon Slayer kit but uses **`ObsidianDragonSlayerDurability`** wear (**40** swings / volleys, **`actors/Weapons/MonsterWeaponDurability.dec`**) and **`WeaponBreak`** when depleted. **`Obsidian Ravager`** **`Death` / `XDeath`** spawns the pickup at the corpse (**`zscript/Monsters/ObsidianRavager.zc`**) unless **`pb_NoDragonSlayerWeapon`** is on (**Weapon Spawns** menu: **Drop Dragon Slayer (Obsidian Ravager)**).

### Changed

- **PB_RedArmor (DarkShadow — Custom Red Armor):** **`actors/Items/Health/CustomRedArmor.dec`**, **`SPRITES/ITEMS/Armor/4RM3*`**, **`GRAPHICS/HiResPickups/Armor/4RM3B0`**; **`BlueBodyArmorSpawner`** can roll **PB_RedArmor** on the heavy path when **`pb_redarmor_spawn`** is on (**`CVARINFO`**, **`MENUDEF.txt`**, **`language.enu`**).

- **Cat's Frozen holographic decoy (equipment wheel):** deployment now mirrors **Brutal Doom Platinum** railgun-style behavior (line-of-sight placement anchor, tracer timeout, prior-decoy teardown via **`PBCF_CF_KillHologram`**, third-person marine animation pool, **`A_AlertMonsters`** + sight-checked retargeting of monsters that were chasing the player). Implementation: **`PBCF_CF_HoloDecoyPlacer`**, **`PBCF_CF_HoloTarget`**, **`PBCF_CF_HoloBlood`**, expanded **`PBCF_CF_HoloDecoy`** (**`zscript/Items/CatsFrozen/PBCF_CatsFrozenEquipment.zc`**), **`PBCF_CF_KillHologram`** token (**`actors/Items/CatsFrozen/Equipment.dec`**), equipment deploy spawns the placer (**`zscript/Weapons/BaseWeapon_Equipment.zsc`**). Logic adapted from BDP **`ZScript/Weapons/Slot6/Railgun.txt`** (branch **`gore-overhaul`**, upstream **EmeraldCoasttt/BrutalDoomPlatinum**).

- **Monster weapon wear definitions (`MonsterWeaponDurability.dec`):** undroppable **`Ammo`** classes **`CyberRLDurability`**, **`MastermindCGDurability`**, **`MancubusFlameCannonDurability`**, **`MarauderSSGDurability`** plus ZScript **`AttachToOwner`** / **`HandlePickup`** top-ups on **`PB_WeaponBase`** (**`zscript/Weapons/BaseWeapon.zc`**). Behavior / drop tuning summarized in the **[Unreleased] → Changed** bullet **“Monster-sourced heavy weapons (wear + drops)”**.

- **Hazard floor / sector damage HUD pops:** damage from **sector specials and terrain flats** (no inflictor/source, not `DMG_PLAYERATTACK`) draws **BigFont** totals near the **bottom center** with **bubble-style** motion (multi-frequency horizontal wander, slight vertical bob, ease on rise, soft fade, subtle scale “breathing”). Visibility matches **ammo total floaters** — **`dam_enabled`** or **`pb_xp_rewards`**. Colors follow **`dam_usetypes`** + **`DAMNUM_TYPECOLOR_*`** (added entries for common sector names: Slime, Lava, Crush, Nukage, HellSlime, etc.). `PB_HazardDamageHudHandler` (`zscript/damnums/PB_HazardDamageHud.zc`, `language.enu`, `ZSCRIPT.zc`, `zmapinfo.txt`).

- **Ammo pickup DamNums:** picking up any **Ammo** inventory shows a blue floating **total** count for that ammo type when **`dam_enabled`** *or* optional **PDA XP / reward spin** is on (`pb_xp_rewards`, so floaters still appear with XP on but combat damage numbers off). Same PBXP-style digit actor and `dam_usetypes` color path; new `DAMNUM_TYPECOLOR_PBAMMO` → `DamBlue`. `PB_AmmoTotalFloaterHandler` snapshots per-player ammo each tic (`zscript/Items/PB_XP/PB_AmmoTotalFloater.zc`, `PB_XPRewards.zc`, `ZSCRIPT.zc`, `zmapinfo.txt`, `language.enu`).

- **Windows smoke launcher:** `maint_scripts/Smoke-GZDoom.ps1` is committed (engine via **`PB_SMOKE_ENGINE`** / PATH / Program Files / App Paths; IWAD via **`PB_SMOKE_IWAD`**, **`DOOMWADDIR`**, Steam common paths, and **`Ultimate Doom\(Doom Mod Builds)`**). Runs the repo as **`-file`**, **`+logfile`** `smoketest_<timestamp>.log`, **`-stdout`**, optional **`-Norun`** (passes **`-norun`**), **`-Developer`**, **`-VerboseWarnings`** (`+verbose`). After exit, prints **Warning** lines (skipping typical **KEYCONF** / **subtract** unknown-key noise) and exits **1** if the log matches fatal parse/script/compile substrings; **`-Norun`** with **UZDoom** exit **1337** is treated as success when the log is clean (`AGENTS.md` section 6).

- **Fusil rifle fold (slot 4):** folded the external Fusil addon into the main weapon roster as **`PB_Fusil`** with a hidden sidearm-stance swap on **Weapon Special**, tuned 24-round internal magazine plus conservative **`NewClip`** reserve, chaingun-spawner integration, dedicated HUD ammo block, advanced spawn toggle (**`pb_NoPB_FusilWeapon`**), PDA codex entry + custom preview-strip animation labels, XP reward-spin eligibility, pickup/hint strings, and loose addon art normalized into **`.png`** sprites plus **`.mp3`** support sounds. Credit pass records **TomiikiPro** for the PB2022 port and marks the original Fusil addon authorship as still unverified pending an upstream source (`actors/Weapons/Slot4/PB_Fusil.dec`, `DECORATE`, `actors/Player/PLAYER.dec`, `actors/SPAWNERS/WeaponSpawners/ChaingunWeaponSpawners.dec`, `SBARINFO.txt`, `CVARINFO`, `MENUDEF.txt`, `language.enu`, `SNDINFO.txt`, `zmapinfo.txt`, `PDAWEAP`, `PDAWEAPT`, `zscript/PBPDA.zc`, `zscript/Items/PB_XP/PB_XPRewards.zc`, `README.md`, `CREDITS.txt`, `SPRITES/WEAPONS/Slot4/Fusil/`, `sounds/combat/weapons/Fusil/`).

- **Optional PDA XP & reward spin (single-player):** `server bool pb_xp_rewards` (default off) under **PB 2022 Enhanced → UI and Feel**. When enabled, monster kills credit XP, crossing rank thresholds grants spendable **reward points**, and the blocky PDA home hub shows a left-side **Reward Spin** tile (3 points per spin) that grants **weapons only** from a fixed pool (refunds if none eligible); when disabled there is no XP accrual and no slot-style panel (`CVARINFO`, `MENUDEF.txt`, `language.enu`, `zscript/Items/PB_XP/PB_XPRewards.zc`, `zscript/PBPDA.zc`, `zmapinfo.txt`, `ZSCRIPT.zc`).

### Removed

- Per-roster spawn toggle for Project Survival fodder — removed (standard Tier‑1 content). The **`pb_disablenewenemies`** gate that previously short-circuited `PB_ProjectSurvivalFodderPassive.PostBeginPlay` was also removed in the same pass — PS fodder now spawns as standard always-on content, ungated alongside vanilla zombieman / shotgunner / chaingunner / imp / pinky / spectre replacements. The unused helper class `PB_ProjectSurvivalFodderPassiveUtil` (one-shot CVar-read wrapper) was deleted with it. The DECORATE-side `A_JumpIf(GetCvar("pb_disablenewenemies")==1,"Die")` in `FodderPassiveSpawn.dec`'s `Create:` state was also removed for consistency.
- Tracked **`tools/`** directory (smoke tests, ACC wrappers, zip sync helpers); keep any local build scripts **outside** the tree or in an untracked **`tools/`** folder — see **`.gitignore`**.
- **PDA (blocky mode): dossier icon under description text.** The selected-entry icon that drew below the wrapped dossier body (constants **`PB_PDA_DESC_ICON_*`**) is removed — tile-grid icons and the new right-column preview cover visualization (`zscript/PBPDA.zc`).

### Fixed

- **X12 Shotgun (`X12Shotgun.dec`):** Restored **`ShotgunWasEmpty`** inventory token (previously only in retired **`SHOTGUN.dec`**) so the seven reload/pump/unload references compile without script warnings.

- **Map-load / spawn hitch (handlers):** **AccuDeath** `AccuDeathSpawnHandler` caches DeHackEd lump path and six blood-color dummy actors per map (no per-spawn `Wads.FindLump` / spawn-destroy); skips non-monster, non-missile spawns; Heretic/Hexen stock missile typing removed (PB targets Doom / Doom II only). **NashGore** builds `BLUDTYPE` list once per session. **HatExtravaganza** reads `PB_HatExtravaganza` once on `WorldLoaded`. **Simple Teleporter Effects** sector scan stays batched (`PB_ST_SECTORS_PER_TIC` 1024) with early line-special filter.

- **Simple Teleporter Effects load hitch:** sector scan no longer runs entirely inside `WorldLoaded` (could freeze UZDoom on large maps). Teleporter pads are discovered in batches via `WorldTick`, and sectors with no teleporter/exit specials skip `CenterSpot` work.

- **AccuDeath smoke / `PB_AccuDeathCompat.zc`:** UZDoom 4.14.3 rejected spawn-time `damageType` writes from the compat helper (`Expression must be a modifiable value`); compat now only sets **`PB_Rocket`** `bEXTREMEDEATH` and early-returns for PB projectile class names so DECORATE **`DamageType`** values are not overwritten by the vanilla AccuDeath spawn pass.

- **`A_PlaySound` / `A_StartSound` / `A_StopSound` numeric channels (ZScript):** same **`channel % 7`** rule for quoted-sound **`A_PlaySound` / `A_StartSound`** and digit **`A_StopSound(n)`** when **`n ≥ 7`** across **`zscript/**/*.zc`**, **`.zs`**, **`.zsc`**, and **`.ZMV`**. **Katana barrier** (**`BeamKatana_Weapon`**, **`ArgentSith_Weapon`**) uses **`CHAN_5`** with **`A_StopSound(CHAN_5)`** in **QuickMelee** so the loop is not on **`CHAN_AUTO`**. **Hook** launch/fail (**`PlayerPawn.zc`**) uses **`CHAN_5`** instead of **`CHAN_7`**. **Visor dying SFX**, **PDA reward spin key**, **Trite** steps / headshot, and **Obsidian ravager** pain **`LGLoop`** use **`CHAN_6`** or **`CHAN_5`** instead of **`CHAN_7`**. **`PB_Killstreak_Handler`** and **`PB_SetPlayerExecutionProperties`** (**`BaseWeapon.zc`**) still use **`CHAN_7`** for announcer / execution silencing.

- **`A_PlaySound` / `A_StartSound` / `A_StopSound` numeric channels (actors):** **`A_PlaySound("…", channel)`** / **`A_StartSound("…", channel)`** with **`channel ≥ 7`** remapped to **`channel % 7`** (valid **`CHAN_0`**-**`CHAN_6`** only) across **`actors/**/*.dec`**, with matching **`A_StopSound`** cleanup, so fatality / weapon / prop layers do not use invalid high indices or **`CHAN_7`** (reserved for kill-streak announcer VO in **`PB_Killstreak_Handler`**). **Obsidian ravager** **`LGLoop`** + **`A_StopSound`** use **chan 5**; **imp rocket** spawn pain uses **`A_PlaySound(..., 0, …)`** for the four-argument form the bulk pass skipped.

- **`A_PlaySound` / `A_StartSound` with `CHAN_*` (DECORATE):** removed gameplay use of **`CHAN_7`** in **`SAW.dec`** (chainsaw idle loop → **`CHAN_5`**), **`Blighter.dec`** / **`Burster.dec`** (sight cue + stops → **`CHAN_BODY`** — **`CHAN_4`** is not a valid DECORATE identifier on UZDoom **4.14.3**), **`Hellduke.dec`** / **`Horrorspawn.dec`** / **`Hierophant.dec`** (slide / gore / spray → **`CHAN_6`**). **`RAILGUN.dec`** deselect no longer calls **`A_StopSound(CHAN_7)`** (no in-file paired **`CHAN_7`** play; avoids cutting kill-streak VO on holster).

- **`A_PlaySoundEx` / `soundslot7` / `SoundSlot7` (DECORATE):** **`Stormcast.dec`** **`electogun/fly`** moved off channel **7**; **`BFGBEAM.dec`** Leech beam **`A_PlaySoundEx` / `A_StopSoundEx`** use **5** instead of **`soundslot7`**; **`UACSMG.dec`**, **`Carbine.dec`**, **`MP40.dec`** replace **`"SoundSlot7"`** with **5** so those weapon paths avoid **`CHAN_7`**.

- **`player/cyborg/KICK` recursive `$alias` (SNDINFO):** **`$alias player/cyborg/kick player/cyborg/KICK`** normalized to the same logical name and tripped **`Grecursive sound $alias found for player/cyborg/KICK`**, disabling the sound. Removed the redundant lowercase alias; **`KICK`** now uses **PB Staging’s** **`sounds/Doomguy and Marines/DOOMGUY/Sounds/Kick/KICK.ogg`** (verbatim from **`01 Project_Brutality-PB_Staging`**) instead of the ambiguous **`KICK KICK`** lump binding or a fist **`$alias`**.

- **Melee fist wind-up / incidental hits (`SNDINFO.txt` + `sounds/Gore/gore/`):** **`gore/tension*`**, **`NeckSnap`** (`OHSNAP1`–`4`), **`FUCK4`/`FUCK6`**, and related cues still use **`sounds/Gore/gore/GoreTension1.ogg`–`GoreTension4.ogg`** (plus **`OHSNAP5`** → **`ShieldSaw/Bash`**). **`GoreBreak.ogg`** / **`GoreMinimum.ogg`** live beside them. **`gore/headshot`** → **`fistblod`**. **Bare-fist wind-ups / raise / smash / ultr whoosh** are handled by the **PB Staging fist pack** bullet below (**`COMBAT/WEAPONS/Melee weapons/Fists`**), not GoreTension.

- **Bare hands (`Melee_Attacks`) bottom-right HUD:** **`DrawPB2022WeaponPickupOverlay`** skips **`Melee_Attacks`** (**`PB2022WeaponHudSkipWeapon`** in **`zscript/PBPDA.zc`**).

- **PB Staging fist pack (`SNDINFO.txt` + `sounds/`):** **`weapons/fists_up`**, **`weapons/fists_smash`**, **`weapons/fistwhoosh` / `fistwhoosh2`**, **`weapons/ultrwhoosh`**, **`SwingWave*`,** and **`KNUCKLED`** now use explicit paths under **`sounds/COMBAT/WEAPONS/Melee weapons/Fists/`** and **`sounds/Doomguy and Marines/DOOMGUY/Sounds/Fists/`**, matching **PB Staging 0.3.x** loose-file layout; OGGs refreshed from that tree. **`marine/superfist1`–`6`** → **`plspnch1`–`6`** in the same **`DOOMGUY/Sounds/Fists`** folder (replaces **`ExecutionImpact*`** for **`player/cyborg/fist`** / **`MELEE.dec`** hit puffs). Duplicate **`SNDINFO`** fist block updated the same way.

- **Marauder SSG fire (`MSSFR`) vs other slot-3 shotguns:** Peacekeeper **`MSSFR*`** had no **`$volume`** boost (unlike **`weapons/sg`** / **`SSHFIRE`**) and alt-fire used **`A_PlaySoundEx`** at default gain while primary used **`A_StartSound`** at **1.5×**. **`$volume MSSFR 2.0`** in **`SNDINFO.txt`**; **`actors/Weapons/Slot3/MarauderSSG.dec`** uses **`A_StartSound(..., 1.0, ATTN_NORM)`** on **`CHAN_WEAPON`** for every **`MSSFR`** path so primary and alt match.

- **Shield Saw audio (`GK_ShieldSaw` / PBWP addon `11 GK_ShieldSaw`):** **`sounds/combat/ShieldSaw/`** OGG/WAV refreshed from that folder (including **`Hit1`–`3`**, **`SHLDRIPR.wav`**, **`FlyingShld.ogg`**). **`SNDINFO.txt`** now matches the addon’s logical layout: **`shieldsaw/ripper`**, **`$random ShieldSawBounce`** over **`ShieldSawBounce1`–`5`**, **`$random ShieldSawHit`**, same core names (**`ShieldSawSwing`** … **`ShieldSawBash`**).

- **DECORATE compat stub includes:** **`actors/Compat/WarningStubs.dec`** (and **`ExecutionFxStubs` / `LateWarningStubs`**) lived in a small folder that was easy to drop when zipping the mod; UZDoom then aborted with *Could not find script lump `actors/Compat/WarningStubs.dec`*. The three shards are now under **`actors/Items/Compat/`** with **`DECORATE`** **`#include`** paths updated (**`PORTING_ADDONS.md`**, **`AGENTS.md`**, **`PB_CyberdemonRL.zs`** comment).

- **Lady Golide (`HASG`) Danmaku while zoomed:** **`Fire2Drumed`** matched hip **`Drum_Fire`** for Danmaku projectiles + casing spawner but omitted the five **`ShotgunParticles`** calls present in PB Weapons Pack **`Lady_Golide.dec`** — ADS Danmaku looked thin on muzzle smoke/sparks (**`actors/Weapons/Slot3/HASG.dec`**).

- **Kill-streak `FamiliarDroneBurst` (ZScript 4.5.0):** Local **`const`** in **`PostBeginPlay`** fails parse (**`Unexpected 'const'`**) — use plain **`double` / `int`** locals (**`zscript/Items/PBKillstreak/FamiliarBase.zc`**).

- **Minigun belt notes under `SPRITES/`:** **`SPRITES/WEAPONS/Misc/Casings/MinigunBelt/ASSET_NOTES.txt`** triggered **Invalid data encountered for texture** — moved to **`docs/MinigunBelt_LMLN_ASSET_NOTES.txt`** (plain text must not live under **`SPRITES/`**).

- **Player / melee incidental SFX (`SNDINFO.txt`):** **`marine/superfist1`–`6`** use **PB Staging** **`plspnch1`–`6`** under **`sounds/Doomguy and Marines/DOOMGUY/Sounds/Fists/`** (see **PB Staging fist pack** bullet); **`weapons/railpunch`** follows **`marine/superfist6`**. **`FUCK2`–`FUCK4`/`FUCK6`**, **`MTAUN01`–`07`**, **`AXSWING`**, **`FLMDRAW`**, and both **`Machete/Yum`** pools avoid bare IWAD-only lumps. **`weapons/plasmaloop`** ( **`ArachnoGun_Plasma`** fly loop in **`MELEE.dec`**) was undefined — **`$alias`** to **`weapons/bfg_chargeloop`**. **`weapons/plasmax`** (plasma-ball **`DeathSound`** across **`MELEE.dec`**, **`PLASMA.dec`**, **`M2PLASMA.dec`**, etc.) **`$alias`**es to **`weapons/plasma/explode`**. Cruelty-bonus **`TAUNT01`–`16`** and **`ONELIN01`–`12`** no longer self-alias to missing lumps — they chain to **`MTAUN01`–`07`** (already **`$alias`**ed to **`grunt/pain`**). Maintainer triage: **`maint_scripts/audit_player_sounds.ps1`** scans **`PLAYER.dec`**, **`NEWPLAYE.dec`**, and **`MELEE.dec`** for **`A_PlaySound` / `A_StartSound`** names vs **`SNDINFO`** (line-start logicals, **`$alias`** LHS, **`$random`** pool names).

- **UAC-41 Carbine dual-wield reload:** **`ReadyDualWield`** now spawns **`IdleLeft_Overlay` / `IdleRight_Overlay`** on a **`CB01 E 0`** frame before the main layer switches to **`TNT1`**, so the raise animation does not sit on one visible PSP frame while overlays are empty (removes the brief “both guns vanish” pop at the end of dual reload). **`NewCLip`** → **`NewClip`** in **`actors/Weapons/Slot4/Carbine.dec`** so reserve checks / per-round gives use the real ammo class on case-sensitive loads.

- **Weapon soft-lock after equipment / grenade attempts (`PB_M41A`, `BDPBattleRifle`, other ZScript-only guns):** **`A_DoPBWeaponAction`** could **`return ResolveState("GunEmpty")`** whenever the weapon’s **`UnloaderToken`** was held, but many ZScript-only classes never define a **`GunEmpty`** label — **`ResolveState("GunEmpty")`** is **null**, so the function returned before **`A_WeaponReady`**, blocking fire and weapon switches while melee overlays still ran. The **`GunEmpty`** branch now runs only when that state exists, **`UnloaderToken`** is non-empty, and **`PSP_WEAPON`** is valid (**`zscript/Weapons/BaseWeapon.zc`**).

- **UAC-M1 Plasma Rifle (“M1”) soft-lock (no fire / no weapon switch):** **`Fire`** jumped to **`FireDualWield`** while **`DualWieldingPlasma`** was set, but that state label was missing from **`actors/Weapons/Slot6/PLASMA.dec`**, so the weapon could land in a bad state graph. Added **`FireDualWield`** that clears refire and returns to **`ReadyToFireDualWield`**. Alt-fire wind-up (**`PLSA`** charge loop and **`AltHold`**) now calls **`A_WeaponReady(WRF_ALLOWRELOAD|WRF_NOFIRE)`** each tic so you can still change weapons during overcharge. **`PB_WeaponIsInReadyState`** already treats **`ReadyDualUnloaded`** as ready for dual-empty idle. **`PB_M2Plasma`** alt-fire’s dual branch now defines **`FireDualWield`** → **`LeftToRight`** (jump target was previously undefined).

- **Chaingunner / commando fatality arm rip (silent `misc/xdeath4*`):** **`FatalityComando`** / **`FatalityComando2`** and other finishers called **`misc/xdeath4a`** / **`misc/xdeath4c`**, but **`SNDINFO`** still bound those logical names to missing extensionless **`dsxdth4*`** lumps. **`misc/xdeath4a`–`misc/xdeath4i`** now point at shipped Nash gore / Glory Kill / GoreBreak samples (**`SNDINFO.txt`**) so the arm-tear cues play again.

- **Demon Tech soldier (hood + Hell Rifle) helmet fatalities + Lost Soul / Phantasm stomp SFX:** **`FatalityHelmetComando1`** / **`FatalityHelmetComando2`** mirrored the arm-rip missiles from **`FatalityComando`** but never played the matching **`misc/xdeath4*`** / pain / fist cues (**`actors/Player/NEWPLAYE.dec`**). Fist **`KillLS`** used **`gore/headshot`** bound to a missing **`BMHESHT`** lump; **`PoorPhantasm`**’s **`LSOD`** death pool and **`PoorLostSoul`**’s **`skull/melee`** pointed at missing / undefined samples (**`SNDINFO.txt`** — **`gore/headshot`** path, **`LSOD`** → **`misc/xdeath4*`**, new **`skull/melee`**).

- **Pinky / demon third fatality (`FatalityDemon3`) final stomp:** Knee beats already used **`demon/pain`** / **`player/cyborg/fist`**; the closing **`misc/xdeath2`** call was silent because **`misc/xdeath2a`–`misc/xdeath2o`** still pointed at missing **`dsxdth2*`** lumps. Those logical names now use shipped gore paths (**`SNDINFO.txt`**, same idea as **`misc/xdeath4*`**). **`actors/Player/PLAYER.dec`** adds a **`flesh/step`** on the stomp-down frame before **`FD2M F`**’s long hold so the foot hit reads even under **`$volume misc/xdeath2 0.5`**.

- **Fatality SFX hard pass (`SNDINFO.txt` + Overlord arm rip):** **`misc/xdeath1a`–`misc/xdeath1h`** and **`misc/xdeath3a`–`misc/xdeath3e`** still used missing **`dsxdth*`** lumps (silent **`misc/xdeath`**, **`misc/xdeath1`**, **`misc/xdeath3`** anywhere — curbstomps, HK, imps, etc.). **`neckb1`/`neckb2`** and **`OHSNAP1`–`OHSNAP6`** (**`NECK_BRK`** / **`NeckSnap`**) pointed at missing gore lumps. **`weapons/fistwhoosh`**, **`weapons/fistwhoosh2`**, and **`weapons/ultrwhoosh`** used missing **`fistwh*`** / **`F1STWH*`** samples (silent arm-windups on **`FatalityOverlord`** and many **`NEWPLAYE.dec`** / **`MELEE.dec`** beats). All are remapped to shipped **`sounds/Gore/gore/GoreTension*`**, Nash gore, Glory Kill **`fistblod`**, or **`GoreBreak`** paths; **`dsxdth*`** references are removed from **`SNDINFO.txt`**. **`FatalityOverlord`** adds **`misc/xdeath4c`** on the arm-rip beat after **`weapons/fistwhoosh`**.

- **Fatality SFX second wave (`SNDINFO.txt`):** bare-lump bindings that third-person finishers still hit (**`BONEBRK1`–`3`** in **`NECK_BRK`**, Glory-block **`BONECR1`/`BONECR2`**, **`SwingWave1`/`SwingWave2`**, **`CLAP`**, **`SMASH1`**, **`BURNUP`**, and direct **`FUCK1`/`FUCK5`** plays) are remapped to the same shipped gore / flame-belch pool so Hell Trooper swings, cyber kneecap crunches, imp curbstomps, baron “clap” beats, and ice-knight smashes stay audible without per-state DECORATE churn. **`EYEPULL1`/`EYEPULL2`** bind to **`sounds/monsters/ProjectSurvivalPassive/Gore/EYEPULL*.ogg`** (explicit extension — the prior extensionless paths did not resolve).

- **Commander Shotgun (`PB_CSSG`) Danmaku mode:** **`DanmakuProjectile`** referenced **`TPEL`** art that was missing from the main mod; **`SPRITES/WEAPONS/Slot3/CSSG/Effects/TPELA0.png`** is the PB Weapons Pack orb (source lump was extensionless **`TPELA0`**, normalized to **`.png`** in both trees). Tracer tint initializes in **`BeginPlay()`** (PBX-style). Bounce ring particles still fall back **`SHWKK0` → `SHwKK0` → `5PRKA0`** if a texture name fails on case-sensitive loads (**`zscript/Weapons/Slot3/CSSG/CSSG.zs`**).

- **Commander SSG + Lady Golide (`HASG`) shell casings (Triple Doom / Danmaku):** **`PB_SpawnCasing`** referenced **`TDoomCasing`** / **`DanmakuCasing`** class names that were never defined, and **`ShotCaseSpawnHASGDAN`** previously inherited **`ShotCaseSpawn3`** (dragon casing). PB Weapons Pack art is folded as **`SPRITES/WEAPONS/Slot3/CSSG/Casings/ShotgunShell/DC0S*.dds`** (Danmaku hull) and **`…/Casings/Doom/TDS1*.png`** (Triple Doom hull). New **`ShotCaseSpawnDanmaku`**, **`DanmakuCasing`**, and **`TDoomCasing`** live in **`zscript/Weapons/Slot3/CSSG/CSSG_Casings.zs`** (**`ZSCRIPT.zc`** include after **`CSSG.zs`**). **`NashGoreStatics.zc`** treats **`ShotCaseSpawnDanmaku`** like the other shot-case spawners for casing batching.

- **`TDoomCasing` spin states + HASG casings notes:** **`TDoomCasing`** **`SpinFast` / `SpinMedium` / `SpinSlow`** used invalid one-line **`TDS1 K 1 2 …`** state syntax (UZDoom ZScript parse errors); they now use the same **`TDS1 ABCDEFGHIJ…` + `2 { … }`** pattern as **`DanmakuCasing`** / **`WhitePShellCasing`** (**`zscript/Weapons/Slot3/CSSG/CSSG_Casings.zs`**). **`SPRITES/WEAPONS/Slot3/HASG/Casings/ASSET_NOTES.txt`** moved to **`zscript/Weapons/Slot3/CSSG/HASG_CASINGS_ASSET_NOTES.txt`** so the engine does not scan plain text as a sprite lump.

- **M41A underbarrel 30mm + equipment (dual) + Metal Sniper tube:** map rocket reserve is **`NewRocketAmmo`** (rocket boxes / pinata), not **`PB_RocketAmmo`** (compat stub only). **`PB_M41A`** alt grenade pump now checks / takes **`NewRocketAmmo` → `PB_RocketAmmo` → `RocketAmmo`** (**`M41A_HasTubeReserve` / `M41A_TakeTubeReserve`**). **`PB_MetalSniper`** underbarrel reload uses the same pool (**`MS_HasTubeReserve` / `MS_TakeTubeReserve`**). **`UseEquipment`** restores **`PB_M41A`** dual idle overlays (**10** / **11**) after **`A_ClearOverlays(3,65)`** so **User1** equipment still runs while dual-wielding.

- **PB_XM21 (XM-21) floor pickup + fatality idle:** world **`Spawn`** uses **`X21S A -1`** (side-profile bring-up art) with **`FloatBobStrength 0.35`** and **`Scale 0.95`** instead of **`WM14 B` Bright** at **`0.9`**; **`Steady`** uses **`X21G A`** instead of **`TNT1 A`** while **`GoFatality`** loops so the rifle stays drawn during glory-kill / fatality freezes.

- **Lady Golide (`HASG`) + Commander SSG explosive / alt payloads vs PB Weapons Pack:** **`HASG_Projectiles.dec`** (after **`M41A.dec`** + **`Nailgun.dec`**) replaces **`PB_12GAPellet_ASG` → `DragonsBreathTracer`** and **`IN_MGNail` → `DragonsBreathTracer`** stubs with **`PB_12GAPellet`** / **`MGNail`**-based rounds (flechette + buckshot visuals). **`zscript/Weapons/Slot3/HASG/HASG_Projectiles.zs`** adds **`GolideExplosiveProjectile`**, **`DarkDanmakuProjectile`** (damage **10**), **`BigFlamesIG_SP`**, and **`WPhosphorusProjectile_Golide`** (white-phosphorus bursts spawn **`BigFlamesIG_SP`** like the pack). **`ExplosiveProjectile`** in **`CSSG.zs`** matches pack tuning (**`Gravity 1`**, damage **12**, **`A_Explode(60,…)`**, **`Spawn`** **`TNT1 A 1`** before **`Fly`**); **`WPhosphorusProjectile`** damage function uses **`15*frandom(2,3)`** again.

- **Lady Golide (`HASG`) drum ejecta:** **`ShotCaseSpawnHASG5` / `HASGEXP` / `HASGDAN` / `HASGWP`** use pack-style **`RifleCaseSpawn`** spawns into **`ShotgunCasing5HASG`** (**`CAS8`**), **`ExplosiveCasingHASG`** / **`WPhosphorusCasingHASG`** (**`WCS1`**), and **`DanmakuCasingHASG`** (**`DC0S`**, same strip as PB_CSSG). Hull state machines match **`HASGCasings.zc`** with **`Rest6Dead`** fixing the pack’s duplicate **`Rest2Dead`** label on **`Rest6`**. Sprites are copied to **`SPRITES/WEAPONS/Slot3/HASG/Casings/ShotgunShell/`** from PB Weapons Pack **`SPRITES/WEAPONS/Casings/ShotgunShell/`**. **`NashGoreStatics.QueueCasings`** over-cap cleanup recognizes these hull class names.

- **Minigun / LMG belt links + Commander SSG shell visuals:** **`LMGBeltLink`** / **`LMGMissileBeltLink`** referenced **`LMLN`** sprites that were never shipped; Mastermind **`CAS8K*`** pack art is copied as **`LMLNA0`–`LMLND0.png`** under **`SPRITES/WEAPONS/Misc/Casings/MinigunBelt/`**. **`WhitePShellCasing`**, **`ExplosiveShellCasing`**, **`FlakShellCasing`**, **`BuckShellCasing`**, and **`FlechetShellCasing`** no longer inherit generic **`ShotgunCasing`** **`CAS2`** tumble art — **`zscript/Weapons/Slot3/CSSG/CSSG_Casings.zs`** **`extend`**s them with **`WCS1`**, **`XCS1`**, **`CAS9`**, **`CASX`**, and **`CAF8`** state machines. Full **`CASX`** / **`CAF8`** A–T strips under **`SPRITES/WEAPONS/Slot3/CSSG/Casings/ShotgunShell/`** are duplicated from the pack’s complete **`CAS2`** / **`CAS6`** ShotgunShell strips (the pack ships **`CASXK*` / `CAF8K*`** composites only, not spin strips — see **`docs/CSSG_Casings_ASSET_NOTES.txt`**). Valid **`WCS1K1` / `WCS1K5`** (and existing **`WCS1K0`**) sprites sit beside them; non-standard composite filenames (**`WCS1K2K8`**, etc.) stay archived under **`GRAPHICS/Casings/wp/`** so they do not register as ambiguous sprite lumps.

- **Kill streak rewards with everything disabled:** milestone logic still called **`PBRollReward`** with an empty pool and then granted **`Stimpack2`** as a fallback, and **`GiveKSReward`** always applied **`PB_KSFreeze`** (short speed tint). The pool is now empty when no toggles apply (and backpack/supplies paths miss); in that case **no** stimpack, **no** freeze token, and **no** pending power-up—only the streak title message when HUD text is on. **`PB_KillStreakPowerUp.Use`** returns early on an empty class name so stray pickups cannot fire the old fallback. *Note:* a high-contrast / inverted view is usually **Invulnerability** or **Time Sphere** (`GoldMap`); those options sit **above** the “Life Shield / Crucifix / …” block in **PB Killstreak → Power-Up Pool**—turn those off too if the overlay persists.

- **Captured / rescue marine (`CapturedMarine`):** **`DSSALUTE`** was a self-alias (only worked when the IWAD supplied that lump). It now aliases **`grunt/sight5`**; duplicate **`DSSALUTE`** **`SNDINFO`** line removed. **`HelpMe`** occasionally plays **`TorturedMarinePB`** when **`pb_quietmarines`** is off. Rescue path uses **`A_PlaySound("DSSALUTE", CHAN_AUTO)`** like the classic-marine branch (**`actors/Friendly Marines/CapturedMarine.dec`**, **`SNDINFO.txt`**).

- **X-12 Shotgun (`X12Shotgun`) + SNDINFO extension pass:** shipped the **`PB_X12_Shotgun`** add-on samples under **`sounds/combat/weapons/X12/`** with explicit **`.ogg`** / **`.wav`** names (source lumps were extensionless). **`SNDINFO.txt`** now points **`X12FIRE`** / **`X12PMP`** / **`X12PMP1`** / **`X12PMP2`** at those paths. Same file adds explicit **`.ogg`** / **`.wav`** suffixes for the Commander SSG **`CSSG*`** bank, **Battle Rifle** **`BR45*`** paths, and **Marauder / Peacekeeper** **`MSS*`** rips so Linux loose-folder loads resolve reliably.

- **Commander Shotgun (`PB_CSSG`) flak shells:** flak used ZScript **`CSSGChunk1`/`2`/`4`** stubs (minimal `FastProjectile` defaults, no proper missile setup). Fire now uses the Auto Shotgun **`Chunk1`/`Chunk2`/`Chunk4`** actors (**`actors/Weapons/Slot3/AUTOSHOTGUN.dec`**) via **`PB_FireBullets`**. Flak fire SFX now use **`A_PlaySoundEx`** on **`Weapon`**, **`Soundslot6`**, and **`Auto`** (**`FLAKFIRE`** + **`CSSGFULL`**/**`CSSGFLKF`** / single-barrel **`CSSGSNGL`**/**`CSSGFLKS`**) instead of numeric channels only.

- **Commander Shotgun (`PB_CSSG`) / HASG slug shells:** **`PB_12GASlug`** was a DECORATE stub inheriting **`DragonsBreathTracer`**, so slug mode still spawned fire-trail / ignite projectiles. Slug is now **`class PB_12GASlug : PB_Projectile`** in **`zscript/Weapons/Projectiles/BulletDef.Shell.zsc`** (bullet damage, normal tracer visuals); **`actors/Weapons/Slot3/HASG.dec`** drops the old alias.

- **PDA HUD weapon overlay (`zscript/PBPDA.zc`):** **`PB2022WeaponHudSkipWeapon`** is now **`clearscope static`** so **`ui void`** draw paths can call it (fixes *Can't call play function … from ui context*). (The former center-screen **`PB_WPNHINT_*`** banner draw path was removed entirely in a later pass.)

- **UZDoom 4.14.x ZScript (`MonsterWeaponDurability`):** standalone **`extend class`** on DECORATE-only **`MastermindChaingun`**, **`MancubusFlameCannon`**, and **`MarauderSSG`** failed compile (*cannot be found in the current translation unit*). Wear top-up + same-class **`HandlePickup`** now run from **`PB_WeaponBase.PB_RefillMonsterSourcedWeaponWear`** in **`zscript/Weapons/BaseWeapon.zc`**; **`zscript/Weapons/MonsterWeaponDurability.zc`** removed (**`ZSCRIPT.zc`** include dropped).

- **DECORATE `WeaponBreak` (monster-tech chaingun / SSG / flame cannon):** ZScript-style **`for (int i = 0; …)`** inside **`{ }`** state blocks is not valid DECORATE on UZDoom 4.14.x (parse error: *Expected '(' but got identifier 'i'*). **`WeaponBreak`** shard bursts unrolled to five identical triple-**`A_CustomMissile`** passes (**`actors/Weapons/Slot6/MastermindChaingun.dec`**, **`actors/Weapons/Slot3/MarauderSSG.dec`**, **`actors/Weapons/Slot9/MancubusFlameCannon.dec`**).

- **Stray Realm667 `ZSCRIPT` lumps under `SOUNDS/Realm667Pack/`:** removed addon-era shards (broken **`#include`** in **`possessedgibs/zscript.txt`**, duplicate **`ObsidianRavager`**, and unused **`Venor` / `Vulgar` / `BabyCaco` / `Nitromite` / `DarkCardinal`** scripts) so the engine only merges the root **`ZSCRIPT.zc`** tree — avoids merged-script failures that surface as **`PB_WeaponBase` not found** for **`PB_Weapon`**.

- **Beam Katana / Argent Sith barrier:** monster-sourced explosive damage (**`Explosive`**, **`Rocket`**, **`ExplosiveImpact`**, **`BarrelExplosion`**, **`Grenade`**, **`Frag`**) no longer applies to the player when a **`KatShieldPart`** / **`KatShieldPart2`** segment is nearby (splash overlap with **`+REFLECTIVE`** barrier). **`PB_KatanaShieldFilter`** + early **`PlayerPawnBase.DamageMobj`** gate (**`zscript/Weapons/Slot1/PB_KatanaShieldPlayerFilter.zc`**, **`zscript/PlayerPawn.zc`**, **`ZSCRIPT.zc`** include order).

- **CSSG PBX casing stubs:** buck/slug/dragon/explosive/flak/flechette/white-PB aliases remain one-liners at the end of **`zscript/Weapons/Slot3/CSSG/CSSG.zs`**. Pack-accurate **Triple Doom** / **Danmaku** hull visuals (**`TDoomCasing`**, **`DanmakuCasing`**, **`ShotCaseSpawnDanmaku`**) ship in **`zscript/Weapons/Slot3/CSSG/CSSG_Casings.zs`** with sprites under **`SPRITES/WEAPONS/Slot3/CSSG/Casings/`** (an older empty **`CSSG_Casings.zs`** placeholder was removed in a prior pass—this file is the real implementation).

- **Fusil / XM-21 slot roster:** **`PB_Fusil`** **`Weapon.SlotPriority`** **`0.18`** (below **`PB_XM21`** **`0.21`**). **`PB_SideFusil`** (Weapon Special sidearm stance only) uses **`Weapon.SlotNumber -1`** and no longer occupies slot 4 priority.

- **Monster weapon wear (PBX parity):** picking up a **second** copy of **`PB_CyberdemonRL`**, **`MastermindChaingun`**, **`MancubusFlameCannon`**, or **`MarauderSSG`** while already holding one merged ammo via **`Weapon.HandlePickup`** without calling **`AttachToOwner`**, so wear never refilled. Same-class **`HandlePickup`** now runs the same wear top-up as **`AttachToOwner`** (**`PB_WeaponBase`** in **`zscript/Weapons/BaseWeapon.zc`**, **`zscript/Weapons/Slot6/CyberdemonRL/PB_CyberdemonRL.zs`**).

- **Dying*.dec corpse `Suffer` strips (explicit SFX):** long bleed-out states in **`DyingImps.dec`**, **`DyingDarkImps.dec`**, and **`DyingGuys.dec`** that only used **`A_Pain`** / spawn-time audio now open with **`A_PlaySound`** on **`imp/pain`**, **`dimp/pain`**, or **`monsters/zombie/suffer`** (voice channel for imps) so multi-second choreography has a clear first cue. **`DeadImp_Half`** / **`DeadSavageImp_Half`** multi-label gib bursts (**`Death.ExtremePunches`** chain in **`IMPS.dec`**, **`SavageImp.dec`**) cue **`misc/gibbed`** before **`GibsZ`**. Maintainer audit: **`maint_scripts/audit_fatality_sfx.py`** — **`--dying`** scopes **`actors/**/Dying*.dec`** and matches **`Death*` / `Pain*` / `Suffer` / `DieNow` / `Active`**; **`--choreo`** matches **`Death.Melee` / `Kick` / `SuperPunch` / `ExtremePunches`**, **`SpecialFatality*`**, **`FatalityMarine*`**; **`--playsound-only`** counts **`A_PlaySound*` / `A_Playsound` / `A_StartSound`** (still ignores **`A_Pain`** / screams).

- **Glory curbstomps + lab / chainsaw scientist fatalities:** curbstomp chains (**`CurbstompZombieman`**, **`CurbstompSergeant`** in **`actors/Player/PLAYER.dec`**; **`CurbstompHelmetZombieMan`**, **`CurbstompNazi`**, **`CurbstompHFQSZ`** in **`actors/Player/NEWPLAYE.dec`**) now cue **`flesh/step`**, **`misc/gibbed`**, and **`misc/xdeath2`** on the stomp crush so they are not silent after the death cry. **`FatalityLabGuy2` / `FatalityLabGuy3`** (axe scientist) gain **`AXSWING`** plus **`misc/gibbed`** on the heavy swings; **`FatalityLabGuy3`** uses **`A_PlaySound`** for grunt / xdeath lines. **`FatalityChainsawZombie`** starts pain / fist / **`sawswing`** before the first **`FSF4`** frames (sounds were previously queued only after the opening strip), and the saw-stop frame uses **`weapons/chainsaw/stop`** instead of **`CSAWSTAT`**.

- **El Diablo / short third-person finisher strips (audio pass):** **`FatalityHellduke1Eld`–`6Eld`** now layer **`superbaron/pain`**, **`cyber/melee` / `cyber/impact`**, **`Monsters/DKDIE`**, and **`misc/xdeath*`** on the matching beats. **`FatalityBaron3Eld` / `4Eld`**, **`FatalityHK5Eld` / `6Eld`**, and **`FatalityArchVile3Eld`** gain baron / knight / vile pain plus gore and impact cues where the strip had none. **`FatalityCrawlingArachnotron`** cues **`baby/pain`**, metal tear/hit, **`misc/xdeath`**, and **`baby/death`** through the stomp and decap. **`FatalityPVP`** in **`PLAYER.dec`** opens with **`grunt/pain`** and **`misc/xdeath`** on the dismember beat (**`NEWPLAYE.dec`**’s PVP path already had **`BPSLOP`**).

- **Monster vs marine fatality / curbstomp audio:** imp **`FatalityMarine`** (**`IMPS.dec`**, **`ImpVariant1.dec`**, **`SavageImp.dec`**) and caco **`FatalityMarine`** (**`CACODEMON.dec`**) gain **`grunt/pain`** / **`misc/xdeath*`** alongside the existing **`DSFDTHE`** death cry. Baron-on-human **`FatalityZombieman` / `FatalitySergeant`** (**`BARON.dec`**) layer grunt pain and xdeath on the head-rip beats. **`CurbstompMarine`** on **`BELPHEGOR`**, **`KNIGHT`**, **`CyberBaron`**, and **`CyberKnight`** gains grunt pain, **`misc/gibbed`**, **`NECK_BRK`**, **`misc/xdeath2`**, and/or **`cyber/impact`** so long stomp strips are not mostly silent SFX-wise. **`REVENANT.dec`** / **`Watcher.dec`** **`FatalityMarine`** add grunt pain and xdeath layers on the brain-feed / bite beats.

- **`FatalityDraugr2Eld` / `FatalityDraugr3Eld` + `NEWPLAYE` PVP:** draugr first-person strips add **`misc/xdeath*`** / **`misc/xdeath4b`** on dust and xdeath missile beats; **`FatalityPVP`** on **`PB_Doomer`** adds **`grunt/pain`** and **`misc/xdeath`** before the existing **`BPSLOP`** meat hit.

- **Pinky / demon family fatality audio:** Glory-kill **`Death.Fatality`** paths on **`PB_DemonGK`**, **`PB_SpectreGK`**, **`PB_MeanDemonGK`**, **`PB_MechDemonGK`**, **`PB_VoidSpectreGK`**, and **`CrackoDemonGK`** cue **`demon/pain`** (or **`Cracko/Pain`**) after **`A_NoBlocking`** before pinata / token grants. Non-GK **`Death.Fatality`** on **`PB_Demon`**, **`PB_MeanDemon`**, **`PB_MechDemon`**, and **`PB_VoidSpectre`** adds the same pain cue after **`A_NoBlocking`**. Player finishers layer **`flesh/step`**, **`misc/xdeath`** / **`misc/xdeath2`**, and **`misc/gibbed`** on shake / dismember / stomp beats (**`FatalityDemon`**, **`FatalityDemon2`**, **`FatalityDemon3`**, **`FatalityVoidSpectre`**, **`FatalityMeanDemon1`**, **`FatalityDemon4Eld`**, **`FatalityDemon5Eld`**, **`FatalityCrackodemon1Eld`**) alongside existing **`demon/melee`** / **`demon/pain`**. Post-fatality props **`DyingDemon`**, **`DeadDemonHalf`**, and **`DeadDemonStomped`** open with a gore or footfall cue so long idle corpse holds are not silent. Maintainer script: **`--player-fatalities`** on **`maint_scripts/audit_fatality_sfx.py`**.

- **T3 monster tier fatality audio (Glory Kill handoff + player strips):** **`Death.Fatality`** on **`T3-Floaters/GloryKills.dec`** adds **`demon/pain`**, **`monster/ovlpai`**, and **`pain/pain`** where the handoff was pinata-only; **`T3-Fats/GloryKills.dec`** adds **`fatso/pain`** after the high pinata spawn; **`T3-Revies/GloryKills.dec`** adds **`skeleton/pain`** after **`A_NoBlocking`**; **`T3-Arachnos/GloryKills.dec`** adds **`arachnophyte/pain`** / **`baby/pain`** on arachnid-family routes. **`PLAYER.dec`** / **`NEWPLAYE.dec`** third-person revenant / cacodemon / fatso / arachnotron finisher states gain **`flesh/step`**, **`misc/xdeath`**, and **`misc/gibbed`** on long shake or missile-gore beats so mid-choreography is not silent. Follow-up on **`NEWPLAYE.dec`** only: **`FatalityBeamRevenant1`**; base **`FatalityDraugr`** (**`skeleton/pain`** on entry); **`FatalityOtherFatso2`** (**`Daedabus/pain`** after helper spawn); **`FatalityArachnophyte`** second shake; **`FatalityFatso2`** / **`FatalityMagCaco`** / **`FatalityVolcabus2`** shake rows; El Diablo **`FatalityCacoDemon3Eld`**, **`FatalityCacoDemon4Eld`**, **`FatalityRevenant3Eld`** / **`4Eld`**; Monster Pack Hunger **`FatalityHunger1Eld`–`3Eld`**.

- **T4 monster tier (Nobles + Viles) Glory Kill handoff + finisher cues:** **`T4-Nobles/GloryKills.dec`** **`Death.Fatality`** uses **`A_Pain`** alone for **`PB_BaronGK`**, **`PB_InfernusGK`**, and **`PB_KnightGK`** (no stacked explicit pain on the same handoff). Pinata-only routes still cue **`baron/pain`**, **`cbaron/pain`**, **`knight/pain`**, and **`Paladin/Pain`** where there is no **`A_Pain`**. **`T4-Viles/GloryKills.dec`**: **`PB_ArchvileGK`** / **`PB_HellionGK`** rely on **`A_Pain`** only (avoids doubling with **`vile/pain`**); **`PB_FleshWizardGK`** / **`PB_IcevileGK`** keep a single explicit **`wizard/pain`** / **`icevile/pain`** after the pinata. **`NEWPLAYE.dec`** **`FatalityBelphegor`** / **`FatalityHellion`** use a low-volume **`flesh/step`** (channel **6**, volume **0.35**) before the first long **`ShakeShakeShake`** beat.

- **T3 GK handoff (no stacked pain):** **`T3-Floaters/GloryKills.dec`** drops redundant **`demon/pain`** / **`pain/pain`** plays in the same **`Death.Fatality`** chain as **`A_Pain`** (MagCaco / Pain Elemental family). **`T3-Arachnos/GloryKills.dec`** drops redundant **`baby/pain`** immediately after **`A_Pain`** on arachnid GK wrappers.

- **`BaseWeapon_Equipment.zsc` parse (UZDoom 4.14.3):** ZScript weapon `States` lines require trailing **`;`** (unlike DECORATE). Missing terminators on the early **`UseEquipment`** / **`SwitchEquipment`** chain caused **`Unexpected identifier` / `Expecting ';'`** at load. **`States`** must not be written as **`States;`**. **`GoingToReady`** / **`SelectingAnimation`** close with a proper **`States`** **`}`** brace pair (not merged onto **`Goto Ready3`**). Do not split **`TNT1 A 0`** / sprite lines from a following **`{`** on the next line—the block must attach to the same state (**`TNT1 A 0 { … }`**, **`SYNP … { … }`**). **`BRIGHT`** with a call must use **`BRIGHT { A_…(); }`**; **`RVCF … A_FireCustomMissile(…)`** lines need a trailing **`;`**.

- **ZScript-only weapons + equipment (`PB_CSSG`, `PB_NeoHMG`):** ZScript cannot subclass DECORATE-only **`PB_Weapon`**, so inheriting it caused **`unknown base class PB_Weapon`** and cascaded compile errors. Both weapons extend **`PB_WeaponBase`** again. Shared **`UseEquipment` / `SwitchEquipment` / grenade & equipment throw states** that lived only on DECORATE **`PB_Weapon`** are merged onto **`PB_WeaponBase`** via **`zscript/Weapons/BaseWeapon_Equipment.zsc`** (included from **`ZSCRIPT.zc`**); the duplicate block was removed from **`actors/Weapons/BaseWeapon.dec`**. A base **`GoingToReady`** label jumps to **`Ready3`** so equipment bailouts work for ZScript-only guns. **`PB_DualWeapon`** still overrides **`ThrowMine` / `ThrowGrenade`** and now resolves **`GoingToReady`** on the base.

- **`PB_WeaponBase` vs `PB_Weapon` parity:** **`Steady`** and **`GoingToReady2`** in **`zscript/Weapons/BaseWeapon.zc`** now match DECORATE (fatality freeze clear, **`SawSelected` → `OnLoop`**, **`SelectAnimation` / `Ready`** return path). **`SelectingAnimation` + `GoingToReady`** on the base (**`BaseWeapon_Equipment.zsc`**) share the same token-reset entry as equipment fallbacks and **`Goto Ready3`** for ZScript-only weapons; DECORATE **`PB_Weapon`** still defines the full **`Ready4` / `Ready3`** **`GoingToReady`** (child overrides the base). Mini overlay handlers (**`Equipment_Toggle_Handler_Overlay`**, **`Melee_Equipment_Handler_Overlay`**, **`KickHandler_Overlay`**) exist only on **`PB_WeaponBase`**; the duplicate DECORATE copies were removed so **`PB_Weapon`** uses the same ZScript implementation. **`Equipment_Toggle_Handler_Overlay`** uses **`return ResolveState(null)`** when the toggle is not held (empty **`ResolveState("")`** is invalid on UZDoom 4.14.3). **`PB_CSSG`** / **`PB_NeoHMG`** no longer ship a local **`Steady`** that **`Goto Ready3`** (they inherit the base **`Steady`** → **`GoingToReady`** path).

- **`PB_HazardDamageHudHandler` (UZDoom 4.14.3):** **`RenderOverlay`** is UI scope — use **`pl.PlayerNumber()`** (not **`pl.player.PlayerNumber()`**), a **`clearscope static`** CVar gate instead of **`PB_XPRewardsHandler.AmmoTotalFloatersEnabled`**, prune expired pops in **`WorldTick`** (not from the draw path), and replace **`pow`** with closed-form easing. Class constants stay untyped (**`const HAZ_TTL = 58`**, not **`const int`**).

- **Frost aura (`FrostAuraPower.Tick`):** avoid **division by zero** when `mon.health` is 0 while the actor is still iterated as a living monster (`shp / hp`); clamp a minimum divisor of 1 before computing `hpfactor` (`zscript/Items/Realm667Powerups/Realm667Powerups.zs`).

- **Glory kill / fatality audio (wider):** `SNDINFO.txt` now aliases a batch of **legacy logical names** still referenced by **`NEWPLAYE.dec`** / **`PLAYER.dec`** / a few monster deaths (`ZOSCRE`, `BPSLOP`, `HKPAIN`, `PLSM8`, `BLAM2`, `fleshimpact`, bare `xdeath` / `xdeath2c`, `vile/pain` + `vile/death` / `vile/Death`, `skeleton/attack`, `XDeath1`, `player/comehere`, and Cryotron-adjacent **`baby/*`** cues) onto **existing PB sounds** so they resolve instead of no-op. **`A_Playsound("ZOSCRE")`** typo fixed to **`A_PlaySound`**. **Monster finisher pass:** a second alias block maps **34** death/fatality-shard names used under `actors/Monsters/**` (e.g. `fatso/*`, `chainguy/death`, `Weapons/YamatoExp`, `skeleton/death`, `misc/PB_XDeath4`, `Marauderguy/death`, IceVile / FleshWizard / Hellduke cues) onto in-mod definitions. A repo smoke script confirms **all `$alias` chains resolve** and **zero** unresolved `A_PlaySound*` strings remain in monster files that reference finisher-related state labels (engine launch not run here — GZDoom/UZDoom not on `PATH`). Earlier Marauder / Commando strip fixes remain (`Marauder.dec`, `PLAYER.dec`, `NEWPLAYE.dec`).

- **Player fatality shard audio (`NEWPLAYE` / `PLAYER`):** `SNDINFO.txt` adds aliases for **`baron/pain`**, **`cyber/death`**, vanilla-style knight/baron cues **`DSDMPAIN` / `DSKNTDTH` / `DSKNTSIT`**, **`HEADRIP`**, **`player/cyborg/KICK`**, and **`weapons/rockethit`**. **`PLAYER.dec`** fixes a typo **`imp/die` → `imp/death`** (matches the existing **`$random imp/death`** pool).

- **Slot 4 weapon priority (XM-21 vs Fusil) + sidearm not in cycle:** `PB_Fusil` used a higher `Weapon.SlotPriority` than `PB_XM21`, so the engine picked the Fusil first when pressing slot 4 even though `Player.WeaponSlot` lists XM-21 before Fusil. `PB_XM21` priority is now **0.21** and main **PB_Fusil 0.18** (`actors/Weapons/Slot4/XM21.dec`, `PB_Fusil.dec`). Hidden **`PB_SideFusil`** is no longer registered in a numbered slot (**`Weapon.SlotNumber -1`**) so **`give all`** / prev-next cycling cannot land on the cheat-not-weapon sidearm view; **Weapon Special** still selects it via **`A_SelectWeapon("PB_SideFusil")`** (`actors/Weapons/Slot4/PB_Fusil.dec`).
- **Glory Kill Eternal (NewHUD) fuel strip invisible:** the Eternal layout is now drawn from **`PB_EventHandler.RenderOverlay`** in **`zscript/GloryKills/PB_GloryFuelHudOverlay.zc`** (same art as `GRAPHICS/HUD/glorykills/newhud/`), so it sits above the fullscreen SBARINFO without depending on a fresh **`GloryHUD.o`** compile. The ACS `sv_fuelhud_background == 3` branch is timing-only; **`SRC/GloryHUD.acs`** still adds **`HUDMSG_PLAIN`** to **`GLORYHUDMSG`** for the legacy panel modes when recompiled.
- **Glory Kill Eternal HUD strip spacing:** NewHUD icons use a uniform **center-to-center** step scaled from a **21** virtual-pixel baseline at 480p (was **17**) so crucible, pips (when present), blood punch, flame belch, and ice bomb stay in one row without border overlap (`zscript/GloryKills/PB_GloryFuelHudOverlay.zc`).
- **Shoulder launcher back-to-back use:** **`FireShoulderCannon`** no longer routes solely on **`FlameBelchSelected` / `IceBombSelected`** when the chosen ability is still on **`*Ready`** cooldown — if the other ability is ready, selection flips and that shot fires immediately (`actors/Weapons/BaseWeapon.dec`).
- **`FireShoulderCannon` DECORATE parse (UZDoom 4.14):** every `if` that ends in **`return ResolveState(...)`** now uses a braced body so the weapon parser does not abort with **`Expected '{', got 'return'`** during **`LoadActors`** (`actors/Weapons/BaseWeapon.dec`).

- **Auto Shotgun + Quad Shotgun sprites missing (invisible pickup / view).** Restored the full **`SPRITES/WEAPONS/Slot3/AUTOSHOTTY`** and **`SPRITES/WEAPONS/Slot3/Quad-Shotgun`** trees from git **`bf0cf3a93`** (last revision before **`858dc24a0`** deleted hundreds of `Slot 3` autoshotgun frames and **`98376ded0`** dropped the remaining `Slot 3` → `Slot3` rename fallout). Brightmaps-only strips are not substitutes for base weapon sprites; this brings back world (`QSPG`, etc.) and first-person strips (`AG10`, `QSSG`, `QSGS`, …) expected by `AUTOSHOTGUN.dec` / `QUADBARREL.dec`.

- **Fusil hip-fire:** `RPTF G`–`I` recovery frames no longer sit **before** `A_Refire` (rapid taps could fight the next shot’s muzzle strip). After `RPTF F`, `A_Refire` runs first; only when the player is not holding fire does a one-shot `PB_FusilHipFireTail` token queue `G`–`I` **after** one **`Ready3`** ready tic (`RPTG A 1` + `A_DoPBWeaponAction`), then **`FusilHipFireTail`** clears the token and returns to **`Ready3`**. Token cleared on **Deselect** (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Startup log noise: `RevenantRider` invalid-state warnings removed.** The base `Doomer` state table in `PLAYER.dec` referenced `RevenantRider` / `RevenantRiderPain`, but the real ride states live on `PB_Doomer` in `NEWPLAYE.dec`; added base-state shims so startup no longer warns while the subclass keeps the real possession flow (`actors/Player/PLAYER.dec`, `actors/Player/NEWPLAYE.dec`).

- **Fusil reload no longer steps through the upright donor poses on entry.** Both reload branches now skip the early `M201 A-C` climb that was still flashing upward-facing frames immediately after clicking reload, and the PDA reload preview was trimmed to match the lower-angle in-game sequence (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil reload no longer flashes the base ready pose again on entry.** Both main and sidearm reload states were opening with a one-tic visible `RPTG`/`SIDE` ready frame before the lowered donor reload sequence, which read as the weapon sprite "popping up" a second time; the reload path now enters the body animation without that extra visible tic (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil reload rebuilt around the original WAD choreography for testing.** The PB fold keeps the no-entry-pop fix and still uses the source add-on's clip-drop / `Reload` cue, but the upright-looking `M201 G-O` donor tail was removed again after testing; the reload now returns through the lower-angle settle frames and the PDA preview matches that trimmed version (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil firing animation pass:** hip-fire now plays through the extra `RPTF G-I` recovery frames, ADS settles through `SHGA B-D` instead of snapping straight back from the muzzle-blast pose, and the sidearm stance now returns all the way to `SIDE A`; PDA fire previews were updated to match the fuller recoil/recovery cycle (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil fire audio hardened across all three gunfire branches.** Hip-fire, ADS, and sidearm fire now all start `MG42FIR` on the first visible recoil frame via `CHAN_WEAPON` with overlap enabled and a light pitch variance, replacing the mixed one-shot/weapon-channel calls so the file uses a single robust fire-sound pattern throughout (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil weapon-special sidearm now consumes ammo on fire again.** The hidden `PB_SideFusil` branch had its `PB_FusilMag` take on a zero-tic prelude before the first visible `SIDE` recoil frame; moving the ammo subtraction into that visible firing frame fixes the “weapon-special mode shoots for free” regression (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil sidearm knife altfire now has a working swoosh.** The folded addon was still calling the missing `SLICKER` sound on the weapon-special melee branch; it now uses PB’s existing `weapons/gswing` quick-melee swing alias instead (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil scope now follows the TC's hold/toggle ADS pattern.** The scoped `Ready2` loop now respects `pb_toggle_aim_hold` like other PB weapons: hold-mode drops out of zoom on `AltFire` release while still allowing primary fire during the hold, while toggle mode keeps the existing press-to-enter / press-again-to-exit behavior (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil scoped refire now stays on the ADS fire path.** The zoomed `Fire2` tail no longer uses the generic refire path that could fall back into normal-fire behavior while zoomed; it now mirrors PB's scoped-weapon routing so held fire chains stay in `Fire2` and zoom exits still honor the hold/toggle ADS rules (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil scoped shots now consume ammo again.** The ADS `Fire2` branch was still subtracting `PB_FusilMag` on a zero-tic prelude before the first visible `SHGA K` recoil frame; moving the ammo take onto that visible scoped firing frame fixes the remaining “zoomed fire costs nothing” regression (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil PDA ready preview now enters faster.** The weapon codex builder now treats `PB_Fusil` like the other long-intro weapons whose generic ready segment is quartered, shortening the Fusil PDA-ready portion by about 75% without changing the live in-game weapon animation (`zscript/PBPDA.zc`).

- **Fusil zoom fold now tracks the original addon more closely.** After the placement/scale experiments introduced centering and bobbing issues, the scoped path was refolded around the source `Fusil.wad` behavior: no ADS-only `PSP_WEAPON` scale hack, default weapon offset, original `SHGA A-D` scoped idle cadence, and the original `SHGA K/A/K/A` scoped-fire recovery while preserving the later PB2022 fixes for hold/toggle ADS, scoped refire routing, and visible-frame ammo consumption (`actors/Weapons/Slot4/PB_Fusil.dec`, `C:\\Users\\shawn\\Downloads\\Fusil.wad`).

- **Fusil scoped overlay no longer drops after each shot.** The zoomed `Ready2` loop was still clearing overlay lanes `10`/`11` on re-entry after `Fire2`; removing that scoped-only clear keeps the scope graphics stable between zoomed shots while leaving the non-scoped overlay clears intact (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Fusil rifle: PB2022 control-flow parity pass.** Main and sidearm stance now use PB's shared weapon-raise helper on select, the live fire states honor the standard pre-fire `GoFatality` guard instead of shooting through pending finishers, and the sidearm melee branch respects the same fatality handoff. Startup smoke-tested cleanly after the fold (`actors/Weapons/Slot4/PB_Fusil.dec`).

- **Project Survival Ballista firing audio restored** — `weapons/ballista/*` logical names were never registered in `SNDINFO.txt`, so primary (`firebolt`), alt (`firerazor`), and demonic (`firedemonic`) shots plus reload / bolt sounds resolved as silence; wired the definitions to the already-shipped `SOUNDS/COMBAT/WEAPONS/ProSurvBallista/*.ogg` set (upstream Project Survival equivalents).

- **PDA dossier pane height and body text** now jointly autosize per entry — short descriptions get a snugger pane with larger text; long ones use the full pane with smaller text and a clip-rect safety net (`zscript/PBPDA.zc`).

- **PDA weapons mega-tile:** cycling **`Fire`** previews no longer pull in Doomguy / third-person marine / kick-slide-air overlays / experimental execution choreography — **`PDA_ShouldSuppressWeaponPlayerSideArt`** now drops additional sprite families (**`STAL`**, **`MARN`**, **`MARJ`**, **`PLA1`**, **`FATF`**, **`KOKO`**, **`SLDK`**, **`AKCK`**, **`D3MI`/`D3ML`**, **`P1N0`**, **`EXE*`**, plus existing **`PLAY`/`STAN`/legs/fist/TH3** keys), and **`PDA_Main_FireWalk_ShouldStopBeforeState`** stops **`NextState`** walks at PB **`Flash*`** / **`DoKick`** / glory-kill / **`Execution_*`** chord entry (**`PDA_Main_PushTo`** weapon **`Fire`** + **`PDA_Play_AppendChain`** weapon **`Fire`/`AltFire`** when **`filterPlayerBody`**). (`zscript/PBPDA.zc`)

- **PDA monster codex: kills never unlocked catalogue rows.** `ApplyMonsterCodexKill` compared **`String.Format("%s", ty)`** on each **`GetParentClass()`** step to bare names from **`PDAMONSTT`**, but ZScript **4.5.0** does not format class pointers as stable plain identifiers — the comparison never hit and **`FoundMonster:`** never fired. The codex registry now stores parallel **`(class<Actor>)(name)`** pointers (**`PDA_CodexMonsterClasses`**) and matches **pointer equality** against **`(class<Actor>)(ty)`** at each hierarchy tier (same “first matching tier only” behavior as before). **`FoundMonster:`** / **`UiTick`** unchanged (`zscript/PBPDA.zc`).
- **PDA monster codex: `(class<Actor>)(stringVar)` cast silently produced null entries.** Follow-up to the codex-kill fix: **`RegisterMonsterCodexClass`** was casting the raw **`String cls`** parameter directly via **`(class<Actor>)(cls)`**, which in ZScript **4.5.0** does **not** auto-resolve a `String` to a class pointer — every push landed `null`, so the new pointer-equality match never hit either. The registrar now mirrors the proven **`PDA_Play_BuildWeapon`** / **`PDA_Play_BuildMonster`** pattern: **`Name nmCls = cls; (class<Actor>)(nmCls)`**. Same idiom applies anywhere else — convert string class references to **`Name`** before any **`(class<X>)(…)`** runtime cast (`zscript/PBPDA.zc`).
- **PDA monster preview: stray Doomguy `PLAY*` frames crept into Imp (and any monster with execution-chained Death).** `PDA_Play_AppendChain` walked monster state graphs via `NextState` and skipped only empty-frame **`TNT1`** sprites; an Imp's `Death` chain transitions through Glory Kill / third-person fatality choreography that briefly uses `PLAY*` (player) sprites, which were rendered into the right-side preview pane between actual imp death frames. Added an `isMonster` flag on **`PDA_Play_AppendChain`** that, when true, additionally rejects 4-char-prefix `PLAY` sprites; **`PDA_Play_BuildMonster`** passes it for all six monster state walks (`Spawn` / `See` / `Missile` / `Melee` / `Pain` / `Death`). Weapon preview deliberately keeps `isMonster=false` so legitimate first-person PSP walk frames are unaffected (`zscript/PBPDA.zc`).
- **PDA monster codex: `PDAMONSTT` lump lookup collided with `PDAMONST`.** Real root cause of the persistent "kills don't unlock monster rows" symptom (the prior cast/format fixes were necessary but not sufficient). **`PDAMONSTT`** is **9 characters** — Doom's classic short-name lump table truncates to 8, so `Wads.FindLump("PDAMONSTT")` was returning the 8-char **`PDAMONST`** comma-separated names lump (same lump id, identical raw length). Splitting that on `#` yielded a single block with no `$`-fields, so **every** call to `RegisterMonsterCodexClass` was skipped and the registry stayed empty. Switched the dossier load to **`Wads.CheckNumForFullName("PDAMONSTT")`** (long-name path, ZIP-stored files preserve full filenames). Added a one-shot `[PDA] WARNING: monster codex empty after init` console line that fires only when registration finishes with zero classes despite a non-empty names lump, so a future regression of this kind is loud instead of silent. Optional **`pb_pda_debug`** user cvar (off by default; declared in `CVARINFO`) gates verbose `[PDA-DBG]` traces in `WorldThingDied` / `ApplyMonsterCodexKill` / `RegisterMonsterCodexClass` for any future codex investigation (`zscript/PBPDA.zc`, `CVARINFO`).
- **ZScript 4.5.0 parse fix — `Array<Class<Actor>>` parametrized types need an interior space.** The 4.5.0 parser rejects two consecutive close-angle brackets without a space (`>> without space in parametrized types only supported for zscript >= 4.15.1`); declared as **`Array<Class<Actor> >`** instead. Same rule applies to any future `Array<Array<X> >` / `Array<Map<…> >` shapes (`zscript/PBPDA.zc`, `AGENTS.md` PDA quirks block).

- **PDA preview: ZScript 4.5.0 — runtime state labels use `FindStateByString`, not `FindState`.** `Actor.FindState(StateLabel)` is compile-time only (literal at the call site); `PDA_Play_AppendChain`'s **`label`** argument is now declared **`String`** (string literals at the 12 call sites coerce cleanly) and passed straight to **`def.FindStateByString(label, true)`** so `Select` / `Fire` / etc. parse and match exactly. Earlier attempts via `Name label` + `String(label)` failed because ZScript 4.5.0 lacks a `String(...)` constructor function (`zscript/PBPDA.zc`).
- **PDA preview: removed bogus `state.bIsLoop` check.** `State` has no `bIsLoop` field — `Loop` is a state-list terminator handled by the engine setting `NextState` to the label's first state, not a per-state flag. Walk termination already relies on the `steps < maxSteps` cap in `PDA_Play_AppendChain`'s `while` and the `nx == st` cycle guard, so the spurious flag check is gone (`zscript/PBPDA.zc`).
- **PDA: Monsters button now visible on the main menu.** The `objecttype == 1` Monsters button in `InitializeUiObjects` was being created with `dontdraw = true` and never re-shown — the only `dontdraw = false` flip lives in the `monsterFound` unhide loop, which matches `objecttype == 6` (per-monster catalogue tiles) rather than the main-menu button. Removing the initial `dontdraw = true` makes the Monsters button appear at PDA open alongside Weapons, matching the Weapons button's behavior (`zscript/PBPDA.zc`).

- **PDA: ZScript 4.5.0 parse fix — nested `class<>` inside `readonly<>` in blocky weapon icon resolver.** The 4.5.0 grammar rejects a **`readonly<>`** template whose type argument is a parameterized class pointer (the prior **`Inventory` / `Weapon` / `Actor`** staged-local form). **`ResolveBlockyWeaponIconTex`** now binds only **`readonly<Inventory>`** / **`readonly<Weapon>`** / **`readonly<Actor>`** defaults from **`GetDefaultByType((class<X>)(nm))`** (`zscript/PBPDA.zc`).

- **PDA preview pane / blocky icon resolve: ZScript 4.5.0 `class<Actor>readonly` conversion.** C-style casts such as **`(class<Actor>)(name)`** yield a **readonly** class pointer; assigning that to a mutable **`class<Actor>`** local or **`PDA_Play_AppendChain(class<Actor> cls, …)`** parameter fails to compile. **`PDA_Play_AppendChain`** now takes **`Name`** and calls **`GetDefaultByType((class<Actor>)(actorClass))`** into **`readonly<Actor>`**; preview builders follow the same **`GetDefaultByType((class<Actor>)(nm))` → `readonly<Actor>`** idiom (`zscript/PBPDA.zc`).

- **PDA blocky preview pane: ZScript 4.5.0 compile fixes.** Preview timeline fields stayed **play-scope** (built in **`WorldTick`**); **UiTick** no longer assigns **`pdaPrevNeedsBuild`** / pending keys directly — it debounces **`SendNetworkEvent("pdaprevq#"+key)`** into **`NetworkProcess`**, **`InitializeUiObjects`** clears via **`pdaprevclear`**, animation stepping moved from **`RenderOverlay`** to **`PDA_Play_AdvancePreviewAnim`** in **`WorldTick`**, and compile-time state walks on defaults use literal-label **`FindState(...)`** where applicable (`zscript/PBPDA.zc`).

- **Friendly Drone Familiar: spawns at hover height, lifts off the floor immediately.** The drone summon (kill-streak `FamiliarSummon` + `FamiliarSummonThrown` projectile in **`actors/Items/PBKillstreak/PB_UnlessYouHavePowah.dec`**) used to spawn the drone with its bottom flush against wherever the orb landed — and since `FamiliarSummonThrown` is a `+MISSILE` without `+NOGRAVITY`, the orb arcs down and almost always dies right at floor level. The Familiar then sat ground-bound until `+FLOAT`'s slow target-z chasing in `A_Chase` (capped to ~1–2 mu/tic for floating monsters) gradually pulled it up to the player's chest level — read as "drone spawns low on the floor and eventually rises up into the air." Two-part fix: **(1)** `FamiliarSummonThrown.SpawnCheck` now spawns the Familiar at z-offset **24** instead of **0**, lifting it one drone-height (`Height 24`) clear of the impact point so its bottom isn't sunk against the floor. **(2)** `Familiar.Spawn:` applies an explicit upward velocity pulse during the deploy animation: `A_ChangeVelocity(0, 0, 5, CVF_REPLACE)` after the first `HDRN A 5` frame, then `A_ChangeVelocity(0, 0, 0, CVF_REPLACE)` after the next two `HDRN A 4` frames, giving ~8 tics of velZ=5 lift = additional ~40 mu rise. Combined floor offset of **~64 mu** puts the drone at / slightly above player eye level the moment deploy ends; `+FLOAT` in the existing `Chase:` state then settles it gracefully to the player's z. velZ is explicitly halted before the hurtable hover frames so the drone doesn't keep climbing into low ceilings on tight maps. `CVF_REPLACE` is used (not the default add-mode) so the pulse doesn't compound with any residual velocity from the projectile chain.

- **Realm667 Frost Aura: guard `CurState` sprite-texture reads (UZDoom VM abort fix).** Hardens `FrostAuraPower.Tick` in `zscript/Items/Realm667Powerups/Realm667Powerups.zs` against `State.GetSpriteTexture` null-deref aborts by guarding `mon.CurState != null` before `GetSpriteTexture(0)` and only calling `A_SetSize` when the extracted texture dimensions are non-zero. This matches the earlier Fire Aura hardening pattern used to prevent `VM execution aborted: tried to read from address zero` from `State.GetSpriteTexture [Native]`.

### Documentation

- **Glory Kills (PB Staging `glorykills-PB_Staging` audit):** `Graphics/HUD/NewHud` (14 PNGs) matches **`GRAPHICS/HUD/glorykills/newhud/`** in this repo (same filenames; `fc /b` on `CRUCRY00.png` vs upstream showed no differences). Folded **`OptionMenu "Glorykills"`** rows use **`CVARINFO`** keys present in-tree (`PinataParticles`, `be_*`, `sv_*`, shoulder cooldown sliders, etc.). **`sv_fuelhud_background`** differs from Staging addon `menudef.addon`: there **`2` = Eternal**; here **`OptionValue "BackStyle"`** is four-wide (`0`–`3`), and **`3` = Eternal (NewHUD)`** is the strip that Staging drove at **`2`**. Old configs with **`sv_fuelhud_background 2`** expecting the Eternal row should set **`3`** (see **`MENUDEF.txt`** / **`language.enu`** hints under Glory HUD).

### Changed

- **M41A ammo balance:** reduced the per-rifle chamber/mag cap from **95** to **30** rounds and kept pickup reserve at **20** rounds of **`NewClip`**, bringing the pulse rifle's ammo economy back toward the Carbine/Fusil slot-4 baseline while keeping its underbarrel and dual-wield identity intact (`zscript/Weapons/Slot4/PB_M41A.zs`).

- **Fusil rifle ammo balance:** reduced the internal mag from **33** to **24** rounds and kept the initial reserve grant at **20** rounds of **`NewClip`**, pulling its total starting loadout closer to the Carbine baseline while keeping the Fusil's stronger damage profile in check (`actors/Weapons/Slot4/PB_Fusil.dec`, `language.enu`, `README.md`).

- **Kill streak — Realm667 reward power-ups:** Durations trimmed to align with the existing PB kill-streak pool (mostly ~20s equivalents); **Flight Sphere** is fixed at **12 seconds**. (`actors/Items/Realm667Powerups/Realm667Powerups.dec`, `zscript/Items/Realm667Powerups/Realm667Powerups.zs`)

- **Monster spawner rebalance (DiceMain `MidLevelMob` chains + aligned pack prefixes):** Retuned sequential `A_Jump` weights on nine vanilla pack spawners so Mid-tier play sees less “single default grunt” flooding and more scientists / helmet variants / Realm667 guests / alt demons per the spawn-ratio audit. **`ZombieManSpawner`:** `PB_ZombieMan` fall-through pack weight ~46% → ~20%, `MixedZombieManPack` / scientist chains / rifle-adjacent packs absorb the gap. **`ImpSpawner`:** DiceMain pack weights rescale toward ~18% normal imp branch and ~21% mixed-brown branch (marginal, after realm prefix); Realm667 prefix jumps retuned toward ~5% per guest line (~25% total guest attempts when gates allow). **`ShotgunGuySpawner`:** `PB_ShotgunGuy` ~45% → ~30%, `PB_ShotgunGuyHelmet` / `ASGGuy` / `PB_DemonTechZombie` / mixed pack raised per targets. **`ChaingunGuySpawner`:** `PB_Commando` ~60% → ~41%, `PB_HelmetCommando` / `PB_ClassicCommando` raised. **`PinkySpawner`:** `PB_Demon` ~49% → ~41%, Mech / Mean shares absorb freed mass ~59:41. **`CacodemonSpawner`:** normal caco token share reduced (~69% → ~50%) toward ~45% marginal `PB_Cacodemon` with FrostBrain/BabyCaco prefix unchanged. **`MancubusSpawner`:** normal manc token tuned toward ~38% marginal `PB_Mancubus` with Volcabus/Daedabus ratio preserved on the dice chain; Cryocubus/Behemoth prefix jumps untouched. **`LostSoulSpawner`:** Mid Phantasm jump weight raised; `LostSoulPackSpawner` Realm667 sequential jumps **8 → 20** each so guests surface far more often while `PB_LostSoul` stays ~30% marginal. **`BaronSpawner`:** Baron pack token mix shifted toward ~30% marginal `PB_Baron` with Belphegor/Infernus/CyberBaron ratios preserved on the dice chain; FrostBaron/Venor/SlayerKnight prefix jumps untouched.
- **`PB_ProjectSurvivalPassiveSpawner`:** `PickClass` thresholds rebalance passive hazard props (`TritePodCeiling` / `Pustule` / `TritePod` / `Mimic` / `Tentacle` / `Hangman`) toward ~16% / ~19.6% / ~20.4% / ~16% / ~15% / ~13% of successful rolls (outer early-out unchanged).

- **Project Survival fodder roster: uniform infighting flags + `BloodType` audit.** Aligned the PS fodder file headers (`actors/Monsters/T1-Grunts/ProjectSurvivalFodder/`) so every actor in the group (`Shambler`, `Puker`, `Blighter`, `Screamer`, `Burster`, `Spiker`, `CyberFodder`) carries the full Shambler-style `Species "Fodder"` infighting block. **`CyberFodder.dec`** was missing **`+THRUSPECIES`** + **`+NOINFIGHTING`** and had **no** `BloodType` line at all (it relies on `+NOBLOOD` for projectile-impact splatter, but its non-blood gore states still benefit from the explicit triplet for any future pass that drops `+NOBLOOD`); added `BloodType "Brutal_Blood", "BloodSPlatterReplacer", "SawBlood"` next to `Species "Fodder"`. **`Burster.dec`** + **`Blighter.dec`** were missing **`+DONTHURTSPECIES`** (they had `+DONTHARMCLASS` / `+DONTHARMSPECIES` / `+NOINFIGHTING` / `+THRUSPECIES`); added it next to the existing flag block. No `Death:` / `Death.Head:` / `Death.Burn:` / `Death.Ice:` / `XDeath:` content changed — the previous PS-fodder pass already routed every fodder corpse drop through PS-themed actors (own `Dead<Actor>*` pool plus `FodderHeadExplode`, `XDeathFodderArm/Leg`, `BurnedFodder*`, `BurningFodderZombie`, and per-actor frozen variants where authored). `Puker.dec` keeps its PS-themed alternate `BloodType "MuchBloodGreen", "BloodSPlatterReplacer", "SawBlood"` per maintainer intent. `zscript/Monsters/ProjectSurvival/Trite.zc` is intentionally untouched (bug-shaped, `BloodType "NashGoreBlood"`, no vanilla corpse drops).
- Project Survival fodder (`Shambler` / `Puker` / `Blighter` / `Screamer` / `Burster` / `Spiker`) is now the main roster fodder. The pass rate of the `FodderExtraPassiveSpawn` controller jumped from ~14% to ~62.5%, and the controller is now wired into the Imp / Shotgun / Chaingun / Pinky / Spectre passive chains in addition to Zombieman. Horrorspawn passive bursts (`HorrorPassiveSpawn`) at every spawner call site were halved (e.g. `TNT1 AAA` → `TNT1 AA`) to make room — the Horrorspawn pool itself is unchanged so existing `pb_horrorspawnchance` / `pb_horrorspawnmultiplier` / `nohorror` tuning still works.
- **PS fodder vs Horrorspawn passives (second pass):** `ZombieManSpawner` no longer double-fires `HorrorPassiveSpawn` (`TNT1 AA` → `TNT1 A` on every branch); `PB_ProjectSurvivalFodderPassive` tightens the early-out from `random(0,255) >= 160` to `>= 224` (**~62.5% → ~87.5%** spawn once the actor runs); the six tier‑1 pack spawners add **18** extra `FodderExtraPassiveSpawn` calls on selected branches (shape-preserving). `HorrorPassiveSpawn` / `PB_HorrorPassiveController` logic is untouched — cvar tuning behaves the same.
- **PS fodder variety rebalance:** `PB_ProjectSurvivalFodderPassive.PickClass` previously rolled `random(0, 255) >= 22` (≈**91.4% Shambler**, ~1.7% each for Puker / Blighter / Screamer / Burster / Spiker), so the supporting cast almost never appeared on screen. Now uses `random(0, 9) < 2` (**20% Shambler**, **16% each** for the other five classes via the existing 5-way switch). Shambler stays a recurring familiar face but the supporting cast (Puker, Blighter, Screamer, Burster, Spiker) now drives the visual variety of the PS roster.

- **PDA cursor: aspect-aware sizing for high-res `ZCURSOR.png` art.** The PDA mouse-cursor draw in **`PB_PDAWeaponContainer.RenderOverlay`** previously hard-coded `pdaCursorSize = 32.0 * pdaCursorScale` for both width and height, on the assumption that **`GRAPHICS/Weapon Wheel/ZCURSOR.png`** was 32×32 (the old × cursor). With the asset replaced by larger, taller-than-wide kukri art, drawing it as a forced square produced visible horizontal squish. The on-screen footprint is decoupled from the source PNG resolution (`DTA_DestWidthF` / `DTA_DestHeightF` pin screen pixels regardless of native texture size), but the dimensions now factor in the real aspect: `pdaCursorH = 32.0 * pdaCursorScale` (matches the previous on-screen height range, ~48 px at 720p → ~112 px at 4K) and `pdaCursorW = pdaCursorH * (cursorNative.X / cursorNative.Y)` from `TexMan.GetScaledSize` (assigned through a `Vector2` first per the ZScript 4.5.0 chained-`.X` rejection rule). `CURSOR.png` (the main-menu cursor) is engine-managed and isn't drawn from any PB code path, so no in-mod scaling change is needed for it (`zscript/PBPDA.zc`).

- **PDA (blocky mode): wider weapon icons inside tile buttons.** **`PB_PDAWeaponContainer.DrawBlockyTileIcon`** previously fit each icon's **largest** axis inside a single shared `box = min(slotH * 0.92, w - pad * 2)` budget, then scaled by `box / max(texSize.X, texSize.Y)`. With wider-than-tall sprites (most weapons — shotguns, rifles, energy weapons) that meant the largest axis was the icon's **width** and `box` was clamped by the vertical slot, leaving substantial unused horizontal slack inside each tile while wide weapons rendered at roughly half the height the tall **fist (Melee)** icon enjoyed. Replaced the shared-`box` math with a per-axis fit: vertical budget `maxH = slotH * 0.92` (unchanged — Melee height is preserved exactly because the fist sprite is height-bound, not width-bound), horizontal budget `maxW = w - pad * 2`, and `scale = min(maxW / texSize.X, maxH / texSize.Y)`. Wide weapon sprites now scale until their height reaches `maxH` (or the tile width is consumed first, whichever clamps), producing the visibly larger icon read the user requested without forcing the tall fist any taller (`zscript/PBPDA.zc`).

- **PDA (blocky mode): dossier column narrowed for preview band.** **`DTA_ClipRight`** / pane dim width shrink by **`PB_PDA_PREVIEW_WIDTH_768`** (scaled to resolution, clamped) + **`PB_PDA_PREVIEW_GAP_768`** gap so the dossier text ends before the new sprite preview column; **`PB_PDA_DESC_BREAK_WIDTH`** reduced **118 → 80** so **`SmallFont.BreakLines`** wraps for the narrower pane (`PB_PDAWeaponContainer.InitializePDATexts`, `zscript/PBPDA.zc`). Category **`>`** arrow X is capped so it stays inside the dossier clip.

- **PDA (blocky mode): centered catalogue + consolidated footer.** `zscript/PBPDA.zc` — the tile grid, description pane, page arrows, category arrows, dossier clip, and capstone icon share a single horizontal shift (`pdaBlockyXOffset` from `ApplyBlockyLayout`) so the grid+pane composition balances left/right margins against `Screen.GetWidth() - 32`. **BACK** and **EXIT** are one bottom-left `BACK / EXIT` widget (8 px hit padding per half; the ` / ` separator is drawn but not clickable); legacy list mode is unchanged when **`pb_pda_blocky`** is off.

- **PDA (blocky mode): page-down tip + category arrows.** `zscript/PBPDA.zc` — the enlarged up/down page arrows no longer hang past the dossier clip: **`SyncBlockyPageArrowHitboxes`** recomputes **`UpArrowPos`/`DownArrowPos`** so the down-arrow bottom aligns with **`descClipH`** (optional **`PB_PDA_BLOCKY_PAGE_ARROW_BOTTOM_PAD`**, default **0**), with the up-arrow shifted by the same Δy to keep spacing. **`<`/`>`** category arrows (Weapons ↔ Monsters stats) now match the **1.65×** visual scale (**`PB_PDA_BLOCKY_CAT_ARROW_VIS_MUL`**) and **8** px hit padding (**`PB_PDA_BLOCKY_CAT_ARROW_HIT_PAD`**), sit **12** px below **`descClipH`** (**`PB_PDA_BLOCKY_CAT_ARROW_BELOW_DESC`**), and share the same hover pulse path as the page arrows. Legacy PDA layout when **`pb_pda_blocky`** is off is unchanged.

- **Weapon-special wheel: aligned to equipment-wheel layout, gold tooltip removed.** `zscript/PbWheel/ev_drawer_special.zsc` `RenderOverlay`. Two parity changes so M41A / Hell Pistoler / Lever Action / M1 Plasma / Metal Sniper mode wheels read like the equipment wheel:
  - **Compact layout:** the `isEquipmentWheel` detection and `layoutSc = sc * (isEquipmentWheel ? 0.8375 : 1.0)` ternary is gone — `layoutSc = sc * 0.8375` unconditionally for the non-`weaponPickWheel` branch. Background, cursor, and orbit now render at the same compact size for both equipment wheels and weapon-special wheels. The Gearbox-driven weapon-pick branch (`if (weaponPickWheel && wpick_SlotNums.Size() > 0)`) keeps its own self-contained `sc * 1.0` layout because that branch has its own background / outer-orbit / inner-orbit math (`outerOrbit = int(128 * sc)`, `innerOrbit = int(76 * sc)`).
  - **Gold `$PB_WHEELTIP_*` line dropped:** the `bool equipmentWheelOnly = ...` detection block and the entire `if (!equipmentWheelOnly) { ... CR_GOLD ... }` text-draw block were removed. Each weapon-special slice's `specialAlias` (the white centered title — e.g. `"7.62 AP Magazine"`, `"Hellfire"`, `"Single"`, `".357 Tube"`, `"Underslung Grenade"`) already names the mode; the gold tooltip just re-prefixed it with the parent weapon's name (`"Metal Sniper: Standard magazines."`, `"Hell Pistoler: Hellfire rounds active."`, `"Lever Action: .357 tube."`, etc.) which read as redundant against the wheel's own branded center icon. The `PB_WHEELTIP_Select_*` / `PB_WHEELTIP_MS_Select_*` strings in `language.enu` are intentionally **kept** so the same data is still available if any future drawer wants to surface it.

- **Powerup screen-tint overlays: minimized across pickup-style powerups.** `Powerup.Color "RGB", alpha` paints a fullscreen tint of the given color and alpha for the duration of the powerup. Loud / wash-out values were the worst offenders for "the screen looks pink/red/gold the whole time the buff is up." Pickup-style overlays were swept down without changing color identity (the tint still hints which buff is active, just much fainter). Untouched on purpose: the **Hierophant** boss curses (`HieroWeaken` cyan / `HieroAmpDMG` red / `HieroSlow` yellow at `0.33`) — those are status-debuff feedback the player needs to clearly read; **`PowerNearDeath`** (`70,0,0,0.2`) — critical-HP warning; entries with no alpha (e.g. `Powerup.Color GoldMap` for `TimeSphere`, `ReflectorSphere`) — those use the colormap-mode path, not an alpha tint, so they are not screen washes.
  - **DragonSlayer god mode** (`actors/Weapons/MeleeWeaponPack/DragonSlayer.dec` `SlayerGodModePowerup`): `"FF4466", 0.2` -> `0.05`.
  - **Rage / Frightener** (`actors/Items/Powerups/SpecialPowerups.dec`): `RageDamageActivate` `Red 0.3` -> `0.05`, `RageSphere` `255,0,0, 0.125` -> `0.04`.
  - **Time-freeze sphere** (`actors/Items/PBKillstreak/PB_UnlessYouHavePowah.dec` `TFST`): `GoldMap, 0.12` -> `0.03`.
  - **Doom sphere / Haste sphere** (`SpecialPowerups.dec`): `255,0,255, 0.042` -> `0.012`, `"FF FF 00", 0.03` -> `0.012`.
  - **Realm667 wearables** (`actors/Items/Realm667Powerups/Realm667Powerups.dec`): `BootsOfTheNorth` `0.04` -> `0.012`; `BracersOfForce` / `FlightSphere` / `LichSkullDamageGiver` `0.06` -> `0.018`.
  - **Realm667 elemental aura spheres** (`zscript/Items/Realm667Powerups/Realm667Powerups.zs`): `FireAuraSphere` `"FF3000", 0.05` -> `0.015`; `FrostAuraSphere` `"4040FF", 0.05` -> `0.015`.
  - **Killstreak misc** (`actors/Items/PBKillstreak/PB_UnlessYouHavePowah.dec`): `CoffeeTime` `Gold, 0.03` -> `0.012`; `AmmoSphere` `"255 0 0" 0.015` -> `0.008`; `PB_GuardsphereST` `GreenMap, 0.015` -> `0.008`.
  - Already-conservative (≤ `0.012`) entries left as-is to avoid making them invisible: `Crucifix`, `LegendSphere`, `LifeshieldSphere` / `PowerLifeshield`, `PowerDeflect`, `ElectricAuraSphere`, `KSFreeze` / `PB_KSHasteSphere` killstreak haste, replacement `InvulnerabilitySphere_`.

- **Realm667 / Killstreak Auras: slightly wider radii, two-tier standard.** Aura `arad` values bumped so the fields reach further than the immediate adjacent monster ring without going back to room-spanning. Two distinct standards now:
  - **Single-target zappers (Fire + Electric): `arad = 160`** (was 120 each, +33%). `FireAuraPower` (`zscript/Items/Realm667Powerups/Realm667Powerups.zs`) and `ElectricAuraPower` (`zscript/Items/PBKillstreak/PB_UnlessYouHavePowah_Powerups.zs`) share this value because they share the same shape: 5-tic cadence, single random target inside `arad`, on-hit spark-burst + temporary light + sound. Both lights and the Electric warp-bubble ring scale off `arad` automatically (`A_AttachLight(... arad, arad ...)`, `AL.scale.X = (arad / 360) * 0.375`, `AL.warp(owner, arad, ...)`), so the visual bubble grows in lock-step with the damage radius.
  - **AoE slow / freeze (Frost): `arad = 448`** (was 384, +~17%). `FrostAuraPower` keeps its own larger standard because it's structurally different — every 10 tics it slows **every** monster inside `arad + 128` (BlockThings) and converts dead/dying monsters to freeze/ice corpses, so a smaller radius would defeat the room-control identity. The two `A_AttachLight` calls (`FRAL1`, `FRAL2`) and the `A_SpawnParticle` snowfall column scale off `arad`, so the visible blizzard area widens proportionally.
  - Both passes preserve every existing FX scaling factor (`mon.tics`, `mon.speed`, light attach radius, particle xy spread `random(-arad, arad)`, sprite-texture sizing for `FrostAuraFreeze` / `FireAuraVictim`) so no other tuning is needed.

- **Realm667 Fire Aura: match Electric Aura radius + FX cadence/layout.** `FireAuraPower` now uses a **5-tic cadence** (randomly selects one monster in radius each pass) to match the shipped Electric Aura behavior, while preserving Fire Aura’s identity: `FireAuraFire` damage actor, `FireAuraVictim` burned-corpse spawn, and `FireAura/firedeath` sound. The heavy ring-per-3-tics particle loop was replaced with ElectricAura-style per-tic aura particles plus on-hit “spark burst + temporary light + sound” pacing so the two auras feel like sister effects with different elements (electric vs fire). (Common radius value moved out to the unified entry above.)

- **Helmet visor: switched to `RenderUnderlay` + scale-aware insets.** Two follow-up fixes on the screen-pixel rewrite. **(1) Visor now layers BEHIND the SBARINFO bar.** **`zscript/PB_VisorOverlay.zc`** `PB_VisorHandler` now overrides **`RenderUnderlay`** instead of `RenderOverlay`. Engine render order in `D_Display` is *3D world → `RenderUnderlay` → SBARINFO HUD → `RenderOverlay`*, so the visor corners + glass + brace paint first and the SBARINFO ammo / health / armor readouts paint on top of them. PB Staging didn't need this distinction because its custom ZScript HUD draws the visor as part of itself; PB2022 still uses the engine-wrapped SBARINFO bar, so a visor in `RenderOverlay` covered the bottom-of-screen readouts — exactly the regression that prompted this fix. The dash effect in `PB_EventHandler` keeps `RenderOverlay` because it's meant to sit on top of everything (cyan tint over the whole frame). **(2) Insets now scale with `pb_visorscale`.** The earlier rewrite hardcoded the corner inset (24 px), glass inset (30 px), and center-brace inset (44 / 50 px) in virtual coordinates regardless of `pb_visorscale`. At the new default scale of `0.5` the center brace texture is half its native height (32 px in virtual coords) but the inset still anchored its top edge **44 px above** the screen, so the entire brace fell off-screen and "the visor center strips are not visible." `DrawVisor` now multiplies the four base insets (24, 30, 44, 50) by `visorScale` so a `0.5` scale also halves the off-screen distance — the brace keeps the same proportional reveal at any scale. The boot-in slide (`m32to0`) is also multiplied by `visorScale` so the visor doesn't snap in from a constant 64 px regardless of size.

- **Helmet visor: rewrote draw against actual screen pixels, default scale = 0.5.** The first iteration of the visor fold tried to use `Screen.DrawTexture` with `DTA_VirtualWidth, 320, DTA_VirtualHeight, 200, DTA_KeepRatio, true, DTA_DestWidth, …, DTA_DestHeight, …` together — that combination interacts oddly with widescreen scaling and made the visor pieces render at the wrong size with the corner content positioned far inside the screen instead of hanging off the edge. **`zscript/PB_VisorOverlay.zc`** now computes a single `hudScale = Screen.GetHeight() / 200` matching PB Staging's `BeginHUD(1.0, false, 320, 200)` math, multiplies position offsets and texture size by `hudScale`, and uses only `DTA_DestWidth/DestHeight` (no virtual-coord layer) so there's a single source of truth for scaling. Outer-edge anchoring is now expressed as "how far OUTSIDE the screen the matching texture edge sits" (`px = -ofsX * hudScale` for left, `px = scrW + ofsX * hudScale - dw` for right). Glass pieces hang **30 px** off the edge, dark/lit corner pieces hang **24 px** off — matching the staging `topOffsets` / `topOffsetsGlass` separation. The default **`pb_visorscale`** is now **`0.5`** so the visor reads as subtle corner brackets out of the box; PB Staging's authored default is `1.0` (full helmet-frame look where the corner pieces nearly meet in the middle of each screen edge), and the slider in **PB 2022 Enhanced → UI and Feel → Helmet Visor Options** now extends down to **`0.2`** for very subtle frames. **`pb_hudalpha`** also drops from `1.0` → `0.7` so visor glass doesn't read as opaque tint at default. Inline help text was added above the Visor Scale slider explaining what 0.5 vs 1.0 looks like.

- **Equipment wheel layout +25% bigger.** `zscript/PbWheel/ev_drawer_special.zsc` `RenderOverlay` — the equipment-wheel `layoutSc` multiplier (background, cursor, orbit) bumped from **`0.67` → `0.8375`** (×1.25). Per-icon scale (`sc` × authored `iconScale`) and the weapon-special / weapon-pick wheels are unchanged, so M41A / Hell Pistoler / Lever Action mode wheels and the Gearbox-style weapon picker keep their original sizing.

### Added

- **Three more possession finishers — Cyberdemon + Mancubus family + Revenant chase ride.** **`CyberdemonPlayer`** + **`CyberdemonTimer`** (`actors/Monsters/Tz-Bosses/CyberdemonPlayerWeapon.dec`, **`CYBF`** arm art, **`CyberBallsPlayer`** salvos like **`PB_CyberdemonRL`**) grants at the end of **`FatalityCyberdemon`** in **`actors/Player/NEWPLAYE.dec`** after the existing camera-stop row. **`MancubusPlayer`** + **`MancubusTimer`** (`actors/Monsters/T3-Fats/MancubusPlayerWeapon.dec`, **`FUM*`** / **`FUMY`** cannon strips, alternating **`BigFireBall`**) grants after **`FatalityFatso`** (**`PLAYER.dec`**) plus **`FatalityFatso2`**, **`FatalityVolcabus`**, **`FatalityVolcabus2`**, **`FatalityOtherFatso`**, and **`FatalityOtherFatso2`** in **`NEWPLAYE.dec`**. **`RevenantRider`** player-state block (**`MHML`** eight-angle sprites copied from BD22 **`RevenantLauncher`** → **`SPRITES/MONSTERS/Revenant/RevenantRider/`**, prefix unique in-tree) lives in **`NEWPLAYE.dec`** after **`ArachnoGunFire`** with tokens **`RevenantRider`** / **`RevenantRiderFiring`** / **`RevenantLauncher`**; **`FatalityRevenant`** / **`FatalityRevenant2`** (**`PLAYER.dec`**) deliberately **omit** tail **`ACS_ExecuteAlways(171,…)`** so **`CameraRunner`** chase cam survives into the ride, and **`ACS_ExecuteAlways(171,…)`** runs only on **`RevenantRider`** dismount paths. Rockets use **`RevenantBalls`**; **`ArachnotronTurretFiring`** is still never granted anywhere, so this ride auto-fires on a ~**25**-tic **`RevenantRiderVolley`** cadence instead of mirroring the dormant Arachno firing hook. **`DECORATE`** adds **`#include`** lines after **`MastermindPlayerWeapon.dec`** and **`MANCUBUS.dec`**; **`Pain`** / **`PainWfist`** in **`PLAYER.dec`** branch **`RevenantRiderPain`**.
- **Spider Mastermind possession weapon (`MastermindPlayer`).** After the third-person **`FatalitySpiderMastermind`** finisher in **`actors/Player/NEWPLAYE.dec`**, the player receives **`MastermindPlayer`** and auto-selects it — same grant pattern as **`CacoPlayer`** at the end of the caco fatality chain in **`PLAYER.dec`**. New **`actors/Monsters/Tz-Bosses/MastermindPlayerWeapon.dec`**: **`MastermindTimer`** ammo (**200** tics), **`MastermindPlayer`** **`PB_Weapon`** grounded mount (**no** **`PROP_FLY`**), invulnerable, **`RMNG`** chaingun view sprites reused from **`MastermindChaingun`**, fire uses **`CHGNSHOT`/`FARMGN`**, **`YellowFlareSpawn`**, **`GunFireSmoke`**, **`A_MastermindChaingunShake`**, and **`A_FireBullets`** (~**5–7** damage; **`MastermindTracerDoomguy`** kept out because its explosive payload is boss-tier). **`DECORATE`** `#include` sits immediately after **`CacoPlayerWeapon.dec`**. README **`### Possession finishers`** summarizes all three possession rides for players — the new **`MastermindPlayer`** alongside the existing **`CacoPlayer`** flying mount and the pre-existing Arachnotron drivable plasma turret (**`ArachnotronTurret`** + **`ArachnoGun`** → **`ArachnoGunner`** first-person states in **`NEWPLAYE.dec`** ~7486).

- **PDA: Equipment codex + pickup unlocks.** New root lumps **`PDAEQUIP`** (comma-separated headers) and **`PDAEQUIPT`** (`#` blocks of **`Header$dossier$class bundle$stats`**, same shape as **`PDAWEAPT`**). **`PDAEQUIPT`** is nine characters long — load it with **`Wads.CheckNumForFullName("PDAEQUIPT")`** so it cannot collide with the eight-character **`PDAEQUIP`** names lump under short-name lookup (same pattern as **`PDAMONST` / `PDAMONSTT`**). `PB_PDAWeaponContainer` registers unlock tiers with **`Name`-then-`(class<Inventory>)(nm)`** into **`Array<Class<Inventory> > PDA_CodexEquipClasses`** (interior space required on ZScript **4.5.0**); A throttled inventory sweep inside **`PB_EventHandler.WorldTick`** (every 7 tics, piggybacking the existing `PlayerPawn` `ThinkerIterator` loop) walks each player's `a.Inv` → `.Next` chain and calls **`NotifyEquipmentInventoryGranted`** per item — the notify is idempotent via the `PDAEquipFound[sl]` guard so re-walks don't re-fire the unlock SFX. Unlocks emit **`FoundEquip:<slot>`** for **`UiTick`** / **`PDAOPEN`** the same way weapon pickup lines do. **Why polling, not a `GiveInventory` override:** ZScript 4.5.0 does **not** expose `PlayerPawn.GiveInventory` as a virtual method — an attempted `extend class PlayerPawnBase { override bool GiveInventory(class<Inventory>, int amount = 0) }` fails with both `Default values for parameter of virtual override not allowed` and `Attempt to override non-existent virtual function GiveInventory`. Future auto-unlock surfaces should follow the same WorldTick-poll pattern. Catalogue **`objecttype == 7`**, preview **`E:`** keys walk **`Spawn` / `Pickup` / `Use`** via **`PDA_Play_BuildEquipment`**. Watchdog: **`[PDA] WARNING: equipment codex empty after init`** when the bundle lump fails.

- **PDA (blocky mode): main-menu category mega-tiles + looping backdrop animations.** When **`pb_pda_blocky`** is on, **`StampBlockyMainMenuTiles`** lays out **Monsters / Weapons / Equipment** (**`objecttype` 1 / 2 / 14**) across the top; each draws **`DrawBlockyTile`** chrome, a clipped **`DrawClipFittedTexture`** backdrop (weapon **`Fire`** chains across the whole **`pdaWeapTextArray`**, **`Doomer`** **`FatalitySpiderMastermind`** on Monsters — sourced from **`actors/Player/NEWPLAYE.dec`** — **`BPv10_BurnedHusk`** **`Burn1`→`Death`** embers on Equipment — Brutal Pack V10 ash corpse, not a literal **`XDeath`** chain), Equipment adds **`DrawPDAEquipmentWheelRing`** (**`PDA_EquipRingTex`**, matching **`specialWheel_Equipment`** icon lookups), and **`BigFont`** captions over a dim band. **`NetworkProcess`** **`openequipmentmenu`** / **`pdaBlockyEquipPage`** mirror weapon + monster menus; **`pdaMainAnimDirty`** rebuilds play-scope timelines when the PDA opens (**`PDA_Main_*`** helpers in **`zscript/PBPDA.zc`**).

- **PDA (blocky mode): right-column sprite-action preview.** When **`pb_pda_blocky`** is on, a preview pane beside the dossier plate loops weapon actor frames in order **`Select` → `Ready` (≤10 frames) → `Fire` → `AltFire` → `Reload` → `Deselect`**, or monster frames **`Spawn` → `See` → `Missile` → `Melee` → `Pain` → `Death`**, walking each canonical state's `NextState` chain up to **16** frames per segment and **64** total, with per-frame tic duration **`max(5, state.tics)`**. Missing states are skipped; empty cycles fall back to the Blocky tile icon resolve (`zscript/PBPDA.zc`).

- **Cat's Frozen Addon — weapons (Slice 5).** Four rolled weapons with **no** `pb_No*` spawn toggles and **no** new **`MENUDEF`** rows (standing directive): **`PB_CryoElectroRifle`** (slot **8**, merged cryo orb + Stormcast **`LightningMissile2`** electric fire; **`PB_CryoCells`** + **`PB_Cell`** reload; **`specialWheel_PB_CryoElectroRifle`** / **`Select_PB_CryoElectro_*`**), **`PB_CF_RocketLauncher`** (slot **6**, **`PB_CF_CryoRocket`** cryo-slow blast), **`PB_CryoCannon`** (slot **9**, **`PBCF_CryoWind`** + **`PB_CryoCannonCells`**), **`PB_CryoASG`** (slot **3**, **`PBCF_CryoPellet`** + **`PB_Shell`**). Ammo pickups **`PB_CryoCellsPickup`** / **`PB_CryoCellsPack`** / **`PB_CryoCannonCellsPickup`** in **`actors/Items/Ammo/CatsFrozenAmmo.dec`**. ZScript **`zscript/Weapons/CatsFrozen/PB_CatsFrozenWeapons.zc`**. Editor numbers **`23212`–`23218`** in **`zmapinfo.txt`**.

- **Cat's Frozen Addon — equipment wheel (Slice 4).** Eight always-on equipment-wheel slices in **`zscript/PbWheel/ev_core_special.zsc`** (`specialWheel_Equipment`) plus **`actors/Weapons/BaseWeapon.dec`** hooks: **Freezenade** (`WW_FreezenadeSelected` / `FreezenadeAmmo`), **Snow Caster**, **Freezebot** deploy, **ice wall** generator, **holo decoy**, **Tesla** / **flame** turrets, **freeze mines**. Consolidated DECORATE **`actors/Items/CatsFrozen/Equipment.dec`**; ZScript **`zscript/Items/CatsFrozen/PBCF_FriendlyDeployableBase.zc`** + **`PBCF_CatsFrozenEquipment.zc`** (friendly-deployable damage filter + mine/turret/decoy/ice-wall logic). **`SBARINFO.txt`** `InInventory` rows per selection token + ammo; **`language.enu`** `$PB_PICKUP_PB_CF_*` pickup strings only (no **`OPTMNU_*`** / no per-piece toggles). Map pickups **`23205`–`23211`** in **`zmapinfo.txt`**. **`PB_Backpack`** has a ~20% bonus branch that grants **one** random Cat's Frozen ammo charge. Wheel slice icons currently all resolve to **`graphics/pywheel/grenade_cryo.png`** (shared placeholder until dedicated **`Equip_*.png`** art).

- **LeverAction weapon-special wheel icons (TomiikiPro).** The `LeverAction` mode wheel (`zscript/PbWheel/ev_core_special.zsc` `specialWheel_LeverAction()`) now uses dedicated authored icons for its two caliber slots: **`graphics/pywheel/Magnum.png`** for `Select_LeverAction_357` (.357 Revolver rounds) and **`graphics/pywheel/Marlin.png`** for `Select_LeverAction_Marlin` (.444 Marlin rounds). Both icons by **TomiikiPro** — added to **`CREDITS.txt`** under the LeverAction line and the Special-thanks list, and to the "Other shipped weapon-special wheels" bullet in **`AGENTS.md`** so future agents know who to credit when touching that wheel art. No code or behavior change here; the `TexMan.CheckForTexture("graphics/pywheel/Magnum.png", ...)` / `Marlin.png` lookups in the existing dispatcher already drove this — the credit pass just documents the authorship.

- **Helmet visor: dedicated bottom-middle strip (`HUDMLDOF`).** New 346×128 asset **`GRAPHICS/HUD/FULLSCRN/VISOR/Off/HUDMLDOF.png`** wired as the bottom-anchored center-brace helmet piece, replacing the previous `HUDMDARK` + `HUDMIDOF` pair that was reused (vertically flipped) on the bottom branch. **`zscript/PB_VisorOverlay.zc`** `DrawVisor` PASS 3b now branches on `pb_visormiddlepartbottom`: TOP (`middlePart == 0` "Top" or `2` "Both" → top draw) keeps PB Staging's authored `HUDMDARK` dark-underlay + `HUDMIDOF` lit-shell composite, while BOTTOM (`middlePart == 1` "Bottom" or `2` "Both" → bottom draw) uses **`HUDMLDOF` for both** the dark-underlay and sector-lit-overlay slots — same lump fills both passes per spec ("same graphic for 'dark' and 'of'"), preserving the same two-pass full-alpha-base + sector-light-alpha-overlay lighting gain the top strip already had without authoring a separate dark variant. `middlePart == 3` "Off" skips the entire branch so neither the original pair nor `HUDMLDOF` ever draws. Width (346) matches the existing top-branch pieces so `cInsetDark` and the scale-aware inset math work unchanged; the extra height (128 vs 64) gives the bottom strip a deeper V-curve. `VA_FLIPY` is preserved on the bottom draws because `HUDMLDOF`, like `HUDMIDOF`, is authored with the dark side at the TOP of the source image — flipping vertically orients that dark side toward the screen's bottom edge.

- **Optional gas-mask layer (2nd visor) with inertial yaw/pitch sway.** New `pb_showgasmask` cvar (default `false`) opt-in fullscreen overlay that paints **`GRAPHICS/HUD/FULLSCRN/gasmask/pb_gasmask.png`** between the visor's glass and the helmet shell — i.e. the helmet corner pieces still frame on top of it, but it sits in front of the visor glass tint. **`zscript/PB_VisorOverlay.zc`** `DrawVisor` was restructured into three z-ordered passes (glass → gasmask → helmet) to give the new layer a clean stacking slot; before this change each per-side iteration drew that side's glass and helmet shell back-to-back so we couldn't interleave a fullscreen layer at the right depth. The mask itself is drawn by `DrawGasmask()` via `Screen.DrawTexture` at the full screen size, with `DTA_CenterOffset, true` so the inertial sway shifts the geometric center. Alpha tracks `m0to1Float` (the same boot-in counter the visor corners / brace use) so the mask fades in alongside the rest of the visor on level start. **Inertial sway / "dynamic tilting":** new `UpdateGasmaskTilt()` ticked from `WorldTick` reads `players[consoleplayer].mo.angle` / `pitch` each tic, takes a signed `Actor.DeltaAngle` against the previous tic, multiplies by `1.6` (yaw) / `1.4` (pitch), clamps to ±10 / ±8 virtual pixels, and runs the result through a `0.85 / 0.15` low-pass filter into `tiltX` / `tiltY`. `DrawGasmask` then converts those virtual pixels through `hudScale = scrH / 200.0` (matching the rest of this overlay) and offsets the center of the texture, so the mask drifts opposite to fast view motion and springs back when the player stops. The smoother is intentionally slow / capped so quick mouse swings don't pop the mask edges off-screen and reveal letterbox seams. Wired up: cvar declaration in **`CVARINFO`** in the visor block, "Show Gas-Mask Overlay" toggle plus two inline help lines added to **`OptionMenu "PB_HelmetVisorOptions"`** in **`MENUDEF.txt`**, master-bailout check in `RenderUnderlay` extended so the gas-mask still draws when only `pb_showgasmask` is true (the helmet shell + glass + brace can all be off and the mask still works).

- **Helmet visor overlay folded from PB Staging (PBv0.3+).** New ZScript class **`PB_VisorHandler`** in **`zscript/PB_VisorOverlay.zc`** that **extends `EventHandler`** and paints the visor via **`RenderOverlay`** on top of the existing **`SBARINFO.txt`** HUD (same pattern PB2022 already uses for the dash overlay in `PB_EventHandler`). The original fold tried to subclass `SBarInfoWrapper` directly; that crashes at startup with `VM execution aborted: tried to read from address zero` from `SBarInfoWrapper.AttachToPlayer line 31` because the engine only auto-populates the wrapper's internal `script` field for the default wrapper that gets created when SBARINFO is parsed, not for hand-written subclasses. The `RenderOverlay` approach sidesteps that entirely. The draw math is the staging routine (**`PB_Hud.zs`** lines ~1105–1181) re-expressed against `Screen.DrawTexture` with a 320x200 virtual coordinate space and a small `DrawVisorPiece` helper that handles edge anchoring + horizontal/vertical mirror via `DTA_FlipX` / `DTA_FlipY`. What was lifted:
  - **Four corners (top/bottom × left/right):** layered `HUDTDARK` / `HUDBDARK` darkness underlays, `HUDTOPOF` / `HUDBOTOF` lit shells modulated by `cursector.colormap.lightcolor` + `lightlevel/255`, additive `HUDTFLAR` / `HUDBFLAR` lens flares (brighter the darker the room), additive `HUDTOP` / `HUDBOTOM` hologram beams, and the `HUDTOP2` / `HUDBOTO2` (lit) + `HUDTPOF2` / `HUDBTOF2` (off) visor-glass pair that crossfades on the `m0to1Float` boot-in counter. Right side reuses the same lumps with `DI_MIRROR` to flip horizontally.
  - **Boot-in animation:** `m0to1Float` ramps 0 → 1 (×0.1 per tic) and `m32to0` slides 64 → 0 (×4 per tic) so corners "drop in" from outside the screen on level start / after revival.
  - **Central helmet brace:** `HUDMDARK` + `HUDMIDOF` + `HUDMIDD2` / `HUDMIOF2` strip, drawn at top, bottom, both, or hidden via the new `pb_visormiddlepartbottom` cvar (`OptionValue "VisorMiddlePart"` extended with a `3, "\cgOff"` row).
  - **Powerup tints:** the `NIGHTVIS` and `RADSUIT` corner overlays still trigger, but the inventory checks were rewritten from staging's `PB_PowerLightAmp` / `PB_PowerIronFeet` to the engine-standard **`PowerLightAmp`** / **`PowerIronFeet`** that PB2022's `NightVision` / `NewRadSuit` derive from (`actors/Items/Powerups/GenericPowerups.dec`, `DECORATE`).
  - **Wired up:** `AddEventHandlers = "PB_VisorHandler"` added to the **`GameInfo`** block in **`zmapinfo.txt`** (no `StatusBarClass` change — the engine's auto-wrapped SBARINFO bar is left intact); `#include "zscript/PB_VisorOverlay.zc"` in **`ZSCRIPT.zc`** right after **`PBPDA.zc`**.
  - **CVars in `CVARINFO`** (right after `pb_dasheffect`): **`pb_showhudvisor`** (helmet shell), **`pb_showhudvisorglass`** (visor glass tint), **`pb_hudalpha`** (glass opacity, default 1.0), **`pb_visorscale`** (uniform scale, default 1.0), **`pb_visorofsx`** / **`pb_visorofsy`** (edge offset; X is used as a shared offset, Y reserved), **`pb_visormiddlepartbottom`** (0 Top / 1 Bottom / 2 Both / 3 Off).
  - **Menu (`MENUDEF.txt`):** new **PB 2022 Enhanced → UI and Feel → Helmet Visor Options** entry (top-level toggle `pb_showhudvisor` is also exposed inline next to it), opening **`OptionMenu "PB_HelmetVisorOptions"`** with all six sliders / toggles plus inline help text. The existing **`OptionValue "VisorMiddlePart"`** got the new `3, "\cgOff"` row so "hide entirely" is selectable.
  - **Assets copied from PB Staging tree** into **`GRAPHICS/HUD/FULLSCRN/VISOR/`** (preserving the staging subfolder layout): `HUDTOP.png`, `HUDBOTOM.png`, `HUDTOP2.png`, `HUDBOTO2.png`, `HUDMIDD2.png`, plus `Dark/HUDTDARK.png` `HUDBDARK.png` `HUDMDARK.png` `HUDTFLAR.png` `HUDBFLAR.png` and `Off/HUDTOPOF.png` `HUDBOTOF.png` `HUDMIDOF.png` `HUDTPOF2.png` `HUDBTOF2.png` `HUDMIOF2.png`. `NIGHTVIS.png` / `RADSUIT.png` were already present from earlier PB Staging work and are reused as-is.
  - **What was deliberately dropped from the staging port (lite fold):** SetSway-driven **HUD parallax** (movement-induced visor jitter — depends on staging's `PB_DoomStatusBar` interpolators), the **kernel-panic / `visorOff`** death sequence (`helmetKernelPanic`, `S_StartSound("visor/dyingvisor")`, BIOS terminal reboot fallback — depends on additional staging UI assets and KernelPanicMessages array), the **`pb_huddynamics` / `LensDirt.png` / glass-cracks** layer, and the staging-specific **PB powerup classes** (replaced with vanilla `PowerLightAmp` / `PowerIronFeet` — see above). These can be folded later as separate passes; the visible visor (corners + glass + center brace + sector lighting + boot animation + powerup tints) is fully working on the existing PB2022 SBARINFO HUD.

- **Simpler Monster Settings menu** (new `OptionMenu "SimplerMonsterSettings"` in **`MENUDEF.txt`**, sibling above the renamed Classic menu). One screen condensing the most common monster-roster decisions into plain-English groups so casual players don't have to scroll a 60-row per-creature list:
  - **Master Switches** — `pb_classicmonsters`, `pb_disablenewenemies`, `pb_disabledecorations`, `pb_disablemapenhancements`. Each carries a one-line "what this actually does" caption.
  - **One-Click Bundles (`Command "..."` rows with `;`-chained `set` calls)** — single buttons that flip a whole family at once. GZDoom's `AddCommandString` honors semicolons inside `Command` strings, so each row is an atomic preset:
    - **Realm667 monsters** (Blot / Possessed Gibs / Vulgar / FusionCyberImp / Behemoth / BabyCaco / Drone / Venor / SlayerKnight) — Enable / Disable all.
    - **Monster Pack add-ons** (Crackodemon / Hellduke / Hell Smith / Director / Aracnorb Queen / Maledict / D3 Hell Guardian / Marauder / Hellduke / Helemental / Hierophant / Pyrodemon / Dark Disciple / Dark Lord / Obsidian Ravager / Rahovart / ESoul / Rifle Commando / Satyr / Cybersatyr / Terminator / Aracnorb / Afrit / QSG Dreadtrooper / Ice Demon / Overlord) — Enable / Disable all.
    - **Zombie tank reinforcements** (`NoZombieTank` / `NoZombieplasmatank` / `NoZombiemissiletank` / `NoZombieFlyer` / `NoZombietankelite`) — Enable / Disable all.
    - **Reinforcement / Horrorspawn fodder pool** (`NoHorror` / `NoFresh` / `NoHunger` / `NoMeleeZombie` / `NoMeleeSergeant` / `NoCyberFodder` / `NoUnwilling` / `pb_NoProjectSurvivalPassive`) — Enable / Disable all.
    - **Boss-tier summons** (`NoAracnorbQueen` / `NoD3HellGuardian` / `NoD3Maledict` / `NoDirector` / `NoHellduke` / `NoHellSmith` / `NoObsRavager` / `NoRahovart`) — Enable / Disable all.
  - **Quick Family Toggles** — the high-leverage `pb_No*` variant blocks (brown / savage / dark imps; blood / mech demons; void spectre; inferno caco / afrit / suffering elemental / overlord; beam / draugr revenants; inferno / elite / arachnophyte arachnotrons; ice vile / flesh wizard) without scrolling past every per-creature row.
  - **Reinforcement Tuning** — the four most-touched horror sliders (`PB_HorrorspawnChance`, `HorrorMapMultiplier`, `PB_HorrorspawnMultiplier`, `HungerMultiplier`) inline so a player doesn't have to drill into a sub-menu just to dial fodder back.
  - **Drill-Down Submenus** — explicit links to **Horrorspawn settings (full)**, **Summon limit settings (full)**, and **Classic Monster Pack Menu (per-creature)** for anyone who needs the full surface.
  - The full per-creature menu (now titled **`Classic Monster Pack Menu`**, internal id unchanged at `MonsterPackSettings`) and its `PB_HorrorspawnSettings` / `SummonsSettings` sub-menus are untouched aside from the title rename — every cvar is still individually addressable. The parent settings list now shows **Simpler Monster Settings** above **Classic Monster Pack Menu (per-creature)**.

- **Monster Pack Hunger / CrackoDemon Glory Kill wiring (orphan fatality assets reclaimed).** The multi-pass fatality audit catalogued four first-person Eld strips that were authored, lump-shipped, and dispatcher-wired — but never granted by any monster (`HUF1` / `HUF2` in **`SPRITES/MONSTERS/fatalitys/eld_eld_caco/`**, `HUF3` and `HEDF` in **`eld_eld_comando/`**). Their original token names (`CacoDemonFatality3` / `4`, `ComandoFatality3` / `4`) misrepresented the actual sprite content (a bald grey humanoid Hunger or a black-armored Crackodemon, not a caco/commando). New plumbing wires them properly:
  - **Player side (`actors/Player/NEWPLAYE.dec`):** four new tokens (`HungerFatality1` / `2` / `3`, `CrackodemonFatality1`), four new dispatcher entries in the **`FatalityChecker`** block, and four new player Eld animation states (**`FatalityHunger1Eld`** / **`2Eld`** / **`3Eld`**, **`FatalityCrackodemon1Eld`**) that reuse the **`HUF1`** / **`HUF2`** / **`HUF3`** / **`HEDF`** sprite strips. Sounds switched to **`Hunger/Pain`** + **`Hunger/Death`** (Hunger family) and **`Cracko/Pain`** + **`Caco/Death`** (Crackodemon) so the audio cue matches the visible victim. The inappropriate **`DeadCacoDemon`** / **`DyingComando`** / **`DeadChaingunGuy_Half`** corpse spawns from the original `FatalityCacoDemon3Eld` / `4Eld` / `FatalityComando3Eld` / `4Eld` states are not carried over — the live Hunger / Crackodemon's own Death state plays after the player animation, no extra mismatched corpse prop drops.
  - **Hunger GK (`actors/Monsters/T1-Grunts/GloryKills.dec` tail):** `HungerGK : Hunger Replaces Hunger` with `WoundHealth 8`, `ZombieHighlight`, and `ZHUN H` stagger frames. `Death.Crucible` routes to **`Super::Death`** (Hunger has no `Death.cut`), `Death.GloryFire` to `Super::Death.Flames`, `Death.BloodPunch` to `Super::Death.SuperPunch` (Hunger has no `Death.ExtremePunches`). `Death.Fatality` rolls 1/3 each across **`HungerFatality1`** / **`2`** / **`3`**.
  - **CrackoDemon GK (`actors/Monsters/T2-Pinkies/GloryKills.dec` tail):** `CrackoDemonGK : CrackoDemon Replaces CrackoDemon` with `WoundHealth 100`, `CacoHighlight`, and `HED9 E` stagger frames. Standard `Super::Death.cut` / `Super::Death.Flames` / `Super::Death.ExtremePunches` routes (Crackodemon defines all three). `Death.Fatality` always grants **`CrackodemonFatality1`** (only one Eld variant exists for now; the `Death.Fatality` block keeps the standard pinata + `GoFatality` plumbing so a future second-strip add is a one-line change).
  - **No `DoomEdNum` / spawner changes needed:** both wrappers use `Replaces`, so all existing spawn paths (`HungerHorde`, `Horrorspawn` drop, `PB_HorrorPassiveController`, `CacodemonSpawner` Crackodemon branches) automatically substitute the GK actor.
  - The pre-existing orphan tokens `CacoDemonFatality3` / `4` and `ComandoFatality3` / `4` (and their `FatalityCacoDemon3Eld` / `4Eld` / `FatalityComando3Eld` / `4Eld` player states) are intentionally left in place but remain dead code — see **`actors/Monsters/Realm667/ASSET_NOTES.txt`** "Wiring (Hunger / Crackodemon — DONE)" for the full token / state map.

- **Weapon wheel: release-to-confirm by default.** **`gb_select_on_key_up`** in **`CVARINFO`** now defaults to **`true`**, so releasing the weapon-wheel keybind closes the wheel and selects whatever is hovered—mirroring how **`-PBEquipSpecial`** behaves for the equipment wheel (the existing **`-PBWeaponWheel`** alias already fires **`gb_toggle_weapon_menu_up`**, which Gearbox honors when this cvar is on). Players can still toggle it off via **PB 2022 Enhanced → Gameplay Settings / UI and Feel → Weapon & Inventory Selector → Select on key release**.

- **Venor (Realm667 / Kan3)** as optional baron-pack wildlife: ZScript **`PB_Venor`** + Glory Kill wrapper **`PB_VenorGK`** (`Realm667_GloryKills.dec`), **`SNDINFO`** **`venor/*`** → **`sounds/monsters/Venor/`**, editor number **`23188`**, spawn **`BaronPackSpawner`** branch **`A_Jump(13, "SpawnRealm667Venor")`** (~5% before Slayer Knight), gated by **`pb_disablenewenemies`** and **`NoVenor`** (menu under Baron variants).

- **Realm667 monster pack — spawn gating + per-pack toggles.** All 5 spawners that branch into Realm667 monsters (**`ImpSpawner.dec`** for `PossessedGibs` / `Blot` / `Vulgar` / `FusionCyberImp`, **`CacodemonSpawner.dec`** for `BabyCaco`, **`MancubusSpawner.dec`** for `Behemoth`, **`LostSoulSpawner.dec`** for `Drone`, **`BaronSpawner.dec`** for Venor / `SlayerKnight`) now honor **`pb_disablenewenemies`** at the top of the Realm667 jump block (skips to a new **`Realm667Skip:`** label that resumes the vanilla token-dispatch flow), and each **`SpawnRealm667*`** label re-checks its own per-monster cvar so disabling a single creature falls through cleanly. New server cvars in **`CVARINFO`** (next to **`NoCrackodemon`** / **`NoESoul`**): **`NoBabyCaco`**, **`NoBlot`**, **`NoDrone`**, **`NoFusionCyberImp`**, **`NoPossessedGibs`**, **`NoSlayerKnight`**, **`NoVenor`**, **`NoVulgar`**. The previously-orphan **`NoBehemoth`** cvar (declared but unused) is now wired by `MancubusSpawner` for the Realm667 Behemoth. Menu rows added to **`MonsterPackSettings`** (`MENUDEF.txt`) under the matching family headers (Cacodemon variants, Lost soul variants, Mancubus variants, Baron of Hell variants, plus a new **"Imp variants (Realm667)"** section). Brings Realm667 into parity with how every other folded monster pack (Crackodemon / ESoul / Project Survival fodder + passive / Hellduke / Director / etc.) is exposed and gated.

- **Void Grenade equipment-wheel slice** wired in (**`actors/Weapons/BaseWeapon.dec`**, **`zscript/PbWheel/ev_core_special.zsc`**, **`zscript/PbWheel/ev_drawer_special.zsc`**): new tokens **`VoidGrenadeSelected`** / **`WW_VoidGrenadeSelected`**, **`UseEquipment`** dispatch jump to **`ThrowVoid`**, mutually-exclusive clears in every other equipment-select branch, and **`SwitchEquipment`** "Already using" / "No void grenades remaining" / "Equipment: Void Grenade" messaging mirroring the Shield Grenade pattern. Wheel slice icon reuses the existing **`VGR1A0`** pickup sprite (no new art needed); usability gate reads **`VoidGrenadeAmmo`**, icon size multiplier matches the other grenade slices (1.5x). The existing **`ThrownVoidGrenade`** (BlackHole singularity + purple shockwave + radius quake) is the actual payload.

- **Project Survival passive hazards** (PS **Tz-Passive**, by **The Pope of Dope** / ThePopeOfDope): **Pustule**, **Mimic**, **Tentacle**, **TritePod** / **TritePodCeiling**, **Hangman**, plus **Trite** (ZScript under **`zscript/Monsters/ProjectSurvival/Trite.zc`**). No **`Replaces`**; sprites/sounds under **`SPRITES/MONSTERS/ProjectSurvivalPassive/`** and **`sounds/monsters/ProjectSurvivalPassive/`**. Extra chance alongside **Fodder** via **`ProjectSurvivalPassiveExtraSpawn`** → **`PB_ProjectSurvivalPassiveSpawner`** on **`ZombieManSpawner`** / **`ShotgunGuySpawner`** ( **`pb_disablenewenemies`**, **`pb_NoProjectSurvivalPassive`** ). Frozen variants in **`ProjectSurvivalPassiveFreeze.dec`**. Editor numbers **`23175–23187`**. See **`CREDITS.txt`**.

- **Project Survival fodder zombies** (by **The Pope of Dope** / ThePopeOfDope) folded as optional extra Tier‑1 content (DECORATE under **`actors/Monsters/T1-Grunts/ProjectSurvivalFodder/`**, no **`Replaces`**): Shambler, Burster, Blighter, Screamer, Spiker, Puker, CyberFodder; passive reinforcement from **`ZombieManPackSpawner`** via **`FodderExtraPassiveSpawn`** + **`PB_ProjectSurvivalFodderPassive`**. **`pb_disablenewenemies`** gates the passive pick alongside other new enemies. Editor numbers **`23167–23174`** (**`CyberFodder`** / **`FodderExtraPassiveSpawn`** included). corpse half base **`BrainStem2`** ported to **`ZombieXDeath`**; **`FrozenCyberFodder`** thaws **`CyberFodder`**; **`SNDINFO`** block for fodder/`CyberZ`/`BIOSHOOT` (**`ps_fodder` GL sprites** shipped for future GK wiring).

- **Gearbox UI/sounds:** eleven **`GRAPHICS/gb_*.png`** (wheel/block art; **`TexMan.checkForTexture`** names **`gb_circ`**, **`gb_hcir`**, …) and three **`sounds/gearbox/gb_*.ogg`** (**`gb_tick`**, **`gb_toggle`**, **`gb_nope`**) — keep in sync with upstream **gearbox-master** `graphics/` + `sounds/` if you refresh art.

- **Gearbox** (m8f / Alexander Kromm, GPLv3+ — **`zscript/gearbox/COPYING_Gearbox_GPLv3.txt`**): weapon / inventory selector folded from **`gearbox-master`** (ZScript under **`zscript/gearbox/`**, **`zscript/MD5/`**, **`zscript/m_gizmos/`**, **`zscript/zabor/`**). **`+PBWeaponWheel`** now opens Gearbox (`event gb_toggle_weapon_menu`); **`zmapinfo.txt`** registers **`gb_EventHandler`**, **`gb_PreviousWeaponEventHandler`**, **`gb_PreviousWeaponStorage`**, **`gb_VmAbortHandler`**. Full Gearbox **OptionMenu** block at **`MENUDEF.txt`** tail (**PB 2022 Enhanced → Gameplay Settings / UI and Feel → Weapon & Inventory Selector**); mirror **`lumps/includes/menudef_gearbox.inc`** — not `#include`d; UZDoom **`MENUDEF` has no `#include`**). **`gb_*`** CVARs in **`CVARINFO`**. **`pb_weapon_wheel_enable`** gates Gearbox open / slot intercept. **Assets:** eleven wheel / block UI patches as **`GRAPHICS/gb_*.png`** (logical names **`gb_circ`**, **`gb_hcir`**, … per **`zscript/gearbox/tools/texture_cache.zs`**; you may use the same basenames **without** a **`.png`** extension on disk if you prefer loose extensionless lumps). **`sounds/gearbox/gb_*.ogg`** + **`SNDINFO.txt`** **`gearbox/*`** entries.

- **PB Staging barrel fold:** Nukage / flame / ice barrels, explosions, shrapnel, kicked and thrown barrels, and spawner wiring now follow the **Project Brutality PB Staging** split (`Core/NukageBarrel*.dec`, `NukageBarrels.dec`, `IceBarrels.dec`, `FlameBarrels.dec`) with vanilla spawn class names **`NukageBarrel`**, **`FlameBarrel`**, **`BarrelFrozen`**; **`SPRITES/DECORATION/BarrelSprites`** and **`SPRITES/SFX/X005`**; barrel carry tokens unified on **`HasBurningBarrel` / `GrabbedBurningBarrel`** with **`Throwed*`** aliases for **`Thrown*`**; **`GLDEFS.txt`** explosion lights updated for **`BBLA` / `BFEX` / `BIXP`**.

- **Kill streak 5-kill reward:** rolls a **rare backpack** (same ammo bundle as **`PB_Backpack`**, skipped if **`GotAPack`** / **`GotAPack_h`**), then a **bonus supplies** tier (health/armor/ammo **pinata** classes), then the existing power-up pool. User CVARs **`pb_ks_r_backpack`**, **`pb_ks_r_backpack_chance`**, **`pb_ks_r_supplies`**, **`pb_ks_r_supplies_chance`** under **Kill Streak Reward Pool** (`MENUDEF.txt`); logic in **`zscript/Items/PBKillstreak/PB_Killstreak_Items.zc`**.

- **Meat Hook assets (PB Weapons Pack):** **`GRAPHICS/Equipments/hook.png`** for the equipment wheel icon; **`SPRITES/equipment/Hook/H00KA0.png`** pickup; **`SPRITES/equipment/HookChain/`** — **`OCLWA0`** (first grapple frame), **`TENDA0`**, **`warcarlssonhook.png`** alongside existing **`OCLWA1`–`8`**.

- **Meat Hook equipment:** Equipment wheel slice **`WW_HookSelected`** + **`UseHookEquipment`** / **`HookSelected`** in **`BaseWeapon.dec`**; **`PB_FireGrapplePulse`** invokes **`PlayerPawnBase.FireHook()`** when the player owns the hook (**`PB_HookToken`** / **`HookAmmo`** / **`AlreadyHaveMeatHook`**). **`pb_hook`** is optional style only (**`2`** = pendulum re-fire; anything else = detach). **`SNDINFO.txt`** hookshot aliases + wheel tooltip **`PB_WHEELTIP_WW_HookSelected`** in **`language.enu`**.

- **Brutal Pack V10 (fatalities):** Xamps **`MPI2`** first-person imp execution (`ImpFatalityMPI` → **`FatalityImpMPI`** in **`NEWPLAYE.dec`**); sprites under **`SPRITES/PLAYER/Fatality/BPv10/`**. When the fatality pool rolls **`Death.Fatality4`**, imps now **`A_Jump(128)`** between classic **`ImpFatality4`** (beatdown) and **`ImpFatalityMPI`** (pistol), including **`PB_ImpGK`** / **`PB_Imp1GK`** and dark-imp variants.

- **Shield Saw:** Finishing a monster with the thrown saw while **`be_ExecutionsON`** is on now spawns the same **`GlorySawPinata*`** shard burst as a Crucible kill when the victim had **≤20** HP before the killing blow (tier still scales with **`SpawnHealth`**). Implemented via **`PB_ShieldSawPinataHandler`** (`zmapinfo` **AddEventHandlers**).
- **Shiny Nash Gore (PBR)** fold: materials + `BloodSplatPB2022` decal pool under **`GRAPHICS/PB2022/ShinyNashGore/`** and **`materials/pb2022/shiny_nashgore/`**, toggled with **`pb_enhanced_brutality_2022`** (Nash pools / corpse blood / footprints use PBR skins; red supergore / blood cloud / head-blood / small supergore use `A_SprayDecal` between **`BloodSplat`** and **`BloodSplatPB2022`** via **`GetCVar("pb_enhanced_brutality_2022")`** in DECORATE (console/local userinfo context; not a per-remote-player read in MP).
- Peacekeeper / Marauder **SSG** fold-in from PB3.0 (`d6bbe88db`).
- Refreshed **SSG** sprite sets (kick, reload, fire, respect, select) including follow-up missed lumps (`e0076241d`, `f5eba713f`).
- **Shield Saw** and **Mastermind Chaingun** integration with spawner and HUD wiring (`b5c61d297`, related `7003ef7b3`, `bb1099367`).
- Large **MP execution** surface: `actors/Execution/*.dec`, `BaseWeapon_MPExecutionHelpers.zsc` / `BaseWeapon_MPExecutions.zsc`, matching **`SPRITES/Finishers/Execution/**`** art (`0b3ae6a04`).
- **Metal Sniper** ZScript implementation and helpers (`0b3ae6a04`, fixes in `c70971995`, `4d8c11fd0`).
- **M41A** ZScript port and related DECORATE support (`0b3ae6a04`, `c70971995`).
- **LooseRounds** and related weapon plumbing (`0b3ae6a04`).
- **Explosive movement** (`pb_rocketjump` / `pb_plasma_wallclimb` patterns) and item pickup evolution: **Custom Shards & Potions** / **Custom Red Armor** (DarkShadow) supersede legacy Wolvus BON3–BON8 tier bonuses; **`pb_item_mutators`** remains for sphere / elite armor extras (**WolvusMutator**-era commits `ed7f66dd2`, `910d1d46e`, `0b8eef3c4`).
- **Pump shotgun** / **auto shotty** pickup sprites **SHTCA0** / **AUSCA0** (PB Staging) under **PumpShotgun** / **AUTOSHOTTY** asset paths.

### Fixed

- **Beam Katana / Argent Sith alt-fire barrier:** `KatanaShieldSpawnerYEET` / `KatanaShieldSpawnerYEET2` now place the barrier columns **22** map units forward (was **8**) via `A_SpawnItemEx` in **`actors/Weapons/Slot1/BeamKatana.dec`** and **`actors/Weapons/Slot1/ArgentSith.dec`**, so the wall sits clear of the view and typical blast/reflection overlap with the wielder is reduced. Panel `Death` states are still visual-only; self-damage was coming from weapon impact/explosion radius and `+REFLECTIVE` behavior while the assembly sat on top of the player.

- **Multi-pass fatality audit (Realm667 Glory Kills + Archvile orphan):** Follow-up pass after the satyr/zombie cleanup, dispatching parallel readonly explorer agents across **T1+T2 / T3 / T4+Tz+Realm667** monster families plus an **asset-inventory** agent. Most agent-flagged "mismatches" turned out to be **intentional folder-organized authoring** that re-uses an existing prefix for a sibling skin (verified by reading the actual sprite folders): **`FD3M`** ships under both **`SPRITES/PLAYER/Fatality/PinkyMech/`** (frames A–K) **and** **`SPRITES/PLAYER/Fatality/VoidPinky/`** (frames L–O), so **`PB_VoidSpectre`** routing to **`VoidSpectreFatality`** → **`FatalityVoidSpectre`** / **`FD3M`** is the authored Void Pinky finisher, not a wrong-victim mistake; **`AVF3`** lives under **`SPRITES/PLAYER/Fatality/Hellion/`**, so **`PB_HellionGK`** → **`HellionFatality`** / **`AVF3`** is the authored Hellion finisher (the **`AV`** prefix is leftover from its archvile heritage, not a class label); **`FR32`** lives under **`SPRITES/PLAYER/Fatality/Revenant/`** so **`PB_BeamRev`** routing through **`BeamRevenantFatality1`** / **`FR32`** is intentional. **Same-class skin variants** (PlasmaZombie 3–8 → vanilla zombieman strips, MechDemon 2–5 → organic pinky, MeanDemon 2–5 → pinky, DarkImp/InfectedImp/SavageImp Fatality4 → **`MPI2`** brown imp, SufferingElemental / RegretElemental → **`PEFatality`** / **`PA1F`**, Realm667 SlayerKnight / Venor → Baron Eld **`PPLJ`** / **`TOPI`**, InfernalArachnotron / EliteArachnotron → arachnotron **`ARF*`**, Annihilator → cyberdemon **`FTCY`**) are deliberate trade-offs because no dedicated finisher art was authored for the alternate skin — leaving the existing routes preserves animation variety, since the in-hand victim stays in the **same creature class** as the live enemy. Real fixes applied below.

  - **Archvile fatality 1 silently played nothing:** **`actors/Monsters/T4-Viles/ARCHVILE.dec`** **`Death.Fatality`** rolled a 4×/4×/4× pool over **`Fatality1`** (granting **`ArchvileFatality`**), **`Fatality2`** (**`ArchvileFatality2`**), and **`Fatality3`** (**`ArchvileFatality3`**). The dispatcher in **`actors/Player/NEWPLAYE.dec`** (~line 674) jumps **`ArchVileFatality`** → **`FatalityArchVile`**, but only **`FatalityArchVile2:`** and **`FatalityArchVile3Eld:`** exist — there is no plain **`FatalityArchVile:`** state shipped. So 1/3 of all Archvile melee finishers granted a token that no first-person animation consumed; the player would freeze briefly then return to See with no visual payoff. **`Fatality1:`** now grants **`ArchvileFatality2`** instead, collapsing branches 1+2 onto the **`AVF2`** animation (8/12 rolls, ~67%) and leaving **`Fatality3`** on the **`SATZ`** Eld (4/12 rolls, ~33%). The orphan **`ArchVileFatality`** dispatcher line in **`NEWPLAYE.dec`** is left in place as a no-op for compatibility; addons that explicitly grant **`ArchVileFatality`** still hit the inventory check, just with no shipped state.
  - **Realm667 Blot held a humanoid zombieman corpse:** **`PB_Realm667_BlotGK`** in **`actors/Monsters/Realm667/Realm667_GloryKills.dec`** rolled **`BLF1`** / **`BLF2`** to grant **`Realm667_FatalityFP1`** / **`Realm667_FatalityFP2`**, which dispatch to **`FatalityRealm667FPEld1`** / **`FatalityRealm667FPEld2`** in **`NEWPLAYE.dec`** — those states play **`NATT`** and **`NZ0D`** (uniformed / dark **zombieman** Eld strips). Blot itself uses **`EYES`** / **`BLOT`** sprites: a tiny black eye-blob the size of a soccer ball. There is no shipped first-person finisher that fits an eye-blob shape. The whole **`Death.Fatality`** branch table was removed; the state still grants **`GoFatality`** so the kill registers as a Glory Kill (pinata + reward chain), but no **`Fatality*`** token, so the player's **`FatalityChecker`** falls through cleanly with no humanoid animation.
  - **Realm667 Behemoth borrowing brown-imp Eld:** **`PB_Realm667_BehemothGK`** rolled **`BHF1`** → **`OtherFatsoFatality2`** (**`FDA1`** Daedabus mancubus, OK for a fat tier) and **`BHF2`** → **`Realm667_FatalityFP3`** (**`49AN`** brown-imp Eld, completely wrong for **`LARD`** cybermancubus). Reworked: **`BHF1`** now grants **`OtherFatsoFatality`** (**`FDAE`**) and **`BHF2`** grants **`OtherFatsoFatality2`** (**`FDA1`**), so both branches stay in the fat-class fatality bucket and the in-hand victim is always mancubus-shaped.
  - **Realm667 Vulgar / FusionCyberImp picking up zombiemen:** **`PB_Realm667_VulgarGK`** **`VGF3`** granted **`Realm667_FatalityFP2`** (**`NZ0D`** dark zombie) on top of two imp Eld branches; **`PB_Realm667_FusionCyberImpGK`** **`CFF3`** granted **`Realm667_FatalityFP1`** (**`NATT`** helmeted zombie). **`VULG`** (Vulgar) and **`UCIM`** (FusionCyberImp) are both imp-class (humanoid imp + cyborg-imp), so seeing the player suddenly hold a uniformed soldier was jarring. **`VGF3`** now grants **`ImpFatality6`** and **`CFF3`** grants **`ImpFatality7`**, keeping every roll inside the imp-Eld pool (**`YUTI`** / **`49AN`**).

  After this pass **`Realm667_FatalityFP1`** / **`FP2`** / **`FP3`** are no longer granted by any monster (the player-side **`FatalityRealm667FPEld1`** / **`2`** / **`3`** states and dispatcher lines stay defined as latent plumbing for future Realm667 wrappers — they were near-duplicates of **`FatalityZMan7Eld`** / **`FatalityZMan8Eld`** / **`FatalityImp7Eld`**, using the same **`NATT`** / **`NZ0D`** / **`49AN`** sprites with slightly different timing / sound, so no unique animation content is lost). The **`HUF1`** / **`HUF2`** / **`HUF3`** / **`HEDF`** Eld sprites are no longer orphan: they were re-wired into new `FatalityHunger1Eld` / `2Eld` / `3Eld` / `FatalityCrackodemon1Eld` player states + new `HungerGK` and `CrackoDemonGK` Replaces wrappers — see the **Monster Pack Hunger / CrackoDemon Glory Kill wiring** entry under **Added** above for details.

- **Satyr-shaped victim mixed into zombie / commando / revenant / cacodemon fatalities:** Several **El Diablo "Eld"** first-person fatality strips ship sprite sets sourced from El Diablo’s **`Hunger/`**, **`CrackoDemon Fatality/`**, and **`satyr2/`** folders, depicting a **bald grey humanoid**, a **black-armored Crackodemon**, or a **horned Satyr/Cybersatyr** being held in the player’s hands. The corresponding monster `Death.Fatality` jump pools in PB 2022 included those rolls regardless of what the live enemy actually looked like, so a regular **Sergeant**, **Commando**, **Revenant**, **Cacodemon**, or Realm667 **Drone** / **BabyCaco** had a meaningful chance of triggering an animation where the bald-or-horned victim sprite did not match the enemy. The mismatched routes are:
  - **`FatalitySergeant7Eld`** / **`FatalitySergeant8Eld`** (sprites **`ESCR`** / **`SOBO`** in **`SPRITES/MONSTERS/fatalitys/eld_eld_sarge/`**, victim is a horned Satyr).
  - **`FatalityComando3Eld`** / **`FatalityComando4Eld`** (**`HUF3`** / **`HEDF`** in **`eld_eld_comando/`**, victim is a Hunger / Crackodemon shape).
  - **`FatalityCacoDemon3Eld`** / **`FatalityCacoDemon4Eld`** (**`HUF1`** / **`HUF2`** in **`eld_eld_caco/`**, victim is a humanoid Hunger — wrong for a flying floater).
  - **`FatalityRevenant3Eld`** / **`FatalityRevenant4Eld`** (**`KOKO`** in **`eld_eld_reven/`**, sourced from El Diablo `satyr2/`, victim is a horned Satyr — wrong for a skeletal revenant).
  
  Trimmed the Death.Fatality `A_Jump` pools so those rolls are no longer reachable in normal gameplay (the player-side `Fatality*Eld` states stay defined for future addons, but no monster grants the matching tokens anymore):
  - **`zscript/Monsters/Sergeants/ZombieSergeant.zc`**, **`ZombieSergeantHelmet.zc`**, and **`SergeantsGloryKill.zc`** (`PB_ShotgunGuyGK` / `PB_ShotgunGuyHelmetGK` / `PB_RiotShieldGuyGK`) — drop **`Death.Fatality7`** + **`Death.Fatality8`**.
  - **`actors/Monsters/T1-Grunts/GloryKills.dec`** (`PB_RocketZombieGK`, which inherits `Death.Fatality1–8` from **`PB_Sergeant`** → **`PB_ShotgunGuy`** = `ZombieSergeant.zc`) — same trim, since the GK has its own `A_Jump` pool independent of the parent.
  - **`actors/Monsters/T1-Grunts/Commando.dec`**, **`ClassicCommando.dec`**, and **`T1-Grunts/GloryKills.dec`** (`PB_ClassicCommandoGK` + `PB_CommandoGK`) — drop **`ComandoF3`** + **`ComandoF4`**.
  - **`actors/Monsters/T3-Floaters/CACODEMON.dec`** + **`T3-Floaters/GloryKills.dec`** (`PB_CacodemonGK`) — drop **`CacoF3`** + **`CacoF4`**.
  - **`actors/Monsters/T3-Revies/REVENANT.dec`** + **`T3-Revies/GloryKills.dec`** (`PB_RevenantGK`) — drop **`RevenF3`** + **`RevenF4`**.
  - **`actors/Monsters/Realm667/Realm667_GloryKills.dec`** (`PB_Realm667_BabyCacoGK`) reroutes **`BCF1`** / **`BCF2`** to **`CacoDemonFatality`** / **`CacoDemonFatality2`** (the legitimate caco animations); (`PB_Realm667_DroneGK`) reroutes **`DGF1`** / **`DGF2`** / **`DGF3`** to **`RevenantFatality`** / **`RevenantFatality2`** so the in-hand victim stays bony and skeletal-looking.
  
  Also fixed broken first frames in **`actors/Player/NEWPLAYE.dec`**: both **`FatalityZMan8Eld`** and **`FatalityNazi4Eld`** opened with **`NZ0D D 5`** but **`NZ0DD0.png`** is not shipped under **`SPRITES/MONSTERS/fatalitys/eld_eld_zman/`** (only frames **E**–**J** exist). Removed the missing-frame reference in both states so the animation starts from frame **E** instead of flashing GZDoom’s missing-sprite placeholder on the first tic.
  
  **Visual sanity check:** `NATT` (uniformed helmet zombieman/sergeant) is left in the **`PB_ZombieMan`** / **`PB_ZombieScientist`**-family `Death.Fatality7` pool because it depicts the right enemy class; `PPLJ` / `TOPI` (Baron 3+4 Eld) and `CPUI` / `BOTU` (HK 5+6 Eld) stay in the Baron / Hell Knight pools because the in-hand red-and-horned beasts read as Baron-tier; `YUTI` / `49AN` (Imp 6+7 Eld) stay in the imp pool because the brown clawed silhouette reads as a Doom Imp; `SATZ` (Archvile 3 Eld) stays because **PB's `PB_Archvile` itself uses the El Diablo `DIAB` horned-bull sprite**, so the in-hand satyr sprite genuinely matches the live archvile.

- **Bald Zombie Scientist Glory Kill mismatch:** **`PB_ZombieScientistGK`** and **`PB_ZombieScientistChainsawGK`** (plus their base **`PB_ZombieScientist`** / **`PB_ZombieScientistChainsaw`** classes) were rolling the standard 12-entry zombieman fatality pool on **`Death.Fatality`**, so a bald lab-coat scientist (or chainsaw lab guy) had a ~50% chance of triggering **`ZombieManFatality4`–`8`** tokens — playing first-person fatality animations (**`FPZ4`**, **`4PZ2`**, **`NATT`**, **`NZ0D`** strips in **`actors/Player/PLAYER.dec`** / **`NEWPLAYE.dec`**) that show a uniformed/helmeted zombieman in the player's hands instead of the bald scientist they actually killed. **Plain ZSci** now only rolls **`Death.Fatality1`–`3`** (`LabGuyToken1`/`2`/`3` → **`FatalityLabGuy1/2/3`** with **`FSF1`/`FSF2`/`FSF3`** lab-coat strips); **chainsaw ZSci** unconditionally takes **`Death.Fatality1`** (`ChainsawZombieFatality` → **`FatalityChainsawZombie`** with the **`FSF4`** chainsaw lab-coat strip + **`AxeSplitLabguy`** corpse). Touches **`zscript/Monsters/ZombieMen/ZombieScientist.zc`**, **`ZombieScientistChainsaw.zc`**, and **`ZombieScientistGloryKill.zc`** (both GK overrides).

- **Glory Kill Eternal (NewHUD):** **`sv_fuelhud_background`** now defaults to **`3`** so **`GloryHUD.acs`** runs the Eternal branch (fonts **`CRUCRY*`**, **`CRUCAM*`**, **`BPAMMO10–14`**, **`SCFLM*`**, **`SCICB*`**). **`Fontdefs.txt`** NewHUD paths use **`GRAPHICS/HUD/glorykills/newhud/`** for case-sensitive loads. Existing saves: **Glorykill Options → Glory HUD Background → Eternal (NewHUD)** or **`resetcvar sv_fuelhud_background`**.

- **MENUDEF / Gearbox (UZDoom 4.14.3):** **`MENUDEF.txt` does not support `#include`** (`Unknown keyword '#'`). Gearbox **`OptionMenu`** / **`OptionValue`** / **`OptionString`** blocks are inlined at the end of **`MENUDEF.txt`**; **`lumps/includes/menudef_gearbox.inc`** is kept as an editable mirror only.

- **Gearbox + `gb_blur` (UZDoom 4.14.3):** ZScript does not allow `static` member fields on the old `gb_BlurBridge` helper (`Invalid qualifiers for sGearboxMenuBlur`). Gearbox’s “menu wants postprocess blur” flag now lives on **`gb_EventHandler`** as a **`ui`** field (writable from **`renderOverlay`**); **`PB_WheelBSSBlurHandler`** reads it via **`EventHandler.Find("gb_EventHandler")`**. Removed **`zscript/gearbox/display/blur.zs`**. **`gb_ViewModel.icons`** is **`Array<TextureID>`** — **`weapon_menu` / `inventory_menu`** no longer push **`int`**; **`pbWeaponWheelAllowed`** / hide checks use **`ui`** so **`inputProcess`** / **`renderOverlay`** don’t cross play↔ui incorrectly.

- **Equipment wheel after `give all`:** Slices for **Leech**, **Quick Launcher**, and **Meat Hook** no longer require token-only inventory that the console cheat often skips. **Leech** lists if **`HasLeech`** or **`PB_DTech` ≥ 4** (one throw); **Quick Launcher** lists from **`MiniHellRocketAmmo`** only ( **`HasRevGun`** no longer required); **Meat Hook** lists if **`PB_HookToken`**, **`HookAmmo`**, or **`AlreadyHaveMeatHook`**. **`BaseWeapon.dec`** / **`FireHook()`** use the same hook-ownership checks.

- **Weapon selection wheel (bind / `+PBWeaponWheel`):** The list no longer requires **`+INVENTORY.INVBAR`** only — it includes owned weapons with a normal **weapon slot** (0–9), matching the Doom HUD. This fixes an empty wheel and console spam after **`give all`** when **`ReadyWeapon` was also unset, **`BringUpWeapon()`** is used to raise a slotted weapon before opening; the **Deselect** abort in **`WorldTick`** is skipped while the weapon-pick wheel is active.

- **Kill streak announcer (UZDoom 4.14.3):** **`PB_Killstreak_Handler`** used **`CHAN_8`** for voice lines; this engine line’s ZScript only defines sound channels through **`CHAN_7`**, which caused a compile error. Announcer **`A_StartSound` / `IsActorPlayingSound`** now use **`CHAN_7`**.

- **Meat Hook / `pb_hook`:** Grapple is no longer gated by a non-zero **`pb_hook`**; owning the hook is enough. **`pb_hook`** only selects pendulum (**`2`**) vs default detach-on-re-fire; **`GrapplingHook`** net events work regardless. `FatalityRevenant2` retargeted from missing **`FRV2`** to existing **`FREV`** (same letter sequence as `FatalityRevenant`). **`FatalityPVP`** retargeted from missing **`PVPF`** to existing **`FSP7`** (pistol zombieman FP strip); `PPOD` gib spawns unchanged.
- **Shield Saw execution pinata:** `WorldThingDied` never fills `e.Damage`, so the handler’s `dmg < 1` check always failed. Eligibility is now set in `WorldThingDamaged` (`PB_ShieldSawPinataPending` when a saw hit’s pre-hit HP ≤ 20), then confirmed on death with saw `Inflictor`.
- **Hellduke Glory Kill (`FatalityHellduke6Eld`):** `HDF0` strip has no **F** lump under **`SPRITES/MONSTERS/fatalitys/eld_eld_hellduke`** (only **A–E** and **I**); state now plays **A–E** then **I** so the sixth frame is valid.
- **Pinky Glory Kill (`FatalityDemon4Eld`):** El Diablo LSLS token **4** no longer flashes past in a few tics; sequence paces like classic **FatalityDemon** (hold / CDCDCD beat) and spawns **XDeathDemonHead** before **DyingDemon**, matching the satisfying head-rip payoff of the other pinky paths.
- **PB_CSSG:** Empty tube no longer ran the single-barrel `LeftFire` path (same `count < 2` branch as one shell loaded), which fired projectiles without removing shells after punch / muzzle-flash returns to **Fire** / **AltFire**; empty mag now jumps to **Reload** first.
- **ZScript weapons + Glory Kill token (`GoFatality`):** **`Fire` / `AltFire`** paths that set **`SetPlayerProperty(0, 1, 0)`** then **`Steady`** could leave **`PROP_FROZEN`** on after **`GoFatality`** cleared (movement / weapon PSprite soft-lock). **`Steady`** now waits out **`GoFatality`** then **`SetPlayerProperty(0, 0, 0)`** before returning to ready — **PB_CSSG**, **PB_BeamKatana**, **PB_ArgentSith**, **PB_MetalSniper**, **PB_NeoHMG**, **PB_M41A**, **PB_Excavator**, **BDPBattleRifle**, **Stormcast**.
- **Shield Saw + Commander SSG / fatality cancel:** **`ThrowShieldSaw`** and **`PB_CSSG.Steady`** only cleared **`SetPlayerProperty(0,0,0)`**; **`PROP_TOTALLYFROZEN`** (ice-style freeze, ledge/camera locks, etc.) could stay on and leave you unable to move with an invisible **`Steady`** weapon layer. **`ThrowShieldSaw`** (**`BaseWeapon.dec`** / **`BaseWeapon.zc`**) and **`PB_CSSG`** **`Steady`** now also **`SetPlayerProperty(0, 0, PROP_TOTALLYFROZEN)`** when unblocking. The same dual-clear is applied across other **`GoFatality`** **`Steady`** waits and **`Fire`/`AltFire`** branches that unfreeze the player (remaining ZScript guns/katanas/Stormcast/CSSG, plus shared **`PB_Weapon`** **`Steady`** in **`BaseWeapon.dec`**, and DECORATE weapons that override **`Steady`** or fatality **`Fire`** else—including LMG/SSG/X12/Super GL/HASG/Hell Pistoler/Quad Barrel/Paingiver/black-hole/Ballista/Demon Exterminator/Carbine/Riot Shield/Nailgun/Minigun/Marauder SSG/XM‑21/Lever Action **`Fire`** else, plasma/railgun/freezer/BFG Mk IV/BFG beam/Mastermind chaingun/Demon Tech/flamer/pistol, and fists **`MELEE`** including combo kick end).
- **Shield Saw + experimental executions (`pb_experimental`):** **`PB_SetPlayerExecutionProperties()`** applies **`PROP_INVULNERABILITY`**, **`PROP_FLY`**, and **`PROP_TOTALLYFROZEN`** before **`Execution_*`** states; **`ThrowShieldSaw`** previously only cleared classic freeze / totally frozen bits, so cancelling with the saw could leave you floating, unable to take damage, and with no usable weapon view. **`ThrowShieldSaw`** now calls **`PB_UnsetPlayerExecutionProperties()`** when the throw starts and again after the throw sequence (**`BaseWeapon.zc`** / **`BaseWeapon.dec`**).
- **First-person fatalities (`NEWPLAYE.dec` / `PLAYER.dec`) + shared weapon unblock:** Every fatality / spawn-recovery path that ends with **`SetPlayerProperty(0,0,0)`** (`PROP_FROZEN`) now also calls **`SetPlayerProperty(0,0,PROP_TOTALLYFROZEN)`** on the next line (same sprite prefix), so total freeze from ice, ledge, or weapon layers cannot persist after **`FatalityChecker`** finishers (e.g. Spider Mastermind / boss pool). **`BaseWeapon.dec`** / **`BaseWeapon.zc`** also pair clears on **`ThrowShieldSaw`**, **`DoKick`** end, and **`PB_WeapTokenSwitch`**. Remaining orphans on **`LMG`**, **`MINIGUN`**, **`Nailgun`**, and **`OldHMG`** deselect branches were aligned the same way.
- **Gore / footprints:** Stopped **NashGoreBloodPlane** spawn logic from forcing **0.9–1.0** actor scale on **`GrowingBloodPool`** (so **`RedBloodFootPrint*`** / smear chain keep their small **DECORATE** scales instead of ballooning ~10×). Removed erroneous **`MODELS/Definitions/GreenBlood.txt`** footprint model override that used **90³** MD3 scale (huge voxels + footprint spam could exhaust VRAM/system memory and hard-crash). Corrected **NashGore** footprint hooks: **`NashGoreFootprint`** class check in **PostBeginPlay**, **`footprintFootSide`** typo in **NashGoreActor** (left/right alternation).
- **ZScript quick melee / Shield Saw + Stormcast:** Glory-stagger melee (`FinisherToken` + line of fire) now runs the same **PermormGloryKill** chain as DECORATE **`PB_Weapon`** (new states on **`PB_WeaponBase`**), **close-range punch** is preferred **before** **`PB_Execute()`**, and throwing the **Shield Saw** clears stray **`GoFatality`** + **`SetPlayerProperty(0,0,0)`** so PSprites / weapon switching do not stay locked after a bad execution vs throw path.

### Changed

- **Folded-monster spawn rebalance (power-tier fairness pass).** Audited every monster spawner and the `PB_HorrorPassiveController` Hunger pool to compare each folded creature's spawn weight against its strength relative to the vanilla counterpart it sits beside. Result: the **Realm667** family was uniformly under-represented (most branches at ~0.4–1.2% of their tier), and the **Hunger** weight in the horror passive pool was effectively a 1% lottery ticket. Bumps applied (sequential `A_Jump(N, …)` weights out of 256, exact percentages reflect drop-through math):
  - **`actors/SPAWNERS/MonsterSpawners/ImpSpawner.dec`** → **`ImpPackSpawner.Spawn:`**: `PossessedGibs` 1→4 (~0.39%→~1.56%, fodder; HP 48–115), `Blot` 1→4 (~0.39%→~1.54%, eye-blob; HP 20), `Vulgar` 2→5 (~0.78%→~1.89%, imp-class; HP 150), `FusionCyberImp` 2→5 (~0.77%→~1.86%, cyborg-imp; HP 100). Combined Realm667 imp share rises from ~2.3% to ~6.85% of imp spawns.
  - **`actors/SPAWNERS/MonsterSpawners/CacodemonSpawner.dec`** → **`CacoPackSpawner.Spawn:`**: `BabyCaco` 3→8 (~1.17%→~3.13%, weaker caco at HP 150 vs `PB_Cacodemon` HP 400). Sized so the regular `CacoPack` token still dominates pack rolls.
  - **`actors/SPAWNERS/MonsterSpawners/MancubusSpawner.dec`** → **`MancubusPackSpawner.Spawn:`**: `Behemoth` 2→4 (~0.78%→~1.56%). Kept deliberately rare because the actor has **HP 2400** — much tougher than `PB_Mancubus` (HP 600); doubling rather than quadrupling avoids flooding maps with a boss-tier brute.
  - **`actors/SPAWNERS/MonsterSpawners/LostSoulSpawner.dec`** → **`LostSoulPackSpawner.Spawn:`**: `Drone` 2→8 (~0.78%→~3.13%, flying brute at HP 150 — between `PB_LostSoul` HP 60 and `PB_Phantasm`).
  - **`actors/SPAWNERS/MonsterSpawners/BaronSpawner.dec`** → **`BaronPackSpawner.Spawn:`**: `Venor` stays at 13/256 (the user-set ~5%); `SlayerKnight` 2→5 (~0.78%→~1.95%, HP 500 / dual-trident, comparable to `PB_HellKnight`). Combined Realm667 baron-tier share rises from ~5.9% to ~7.0%.
  - **`zscript/Spawner/PB_HorrorPassiveController.zc`** → `PickHorrorspawnMap` weights: `w4` (HungryGroup) 2→4, `w6` (Hunger) 1→3. Hunger pool combined share in the horror passive table rises from ~3.2% to ~6.5%; `Horrorspawn1` (`w1=50`, ~46% of pool) is still the dominant pick. Hunger is zombieman-tier fodder (HP 20) so this stays "fair" — it never crowds out the Horrorspawn fodder.
  - **Not bumped:** Crackodemon (50% passive on Caco-spawner *2 branches), Hellduke / Hellsmith (33% per Cyberdemon spawn), AracnorbQueen / Director (33% per Mastermind spawn), Helemental / Hierophant / ESoul / ZombieTank-family / ZombieFlyer (100% per matching pack-spawner branch). Those Monster Pack additions already hit at boss-/elite-anchored frequencies and would over-saturate maps if pushed higher. Venor was kept at the explicit 5% the user picked in its initial fold.

- **Gearbox options:** No longer a separate **Options** root item. The **`gb_Options`** tree (**Weapon & Inventory Selector**) is linked from **PB 2022 Enhanced → Gameplay Settings** (next to **Weapon selection wheel**) and **PB 2022 Enhanced → UI and Feel**. **`language.enu`**: **`PB_WEAPON_SELECTOR_MENU`**, **`PB_WEAPON_SELECTOR_MENU_HINT`**, **`PB_WEAPON_WHEEL_ENABLE_DESC`** path text updated.

- **PBX 2022 weapon HUD** (`PB_EventHandler` in **`zscript/PBPDA.zc`**): pickup and mode icon overlays apply a **conservative size normalization** (longest-edge vs reference **52px**, multiplier clamped **0.70–1.35**) on top of the existing **`pb_weaponhud_scale`** / **`pb_weaponmodehud_scale`** sliders so small/large **`Icon`/`AltHUDIcon`** / wheel PNGs read closer to the same on-screen footprint without changing aspect ratio.

- **Equipment wheel (melee / User2):** **Flame Belch** and **Ice Bomb** are no longer listed — they stay on their **Glory Kill / shoulder** binds (`pb_shouldercannon` / `pb_glorycryo`). **Meat Hook** lists when you own the hook; **`pb_hook`** is no longer a master on/off switch (**`2`** = pendulum mode; default detach).

- **Weapon selection wheel (`+PBWeaponWheel`):** Default bind drives **folded Gearbox** (wheel / blocks / text, multiwheel, slot intercept); see **[Unreleased] → Added → Gearbox**. Earlier PB-only slot-ring + inner-ring code in **`zscript/PbWheel/pb_weapon_pick_wheel.zsc`** / **`ev_drawer_special.zsc`** remains for **`netevent PBWeaponWheelOn`/`Off`** if aliased manually.

- **`SPRITES/` layout (weapons / player / decorations):** **`CustomMelee/`** and **`GloryKillLauncher/`** now live under **`SPRITES/WEAPONS/`**; Shield Saw throw sprites (**`ThrowShield/`**, `SHIE*`) are nested under **`SPRITES/WEAPONS/GloryKillLauncher/ThrowShield/`**. **`HELMET/`** → **`SPRITES/PLAYER/HELMET/`**. **`RUNES/`** → **`SPRITES/DECORATION/RUNES/`** (sprites bucket name stays **`DECORATION`**).
- **SPRITES layout:** Consolidated **`SPRITES/Fatality (Player)/`** into **`SPRITES/PLAYER/Fatality/`** with short subfolder names (e.g. **`Imp/`**, **`Revenant/`**). Removed duplicate **`Aracnorb Fatality (Player)/`** (identical **`FTAC*`** to **`Aracnorb/`**). Grouped **`Execution`**, **`FATALITY`**, and **`GLORYKILL`** under **`SPRITES/Finishers/`** (`Finishers/Execution`, `Finishers/FATALITY`, `Finishers/GLORYKILL`); removed duplicate nested **`Finishers/Execution/D3Maledict/D3Maledict/`** (same basenames as the parent **`D3Maledict/`**); renamed **`Finishers/GLORYKILL/Project brutality/`** to **`ProjectBrutality/`**. **Realm 667 pickups:** former **`SPRITES/CRASHPACK/`** tree is now **`SPRITES/MONSTERS/Realm667/`** (flat creature folders); addon brightmaps live under **`brightmaps/doom/Monsters/{BabyCaco,DarkCardinal,Nitromite,Venor}/`** with matching **`doommonsters.bm`** entries; **`RPUF*`** lumps were removed from the Behemoth pack (unused by **`PB_Realm667_Behemoth`**, basename clash with **Hierophant**). **Possessed Gibs** lumps flattened into **`Realm667/possessedgibs/`** (restored from **`SOUNDS/Realm667Pack/possessedgibs/sprites/`** where needed). **No DECORATE/ZScript renames for sprite basenames.**
- **Kill streak rewards (“Unless You Have Powah” pool):** Timed power-ups from this pool run ~33% shorter (duration × **0.67**, rounded): **`PB_KSFreeze`** bonus tick, **`AmmoSphere`** / **`RegenSphere`** / **`LifeshieldSphere`** / **`Crucifix`**, **`PowerDeflect`** / **`ElectricAuraSphere`** / **`PB_PowerTimeSlow`** (TFST), plus **`PB_KSHasteSphere`** / **`PB_KSDoubleSphere`** / **`PB_KSInvulnerabilitySphere`** so rolled streak rewards stay shortened without changing **`HasteSphere`** / **`DoubleSphere`** / **`InvulnerabilitySphere_`** durations on ordinary map drops.
- **`doommonsters.bm`:** Normalized every **`map "brightmaps/doom/monsters/`** path to **`brightmaps/doom/Monsters/`** so lump paths match the repository folder casing (important on Linux). **`brightmaps/monsters/Marauder/`** entries for the Peacekeeper / SSG art are unchanged (no `doom/` segment).
- **`SPRITES/MONSTERS/Marauder/`:** Moved **`SPRITES/Marauder/`** here; **0** basename clashes with the rest of **`SPRITES/`**. Boss / finisher graphics only — no DECORATE path updates required.
- **`SPRITES/WEAPONS/Peacekeeper/`:** Moved **`SPRITES/Peacekeeper/`** here (Marauder SSG / Eternal SSG art); **0** basename clashes. Sounds stay under **`sounds/combat/weapons/Peacekeeper/`**; no lump references used folder paths.
- **`SPRITES/WEAPONS/` slot layout:** Merged duplicate **`Slot 1`…`Slot 9`** trees (spaced names) into single **`Slot1`…`Slot9`** folders (**0** cross-tree basename clashes); removed empty spaced directories. Renamed **`Slot 7`** → **`Slot7`** (only ever existed with a space). Renamed **`Throw Shield`** → **`ThrowShield`**. **`brightmaps/WEAPONS/`** uses the same compact slot folders (**`Slot0`…`Slot6`**, **`M2Plasma`**, **`AutoShotgun`**, etc.); **`BMAP/Weapons/`** and lump **`map`** paths reference **`brightmaps/WEAPONS/...`** (repository **`brightmaps/`** root casing + **`WEAPONS/`** segment for Linux).
- **`SPRITES/` buckets — GK launcher, effects, gore, finishers:** **`GloryKillLauncher/`** groups **ShoulderCannon**, **Pinata** (from **`SOULCUBE/`**), **Crucible**, **CrucibleTrail**, **GLORYSAW**, **PBWP_GloryKill**. Top-level **`FLARES`**, **`NEWFLARES`**, **`FLAMES`**, **`Effect/`**, **`PARTICLES`**, **`SMOKE`**, **`SFX`**, **`AM`** → **`Effects/`** subfolders (**`Flares`**, **`NewFlares`**, **`Flames`**, **`Legacy`**, **`Particles`**, **`Smoke`**, **`SFX`**, **`AM`**, **`Portals`**). **`PB_Gore`** → **`GORE/PB_Gore`**; **BDv22** gore lumps **`SPRITES/BDv22/`** → **`GORE/BDv22/`**. **`MONSTERFATALITY`** → **`Finishers/MONSTERFATALITY/`**. **`GORE/MoreDeath`** was mislabeled: it only held **`D3Maledict`** fatality/death art (**`D3MI*`** / **`D3ML*`**) — moved to **`Finishers/D3Maledict/`**. **`NashGore/`** not merged into **`GORE/`** (overlapping sprite basenames).

- **Stormcast** re-enabled (slot 9): folded melee-pack ZScript + DECORATE are loaded again; BFG spawner can pick **Stormcast**; HUD + **Weapon Spawns** menu use **`pb_NoStormcastWeapon`**. Alt-fire spell branches match **Project-Brutality-Weapons-Pack** behavior (hold **Fire** while charging for orbs, **User1**/equipment for stunwalls, **Weapon Special** held for Arc of Death tiers, **Reload** tap during charge for warper, **Alt-Fire** while airborne for air lightning)—not weapon-special **wheel** `Select_SC_*` / `SCMode_*` tokens (those actors were removed).

- **PB_SSG** / **PB_QuadSG** (dual SSG + quad respect): sprites synced from **`PB Old SSG Skin But Better`** (`Downloads\PB_Old SSG Skin But Better\SPRITES\WEAPONS\Slot 3`) into **`SPRITES/WEAPONS/Slot3/SSG`** and **`SPRITES/WEAPONS/Slot3/Quad-Shotgun`**.

- **Metal Sniper** / **MarauderSSG**: louder primary-fire audio (**MetalSniper** `MS_FireActual` **1.44**; **MarauderSSG** `MSSFR` **`A_PlaySoundEx`** **1.5**).
- **Carbine**: weapon-select fixes; obsolete Carbine sprite lumps removed (`e0076241d`).
- **NeoHMG** deployed shield: floor sampling / `ZatPoint` `Vector2`, no player tracking, locked deploy yaw, `damageCredit` for shield-death burst, wall-facing presentation and scale (`0b3ae6a04`); follow-up **deployment position** tweak (`fef269dba`).
- **PB_WeaponTacticalFeel**: suppress tactical tilt/roll on NeoHMG fire so state-authored offsets stay stable (`0b3ae6a04`).
- Weapon-special **wheel**: reset freeze state on `WorldLoaded` (`0b3ae6a04`).
- **BDP Battle Rifle**: burst audio pacing and auto-reload on empty mag (`11504c30e`).
- **Hell Pistoler** / **Demon-Tech Minigun** rework tracks in history (`1e46ff7dc`); DTech Minigun later **disabled** as broken (`5be253ff0`).
- Menus, tooltips, **Glory Kills** options, killstreak messaging (`e2687bc02`, `8697c2951`, `10e37b923`).
- Wall-detection lowering and **Tilt++** / tactical motion (`5b67a2849`, `6dc1cd6c1`, `1edce0b20`, `eb942028f`).
- Gore handlers (BDv22 merge, BPv10, Nash, death-boost) and **pb_enhanced_brutality_2022** integration touches (`960e3aa36`, `fc291df83`, `08cde24c6`, `79140dc1c`).
- **PB_DeathGoreBoostHandler**: extra death pools/trails now randomly mix **NashGoreBloodSpot** / **NashGoreBloodSpotSmall** (shiny Nash model path when Enhanced Brootality is on) and **NashGoreClassicBloodTrail** with **GrowingBloodPool**, **BigBloodSpot**, and **PB_SquirtingBloodTrail**.
- Branding: killstreak icon **UAC** instead of UT-style (`e0fdfaa8e`); title / logo assets (`4e9f42267`, `2a288a08d`, `4eb7ae9bc`).
- **Weapon slot cycling:** **PlayerPawnBase** overrides **PickNextWeapon** / **PickPrevWeapon** (higher wrap budget; fallback when the ready weapon is missing from the slot table).
- **Pump shotgun:** states use **SH0G** sprite prefix to match shipped **PumpShotgun** lumps; full PB Staging **PumpShotgun** sprite tree synced to the repo.
- **Flamethrower** / **hell rifle:** **Weapon.SlotNumber** and **Player.WeaponSlot** set to slot **9** for consistent next/prev cycling with the updated slot logic.
- **Explosive movement:** **`pb_rocketjump`** now gates player **grenade** family explosions the same way as **PB_PlayerRocketExplosion** (no self-splash + **`PBBlastMomentum`** thrust when **On**; **`XF_HURTSOURCE`** self-splash when **Off**): hand **frag** (`ThrownGrenade1` + **`PB_PlayerHandGrenadeExplosion`**), **Super GL** impact / sticky / incendiary / cryo / acid fog, **M41A** grenade, **Ballista** sticky, **Excavator**, equipment **Molotov**, and **Paingiver** seeker (**`PB_PlayerRocketExplosion`**).

### Fixed

- **Stormcast**: ZScript state action blocks used **`return state("")`** (string → state type error); use **`return ResolveState(null)`** for normal fall-through so UZDoom compiles.
- **Stormcast** stunwall (equipment while holding alt-fire charge): removed **`A_SetPitch`** kick frames that never restored view pitch cleanly; after stunwall, **`Goto Ready3`** when alt-fire is released, or **`Goto AltHold`** when still held so charge tier routing matches **`PowerChargeStorm`** instead of **`Ready` → full `AltFire` restart** (fixes missing sprites / stuck feel and post-fx shake).
- **Marauder SSG**: primary **`MSSFR`** uses **`A_StartSound(..., CHAN_WEAPON, CHANF_DEFAULT, 1.5, ATTN_NORM)`** instead of **`A_PlaySoundEx`** (avoids volume truncation warning on `1.5`).
- **Pump shotgun** buckshot: **Fire** / **PumpFromHip2** used **SHTF** for muzzle frames while **PumpShotgun** only ships **SH0FA0–SH0FG0** (not **SHTFA0–SHTFG0**); states and **`BMAP/Shotgun.txt`** now use **SH0F** with matching brightmaps.
- **UZDoom startup**: `NoDelay`, `Vel.LengthXY`, `A_RadiusGive` compatibility (`a3a7ede64`).
- **HASG** sound, **Metal Sniper** reload, **M41A** / Battle Rifle damage (`c70971995`).
- **Metal Sniper**: infinite reload loop and **SniperAmmo** backpack overflow (`4d8c11fd0`).
- General error/warning cleanup passes (`4926d43ba`, `79140dc1c`).
- **Carbine** reticle and related issues (`761e502d7`).
- **ShieldSaw.dec** / ACS callback flow maintenance (`07d57f6b7`).
- **First-person legs:** **PB_WeaponRaise** uses legs overlay **-10** (matches **SelectFirstPersonLegs**) instead of **-1000**.
- **SSG / quad shotgun:** removed invalid **PB_WeaponBase.Upgrade** from **SSG.dec**; **PB_jumpIfNoAmmo()** DECORATE empty-paren fixes in **SSG.dec** / **QUADBARREL.dec**; removed dead **HasNotPickedUpSSG** line; quad ammo HUD uses **SGN3A0** when **QSGSA0** is absent; dropped stray **SSG_Staging** paths list lump.
- **PB_Freezer:** **UnloaderToken** handling aligned with unload / slot-cycle behavior.
- **X12 Shotgun:** Semiauto High-Power ("Senato") mode no longer crashes on fire — the **`Fire2_WaitRelease`** poll used **`return state("Fire2_WaitRelease")`**, which DECORATE processes as a same-tic **`SetState`** self-jump and trips *Infinite State Loop in Weapon State X12Shotgun.139* (also kills the psprite, which is why the weapon disappears). Now uses the canonical PB **`return state("")` + `Loop`** pattern, matching UACSMG / MP40 / AUTOSHOTGUN reload polls and RAILGUN laser stages.

### Removed

- **Demon-Tech Minigun** as a supported spawn/player weapon (broken); references toggled off per mod policy (`5be253ff0`).
- **X12 Shotgun Semi-Auto / "Senato" mode** entirely. The `IsSemi` token, the `Fire2` / `Fire2_WaitRelease` states, the `AltFire` toggle and `FullAgain` swap-back state, and the `X12ReadyToFire` semi-auto branch are all gone. Even after the prior `Fire2_WaitRelease` `Loop` fix, the mode kept producing engine-level state-loop failures, so the X12 is now full-auto only. `WeaponSpecial` reverts to its no-op `"No X12 Special Available"` print + `Goto X12ReadyToFire`. `language.enu` `PB_WPNHINT_X12Shotgun` no longer advertises the semi-auto toggle.
- **`PB_PulseCannon`** and **`PB_DualPulseCannons`** outright (and their entire support surface), closing **Root Cause 2** in **`docs/weapon_cycle_bug_investigation.md`**: both weapons set **`+CHEATNOTWEAPON`** and **`+POWERED_UP`**, which **`idfa`** / `give allweapons` skipped silently — so after IDFA the slot 8 cycle table had a hole that forward-scrolling treated as an end-of-list and refused to advance into slot 9 (`BHGen`). With both classes gone, **`Player.WeaponSlot 8`** is now just **`PB_M1Plasma, PB_M2Plasma`**, both fully cheat-grantable, and slot 8 → 9 cycling works post-IDFA. Touched: deleted **`actors/Weapons/Slot6/PulseCannon.dec`**, **`actors/Weapons/Slot6/DUALPULSECANNON.dec`**, **`actors/Friendly Marines/Marine-PulseCannon.dec`**, **`BMAP/PulseCannon.txt`**, **`SPRITES/WEAPONS/Slot6/PulseCannon/`** (106 files), **`SPRITES/PLAYER/SKINS/PULSECANNON/`** (102 files), **`brightmaps/WEAPONS/PulseCannon/`** (6 files); stripped includes / actor refs / cvars / menu rows / language strings / `IsSelected` blocks / `DoomEdNum`s 23023+23053 / GLDEFS `pointlight PulseC` + `object PB_PulseCannon` / `doomdefs.bm` include / `BMAP/Weapons.txt` `PLCUA0` line / 4× spawner jumps + state in `PlasmaRifleWeaponSpawners.dec` / upgrade-spawner jump + state in `SpecialPowerups.dec` / `specialWheel_PulseCannon` + dispatcher case in `ev_core_special.zsc` / `NO_TACTICAL_WEAPONS` entries in `PB_WeaponTacticalFeel.zc` / `PBWeapUnloadedToken` + `PBWeapUnloadableClasses` entries in `BaseWeapon.zc` / 3 ACS scripts (`PBUpgradeChecker_PulseCannon`, `TogglePulseCannonUpgrade`, `ToggleWeaponPulseCannon`) plus retargeted `PBUpgradeChecker_DragonsBreath` from `SpawnPulseCannon` to `SpawnRifleUpgrade`, recompiled `ACS/cvars.o` (now 162 scripts) / `SpawnFriends9` ally-summon chain (`SummonPulseCannonGunners`, `RecalculatePulseCannonGunners`) in `PLAYER.dec`, with `RecalculateAutoShotgunners` retargeted to `Goto StandstillLoop` (also incidentally fixing a pre-existing infinite autoshotgun-marine respawn loop). Cross-cutting helpers `PulseCannonBall`, `PulseBeamLaser`, `PulseCannonBeamTrail` were renamed `ChexBall` / `BHBeamLaser` / `BHBeamTrail` and folded into their only surviving consumers (`actors/Weapons/Slot4/ChexRifle.dec` and `actors/Weapons/Slot8/BLACKHOLE.dec`) so no `Pulse*` identifier remains in the project. The five **Purple-Plasma FX actors** that lived inside `PulseCannon.dec` (`PurplePlasmaFlare`, `PurpleFlareSmall`, `PurplePlasmaParticle`, `PurplePlasmaFire`, `PurplePlasmaParticleSpawner`) were also independently consumed by `actors/Equipments/VoidGrenade.dec`, `ChexProjectile`, and `GLDEFS.txt` (a `pulselight` binding for `PurplePlasmaFire`). Smoke testing surfaced the unresolved references; the two flare classes were relocated into `actors/Effects/FLARES.dec` (next to `Flare_General`) and the three particle/spawner classes into `actors/Effects/PARTICLES.dec` (next to `BluePlasmaParticle*`), preserving include order so DECORATE parses cleanly with zero warnings.

### Documentation

- **Inline comment audit (phases 1–3):** One hygiene programme: drop redundant `//` prose already covered by **`AGENTS.md`** (§3–§4 / §10), keep credits / attribution / include-order traps / non-obvious engine quirks (migrate unique bits into **`AGENTS.md`** before stripping). **Phase 1–2:** **`lumps/includes/{gldefs,textures}/*.inc`**, **`bdv22_modeldef.inc`**, root lumps (**`MENUDEF.txt`**, **`SBARINFO.txt`**, **`language.enu`**, **`GLDEFS.txt`**, **`KEYCONF.txt`**, **`gameinfo.txt`**, **`zmapinfo.txt`**, **`ANIMDEFS`**, **`DECALDEF`**, **`modeldef.txt`**, **`Textures.txt`**, **`XHAIRS`**, **`trnslate.txt`**, **`Fontdefs.txt`**, **`TEXTCOLO.txt`**, **`doomdefs.bm`**, **`doommonsters.bm`**), masters **`DECORATE`** / **`ZSCRIPT.zc`**, non-weapon **`zscript/**`** and non-weapon **`actors/**`** branches; **`SNDINFO.txt`** second pass removed **~375** decorative `//` lines (**~55** retained path / attribution hints); **`LOADACS.txt`** / **`CVARINFO`** phase-2A (GK labels; **`SpawnBalance`** → **`AGENTS.md`**). **Phase 3 (formerly locked):** **`zscript/PBPDA.zc`** — removed duplicated blocky / preview / resolver / mouse-bridge commentary now spelled out in **`AGENTS.md`** §10 (file carries **no** `//` lines); §10.9 documents **`InputProcess`** title-screen / early-tic guard and **`PDAMode`** consume semantics. **`zscript/Weapons/**`** (Slot melee specials, Slot4–Slot6 ports, **`BaseWeapon_MPExecutionHelpers.zsc`** repetitive MP-execution scaffolding / commented stash blocks — Realm667 Blood Ghost / Blood Skull notes kept), **`zscript/Monsters/**`**, **`actors/Weapons/**`** (**Slot1–Slot9**, **`MeleeWeaponPack`**), **`actors/Monsters/**`** (tiers + **`GloryKills.dec`** shards): same rubric — preserve **`PDA_Preview_*`** DECORATE chains and **`BaseWeapon_StagingShim.zc`** staging-parity header as load-bearing docs. Some discretionary `//` lines may remain where they encode staging-parity or per-actor behaviour not yet summarized in **`AGENTS.md`**. Retained everywhere load-bearing: PB **`DECORATE`** credit line, **`//#include "actors/Weapons/Slot8/BFGBEAM.dec"`** stub, Marisa Kirisame / Pixel Eater lines in **`ZSCRIPT.zc`**.

- **`README.md`**, **`AGENTS.md`**, **`CREDITS.txt`**: maintenance across `63cf5d62e`, `5213ff4df`, `27d6d37d6`, `a7483b3f1`, and the large **`0b3ae6a04`** doc pass.
- **`docs/weapon_cycle_bug_investigation.md`**: appended a closing **Resolution** section noting both root causes are now resolved and the file is historical (Root Cause 1 fixed 2026-05-01; Root Cause 2 eliminated 2026-05-02 via the Pulse Cannon removal). **`AGENTS.md`** §4 "Other shipped weapon-special wheels" bullet had its **`PB_PulseCannon`** example dropped. **`PORTING_ADDONS.md`** Slot 6 row in the staging-vs-PB-2022 comparison no longer lists **`DUALPULSECANNON.dec`** / **`PulseCannon.dec`** for PB 2022 and the status note now flags that PB 2022 no longer ships a Pulse Cannon at all.

### Pending / follow-up (commit when ready)

- **`Fontdefs.txt`**: Glory Kill HUD entries should use **`graphics/hud/...png`** TexMan paths so font textures resolve when `GRAPHICS/HUD/` is shipped.
- Ensure **PB_Staging** parity for **`GRAPHICS/HUD/glorykills/newhud/`** and **`SPRITES/WEAPONS/GloryKillLauncher/ShoulderCannon/`** is committed so zips include Eternal HUD + shoulder launcher art.
- **Mastermind Chaingun**: document **`pb_plasma_wallclimb`** / self-splash behavior in a focused commit if implemented locally.

---

## Reference commits (umbrella)

| Topic | Example hashes |
| --- | --- |
| SSG / Peacekeeper | `d6bbe88db`, `e0076241d`, `f5eba713f` |
| NeoHMG shield | `0b3ae6a04`, `fef269dba` |
| Shield Saw / Mastermind CG | `b5c61d297`, `07d57f6b7` |
| UZDoom / weapons | `a3a7ede64`, `c70971995`, `4d8c11fd0`, `11504c30e` |
| Large fold (executions, tools, base weapon) | `0b3ae6a04` |

### One-line summary (merge / tag)

`SSG & addon art; NeoHMG shield deploy + tactical; UZDoom + weapon fixes; execution/GK fold; docs & smoke tools`

### Short multi-line summary

```text
SSG/Marauder: Peacekeeper fold, refreshed sprites, missed-lump pass.
NeoHMG: deployed shield placement, floor sampling, no player tracking,
  lock yaw, death burst credit; tactical feel bypass on fire; wheel freeze reset.
Weapons: Metal Sniper reload/backpack, Battle Rifle burst/reload, M41A/HASG fixes;
  disable broken DTech Minigun; RiotShield HUD/dup/shots fixes.
Engine: UZDoom NoDelay / Vel.LengthXY / A_RadiusGive startup fixes.
Content: large execution + Glory Kill + BaseWeapon MP helper import;
  gore/killstreak/menu/logo/README maintenance; smoke/zip tooling.
```
