// Brutal Doom 22 (BDv22) optional model bindings. GZDoom loads all modeldef* lumps.
// When you add MD3/PNG from the BDv22 add-on, place under models/BDv22/ and add Model { } blocks here
// (or in additional files under lumps/includes/ and #include from modeldef.txt). Do not modify stock modeldef.txt in place; append or add shards.
